{
  "id": "single_worker_verified",
  "name": "Single Worker Verified",
  "solves": [
    "Small scoped changes that still need independent verification",
    "Tasks where one worker can implement and self-check within a narrow boundary"
  ],
  "fit_for": [
    "single file or small package edits",
    "low coordination overhead",
    "clear acceptance criteria"
  ],
  "phases": [
    {
      "id": "implement",
      "kind": "worker",
      "task": "Implement the requested change within the assigned write boundary.",
      "acceptance": [
        "Changed files stay inside the assigned boundary",
        "Implementation matches the requested behavior"
      ]
    },
    {
      "id": "verify",
      "kind": "verification",
      "task": "Run the focused tests or checks that prove the change works.",
      "depends_on": ["implement"],
      "acceptance": [
        "Relevant tests or manual checks are recorded",
        "Known residual risks are reported"
      ]
    }
  ],
  "final_checks": [
    "Worker reports changed files",
    "Worker reports verification result",
    "Worker reports residual risk"
  ]
}
