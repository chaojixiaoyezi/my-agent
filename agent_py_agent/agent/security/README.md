# security

LLM: future security boundary layer for permission checks, sanitization, trust policy, and safe defaults.

给人看的解释：
以后路径权限、用户输入净化、模型输出可信度、危险动作确认等硬边界可以沉到这里。
具体工具仍在 `tooling/`，子代理领域规则仍在 `subagents/`。
