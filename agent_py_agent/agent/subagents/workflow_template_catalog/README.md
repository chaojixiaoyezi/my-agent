# Workflow Template Catalog

这里预留给后续 workflow 模板外置化。

- 内置 workflow 仍先沿用 `agent_py_agent/agent/subagent_workflows/builtin/*.json`。
- 后续迁移目标：把“角色模板 role templates”和“流程模板 workflow templates”分开。
- 用户自定义 workflow 目录继续走 `.agent/workflows/user`，后续可以扩展成同一套加载/校验规范。
- 这一轮只预留目录和说明，不改变现有 workflow 行为。
