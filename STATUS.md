# 当前状态

第 4 步本地源码已接通安装、配置、实际启用、普通工具组合、停用释放、重新启用及卸载。
卸载使用释放返回的完整记录做原锁 CAS；原成功结果持久化并严格读回后，才消费退出证明和回收无人引用的旧包。
原准备仍在执行时保留安装与环境；同包重装保留其包，旧请求不控制新安装，UNKNOWN 不因后来清理成功被改写。
公开目录 v3 派生原提交安装引用，避免同包卸载重装后旧目录再次有效；安装表和原操作历史不新增权威副本。
显式 `/plugins@插件ID` 已本地接到原 HostCommand/ToolExecutor/MCP；工具参数和宿主选择分别绑定，业务用户不借管理员资格。
审批在原执行区间等待，明确批准一次后复查固定代次；重复提交只读原结果，单次连接退出与业务结果分别报告。
TUI/Gateway 的独立命令审批已本地接通，复用原 FIFO 与文件桥；规范 owner 由原服务端明确返回，不由客户端猜测。
断连只取消本命令，结果不明仍查询原编号；完整多 TUI 装卸尚待验收，未推送部署，不进入第 5 步。
本片 22 个相关文件 511 项通过，源码无漂移；运输及取消覆盖边界见 [TESTS](TESTS.md#第-4-步独立命令交互审批开发验证)。

以下为前片提交及其独立验收记录，当前进度以上述段落为准。

第 4 步当前片：实际 `/plugins enable` 已沿原管理与工具执行链完成环境准备、完整 MCP 目录匹配和候选退出确认，再提交同代 active。
原 Registry 在新运行前按可信 owner 接入该代工具，各权限视图共用连接但分别生成目录；组件已验证真实工具读取及停用后的旧快照拒绝。
原资源账保存完整 session 清理证明，保留命令终态与 child 回执；未发送拒绝明确结束原操作，发送后失败仍为 UNKNOWN。
缺失完整退出证明的 UNKNOWN 不因 PID 消失变成成功，已提交的清理 redo 可按原事务恢复。
释放/消费、重新启用、卸载、显式插件业务命令及实际多 TUI 尚未完成；未推送部署，不进入第 5 步。
当前开发验收见 [TESTS](TESTS.md#第-4-步实际启用与新运行工具组合开发验证)。
47 个相关文件 1,018 项通过，受测源码无漂移，本地严格 gate 通过；线上 CI 未作为验收来源。

前片：`/plugins disable` 已沿原管理权限与执行链关闭原代，分别清理准确的准备任务和共享 MCP 资源。
阻塞业务调用不挡停用，另一插件和普通任务不受影响；独立 host 准入前后与撤销/冻结交错已做组件验证。
原请求重放、原资源集合与未知清理分别保留；退出记录及 revoked 激活仍保留，release/consume 尚未接入。
该片当时尚无完整 enable 与贡献发布，当前组合见首段；完整装卸及实际多 TUI 仍待完成。
该片开发验收见 [TESTS](TESTS.md#第-4-步管理停用与双归属清理开发验证)。
39 个相关文件 899 项通过，受测源码无漂移；组件验证不充当实际多 TUI 装卸验收。

前片内部接线：插件 MCP 已沿原托管进程启动，固定 owner/激活在 launcher、host 创建 child 前及交接时复查。
排队后的实际发送同时核对原资源、原代和本次任务权限；旧代理不跟随重连，未发送拒绝不关闭共享连接。
原资源清理回执、提交异常和未接管启动的 UNKNOWN 保留；目录锁等待可取消，取得锁后的 redo/提交顺序保持。
完整管理启用、静态目录匹配、贡献发布与撤销收口/卸载尚未完成；本片未发布部署、未新增实际 TUI。
最终 28 个相关文件 681 项通过、源码摘要无漂移，本地严格 gate 通过，尺寸基线不变，线上 CI 未作为验收来源。
前片开发验收见 [TESTS](TESTS.md#第-4-步托管-mcp-与鲜活准入开发验证)；以下逐片结果仅归属于当时受测源码。

此前托管管道和资源归属已有本地实现：stdio 三路直接继承，失败回收未交出管道；session v3 区分任务与共享激活。
旧 v2 仍原版本更新/恢复，任务停止和业务查询排除共享连接，普通裁剪保留其退出证据。
最终 20 个相关测试文件 523 项通过，零失败、错误或跳过，受测源码无漂移；未部署、未新增实际 TUI。
本地严格 gate 与新增内容隐私检查通过，尺寸基线未改；线上 CI 未作为验收来源。
该片当时尚无 MCP 准入，后续本地接线见首段；完整装卸仍未完成，见 [托管合同](docs/design/MANAGED_PROCESS_STDIO.md)。

第 4 步激活权威已有本地实现：原安装表 v3 绑定同一环境计划，准备/发布/撤销在原锁内 CAS，旧代不可恢复。
配置和新一代准备会拒绝尚未清理的激活；撤销不等待准备阶段的配额锁，仍保留原子提交和未知结果。
领域与并发首轮 103 项通过，最终 20 个相关测试文件 549 项通过，零失败、错误或跳过。
本地严格 gate 与新增内容隐私检查通过，尺寸基线未改；线上 CI 未作为验收来源。
该激活 CAS 片当时尚无进程接线，后续见首段；完整装卸未发布部署或实际 TUI 验收。见 [激活合同](docs/design/PLUGIN_ACTIVATION.md)。

第 4 步 MCP 连接边界已有本地实现：旧请求、线程和超时绑定原 transport；永久 stop 不能被注册层重连，目录发布复核原连接。
临时故障仍按原退避恢复，清理未确认时保留原句柄并拒绝替代连接；权限视图共享关闭状态。
163 项相关开发验证通过；最终全仓 18,265 passed、35 项既有 xfail、21 项 skip、5 项既有 XPASS，零失败或错误，本地严格 gate 通过。
全仓发现工作片入口残留旧权限导入，已直接接共享权限裁决；相关 103 项通过、2 项既有 xfail，旧命令夹具修正后的 171 项通过。原失败保留，最终固定源码验证没有漂移。
本片未发布部署、没有新增真实 TUI，线上 CI 未作为验收来源；完整插件激活、持久撤销和卸载仍待接通，见 [连接合同](docs/design/MCP_TRANSPORT_LIFECYCLE.md)。

第 4 步环境准备资源接线已有本地实现：固定计划先进入原 operation 的资源声明，三个准备进程复用原 ProcessSessionStore；准备过程中复查原 holder、代数和锁。
准备进程显式受启动宿主与同一期限约束，普通长期后台默认行为保持。420 项相关开发验证和本地严格 gate 通过，线上 CI 未作为验收来源。完整启用、MCP 发布、撤销和实际多 TUI 装卸仍未完成，本片未发布部署。

第 4 步完整配置替换已有本地接线：HTTP/direct 经原管理员、路径权限及唯一工具执行链保存停用插件的私有设置。
原安装表 v2 原子保存配置、版本及动作回执，v1 来源随首次真实修改显式记录；新目录版本包含安装版本，过期请求不覆盖当前配置。
值只保存在 owner 私有表，不进工具参数/结果或目录；原请求重送及查询不重读来源，UNKNOWN 不重跑。
本片未发布部署、未新增真实 TUI。启用前资源绑定、激活 CAS、MCP 发布、撤销与卸载仍待完成；不能进入第 5 步。
本轮开发验证及覆盖边界见 [TESTS](TESTS.md#第-4-步私有配置开发验证)。
最终 21 个相关文件 614 项通过，无失败或跳过；完整装卸与实际多 TUI 验收仍未完成。
本地严格 gate 和新增内容隐私检查通过，尺寸基线未改；线上 CI 未作为本片验收来源。

第 4 步独立环境已有内部准备实现：固定本地 wheel 先验证元数据和依赖，再在最终地址创建 venv，由宿主只读核对安装结果。
原 owner 配额采用非阻塞准入，取消/超时及异常收尾复用原 token、进程出生标识与退出回执，未知退出不当作成功。
临时真实 venv/pip 组件已通过，模块和 `.pth` 陷阱未执行；这不是产品 TUI 装卸验收，仅覆盖具备所需文件能力的 POSIX 宿主。
准备器尚未接启用命令；配置后续进度见首段，激活、贡献发布、撤销和卸载仍待完成。本片未发布部署、未新增真实 TUI，不能进入第 5 步。
开发检查见 [TESTS](TESTS.md#第-4-步独立环境开发验证)；架构边界见 [环境合同](docs/design/PLUGIN_ENVIRONMENTS.md)。
最终 14 个相关文件 319 项通过、无失败或跳过，本地严格 gate 及新增内容隐私检查通过，尺寸基线未改；线上 CI 未作为本片验收来源。

第 4 步管理入口已有本地接线：HTTP/direct 复用现有管理员、owner 路径和工具策略，安装进入原运行/工具账，保存包后保持停用。
同一请求不重复执行，来源删除后仍可查询；断连保留未知，工具终态与运行收尾分别报告。`enable_plugins` 关闭后原结果仍可读。
33 个相关文件 771 项通过、1 项既有跳过，本地严格 gate 和新增内容隐私检查通过；具体覆盖见 [TESTS](TESTS.md#第-4-步管理执行与只读查询开发验证)。
管理接线片未发布部署、未新增真实 TUI；环境准备及配置的后续进度见首段，激活和撤销仍待完成。

取消后的精确回读已在本地修复：未启动占位保留原领取身份和输入指纹；坏结果保持原文并拒绝读取，不制造空账或成功。
12 个相关文件 360 项开发检查通过，未发布部署、没有新增实际 TUI；不是新版 TUI 资源漏停复现。后续管理接线进度见上，完整装卸仍待完成，见 [TESTS](TESTS.md#未启动取消记录的精确回读开发修复)。

第 4 步宿主命令身份已有本地源码：原 RuntimeDB 事务把重复请求绑定到同一独立 pending 运行，创建链与普通代理共用。
终态结果沿原工具账核对归属和输入，重放标明本次未执行；截断、矛盾及 UNKNOWN 结果不能成为成功。
请求登记首片当时尚无管理授权、执行收口与 HTTP 适配，后续本地接线见本页首段；各片未发布部署，详见 [宿主命令合同](docs/design/HOST_COMMAND_EXECUTION.md)。
此前请求登记片 18 个相关文件 429 项开发检查通过，未调用真实模型或新增实际 TUI；历史已部署版本和第 3 步验收不变。
本地严格 gate 与新增内容隐私检查通过，未改尺寸基线；线上 CI 未作为本片验收来源。

第 4 步本地源码已有静态包校验和默认停用安装记录：原 owner 目录、并发 CAS、原子提交和原请求回执已接通。
包先于状态保存，损坏记录不会被覆盖，提交后异常及清理失败保留实际提交分类；共享锁仍保持旧后台锁名与顺序。
本片 425 项相关检查与本地严格 gate 通过，源码并发和文件边界的检查不替代真实装卸验收。
安装事实首片未发布部署或新增实际 TUI；后续管理适配已本地接线，独立环境、激活和撤销仍待实现，见 [包合同](docs/design/PLUGIN_PACKAGES.md)。

第 3 步宿主目录片 `3730497fc` 已发布 main 并同包部署双机，每端 1,219 个包文件一致，各一个 Gateway、默认入口同版。
669 项相关回归通过、1 项既有跳过，本地严格 gate 通过；线上 CI 列表为空，未作为验收来源。
实际 TUI 151—153 已验补全/暂存恢复、静态错误、运行中命令隔离、普通工具链和两个子代理并行；所测框架范围通过。
151 漏原句句号、153 使用错误时钟并无据解释磁盘环境，完整交付失败单列；152 数据与交付通过，详见 [TESTS](TESTS.md#第-3-步宿主目录与实际-tui-151153)。
目录只读、身份沿原 owner 解析，客户端原候选版本与过期拒绝已接线；实际贡献仍为空，不宣称真实动态插件换代已验。
第 3 步公共命令的本轮框架范围收口；第 4 步安装事实正在本地开发，用户装卸、激活和调用链尚未接通，十步 Goal 仍为 active。

第 3 步参数与补全修复 `faa12068a` 已发布 main，并以同一 wheel 部署双机，每端 1,214 个包文件一致、各一个 Gateway。
公共声明生成解析、帮助与补全，CLI/HTTP 静态回执同源；483 项相关回归和本地严格 gate 通过，线上 CI 未作为验收来源。
TUI 143 的 Enter 擅自追加 `-h` 失败保留；新版 TUI 146 已实际验证 Tab 接受候选后正常提交，两端各 13 类静态命令检查通过。
146 的文件任务、147 的两个子代理并行及 148 的文件工具链已核对；148 数据正确但生成校验器漏检，框架和模型质量分开记录。
149/150 普通正文的插件示例句原样保留；150 的活动请求中两条静态命令没有变成插话或 Shell，原 attempt 自然完成。
参数次片所测框架范围通过；测试准备失误、校验器限制与覆盖边界见 TESTS。当时动态目录尚未接线，后续宿主目录片见本页首节；装卸和业务调用仍未开放。

资源停止修复 `6713c769f` 已发布远端 main，并以同一个 non-editable wheel 部署本机和测试机。
每端 1,210 个包文件逐项一致、各一个 Gateway，默认入口同版，原参数、完整环境与私有配置保留，回滚已留存。
实际 TUI 138—142 使用官方 MiniMax-M2.7，已验证三类控制分离、主后台资源停止、三个并行孩子、交互 PTY、真实孙代理及另一会话隔离。
TUI 139 的独立采样程序自然运行 900.534 秒；299 条记录和两份统计已独立读回核对，不把记录跨度写成程序总时长。
137 的历史停止失败原样保留；新版本所测停止链路通过，模型脚本错误与未覆盖旁支分列，详见 [TESTS](TESTS.md#整任务停止发布与实际-tui-138142)。
最终同源码全仓 17,642 passed、35 项既有 xfail、21 项 skip、5 项既有标注 XPASS，零失败或错误。
该停止版本的本地严格 gate 已通过，线上 CI 运行列表为空，线上 CI 未作为验收来源。后续公共命令与宿主目录进度见本页首节。

执行、提交/投递、Gateway 请求边界和会话存储领域组合已完成结构迁移。
最后的 Guidance 继承已移除，Store 从 1,990 行降为 134 行，聚合入口不再继承领域实现。
本次四批重构已完成实现及本机验收：三类控制已分离，后台终端目录与主代理审批缺口已修复。
历史模型履约失败及未覆盖场景继续单列；auth 与 Jev 不在此次 Goal 范围。

本页只保留当前可核实状态。历史切片见 [重构设计](docs/design/MAINTAINABILITY_AND_JEV_REVIEW.md#第四批存储能力由深继承转为明确组合)，测试规则见 [TESTS](TESTS.md)。

## 当前重构验收

公共命令首片已发布并同包部署双机：核心名称/别名、会话尾部语法、帮助和静态补全归 `agent/command_catalog.py`；删除 CLI 旧目录和会话旧识别函数。`/plugins`、`/plugins@` 及异常后缀明确返回尚未开放，不进模型、插话、Shell 或旧停止分支。原控制类型、参数解释、权限和持久回执保持；插件装卸尚未实现。

用户已明确选择框架功能与模型交付质量分开记录，十步 Goal 为 active。第 1 步框架依赖及第 2 步后台调度的本轮结构迁移与相关框架验收已收口；历史长任务交付失败和未命中的内部支路保留。第 3 步实际 TUI 134—137 已验证保留命名空间、普通工具链和暂停/活动中断区别；137 暴露的主后台资源停止缺口已修复发布并由新版独立用例复验。参数与宿主目录片也已部署复验，公共命令本轮框架范围收口；真实插件装卸与执行授权在第 4—6 步继续。

137 的原采样程序在停止请求后至少 265 秒仍存活并写入，后来在 600 秒自然退出，原三个进程已消失，停止失败保持。分派未混淆 `/stop` 与 `/interrupt`：当时安装版明确停止只接了 PTY 与子代理取消，后台登记又缺独立执行归属；当时核对的实现与命令首片之前字节相同。修复须守住启动期间的停止竞态及后来恢复的新资源，不根据正文猜测或扩大到整个会话。当时安装版另有 direct/local 中断继续进入子代理取消的语义差异；当前源码已修正，但不据 Gateway 实际用例声称覆盖该旁支。134/136 最终数据正确但校验器及完整读回有缺口；135 文件与时长通过、其它控制未覆盖，详见 TESTS。

已发布的停止链先关闭原 task/run/attempt 和排队启动权限，再冻结主任务及原子树的后台/PTY 清单，锁外清理不追随恢复后的新任务。
启动先预留、再绑定 host/child、最后明确交接；独立资源交接后不受旧回合 token 控制。Store 的版本 CAS、目录锁及 redo 保留唯一持久权威。
终态业务历史和 UNKNOWN 锁不改写；独立 runner 由原心跳转交精确取消，宿主退出另行核对，不按共享 PID 强杀整树。
managed 启动使用原 pending ID；无数据库模式在原 canonical 一次性消费非空身份，旧快照和迟到启动不能回滚停止。
插话重放在原排序 mailbox 锁内复读最新 pending，再准确预留；direct/local 与未晋升请求使用正式运行绑定，不猜历史任务。
参考本地 Codex `578c1b2` 的独立中断/后台清理、关闭派工入口及固定集合清理；只核读相关源码与测试，未运行参考项目测试。
全仓、源码竞态覆盖、真实 TUI 和仍未实测的旁支分别见 TESTS；停止修复不代表所有模型交付都通过。
用户反馈 Working 图标偶尔消失，目前没有稳定复现；按用户安排延后至本轮目标完成后排查，不改显示逻辑或扩大当前验收范围。
第 2 步进度策略首片已通过相关回归和 TUI 110—113 框架验收。供应退避次片已独立并部署，197 项相关回归通过，独立复核确认共享状态、时钟和错误分路等价；TUI 114—119 的正常链路、实际连接失败后冷却与自动续接、另一前台任务隔离及原设置回切已验。该切片已收口，详见 [执行台账](docs/tasks/REFACTOR_PLUGIN_GOAL.md)。
路由切片已分离到 `background_goal.py` 与 `background_routing.py`，删除混合职责 GoalMixin，353 项相关回归通过；独立审阅核对原目标事务、路由惰性读取和执行顺序。同版部署后的 TUI 120—123 已完成本片所测框架链路验收，模型交付失败单列。
租约/恢复片已发布并同版部署：`background_claim.py` 接收原 claims 域与精确能力，`background_recovery.py` 每次重读权威并去重日志；旧执行私有方法和恢复 helper 已删除，共享心跳未改。25 项新增合同先在旧实现通过，迁移后的 324 项定向回归与独立复核通过；运行中续租、客户端重连、同车道排队、原请求有序恢复和取消后复用原终端均有实际 TUI 证据，后续公共命令首片单独验收。

租约与资源换代片当时的运行代码 `f78ee8db2` 已正常快进发布远端 main，双机使用同一 non-editable wheel；默认入口与各自唯一 Gateway 同版，每端 1,193 个包文件逐项一致，原启动参数仅更换 runtime，完整环境及私有配置保持，回滚已保留。此次安装切换发生在空闲状态，不能当作活动任务恢复证据。
该源码的本地严格 gate 已通过，对应线上 CI 运行列表为空，线上 CI 未作为验收来源。Git 发布使用已有系统代理的单次命令配置，没有修改用户全局配置。

- 新版 TUI 124 的同一采样进程自然运行约 1080 秒，客户端退出重连、目标暂停恢复、运行中真实续租及执行收尾已验；最终间隔统计仍错。125 数据正确但校验器覆盖和全文件读回不足，127 数据及原需求交付通过。
- TUI 126 的孩子末段加速补批，记录仅约 870 秒，未满足十五分钟需求；原定时工作在前台释放 claim 后才到期，竞争未命中。另发现既有框架问题：成功审批后的 STABLE 资源记录在新执行代次被误置 DIRTY。三项旧实现失败的回归已固定，只收窄原事务的更新条件；186 项相关回归及本地严格 gate 通过，修复已发布并同包部署双机。历史 DIRTY 不清理，原失败保留；新版真实复验见下文。
- TUI 128 已证同车道唤醒排队及原 claim 释放后接手，内部 acquire 拒绝分支没有直接真机证据；程序只跑约 220 秒且读回/哈希覆盖不足。129 普通任务的最终数据正确，但报告首两条和全文件读回不足。128/129 分别因观察脚本误读目录结构和早期身份而未执行停启，准备错误单列；后续 TUI 130 已实际补验活动恢复。所有控制仅针对测试客户端或应用进程，系统网络保持。
- TUI 130 在首轮模型流出现、尚无工具副作用时有序停启同一 Gateway；原请求和 run 保持，旧 UNKNOWN 经 `confirmed_noop` 恢复到第二代并完成，5 对工具、3 个成功持久操作，零锁。数据、汇总和报告正确，但模型主动把产物写到 owner 根；恢复上下文保留正确 cwd，完整交付失败。
- TUI 131 只写脚本并口头询问权限，没有派工或真实能力裁决，原计划分支未覆盖；不追加提示。132 的 18 条明细、分组、总数和哈希正确，框架链路通过；生成与校验脚本未读回，校验器失败退出语义仍弱，完整交付失败。
- TUI 133 已实证测试 Goal 暂停后当前 claim 仍运行，中断后同一 Goal 恢复并进入第二代；原交互终端的 STABLE 记录跨代全字段不变，新代继续使用原累计值，三批为 30/100/210。仅启动一次解释器，模型自行退出后回执为 exit 0；27 对工具、26 次官方模型调用，Goal/run/claim 收口，锁为零。被中断的等待命令 UNKNOWN 整行保留，不自动重放或改写成功。按原需求的间隔、数据读回和报告核对通过，REPL 自修错误保留。此例覆盖 cancelled 后续代，不冒充自然 done 后续代或 131 的能力裁决；详细边界见 TESTS。
- 路由版本 TUI 120 的同一程序自然采样 960.000 秒、97 条，暂停未取消当前 claim 或采样，恢复沿原 Goal/任务继续；3 次 Compact、82 对工具及全部 54 个执行代次收尾已验，原进程退出零。报告的间隔统计和内存单位错误，交付仍失败；完整 CSV 已送入模型，不能归因为正文缺失。
- TUI 121 的工具链与最终 30 行数据正常，但校验器硬编码错误金额，实际回执提示不一致而模型仍报告全部通过，交付失败。TUI 122 三名孩子的执行时间实际重叠约 29.234 秒，数据与报告正确、原生历史及归属收尾通过；父级在全部孩子完成前读取源文件，违反原需求顺序，交付仍失败。实际合并发生在全部完成后，不扩大为合并数据错误。
- TUI 123 的一次定时运行、原会话唤醒和最终回复通过，20 行平方表及平方和 2870 正确，原 job 按一次性合同退休。四路业务均自然结束，双机无活动 claim 或待处理请求，历史 TUI 保留。租约迁移、活动任务的 Gateway 重启恢复及其它通道回退未由这些用例证明；详细范围见 TESTS。
- TUI 117 的原定时工作经历约 609 秒的既有请求重试，typed 连接失败到达供应守卫后冷却 30 秒；原 run/wake 保留，新 attempt 自动续接约 17 秒后完成，故障与续接期间 Gateway 未重启。
  TUI 118 的整个普通前台任务在冷却截止前约 5.760 秒完成，证明该线程冷却没有阻塞同 Gateway 的这个前台会话；不扩大为另一后台唤醒或跨 owner 公平性结论。
  117 的 20 行平方表、平方和 2870、哈希及报告正确；定时时区与报告转义曾由模型自行修正，失败记录保留。只暂停测试专用本地代理，系统网络、系统代理、默认路由与 SSH 未受影响。
  原启动参数、完整环境和配置已恢复，专用代理关闭；新 TUI 119 的官方 M2.7 读写通过。透明代理基线 116 另记，不把代理连接数当成模型请求数，详细范围见 TESTS。
- 供应状态版本的 TUI 114 本机单子代理与 TUI 115 测试机订单均各一条真实需求，共 18 次官方 MiniMax-M2.7 请求，无模型/网络重试；16 对原生工具匹配，读回与磁盘相同，任务和所属进程自然结束，TUI 保留。
  框架和最终交付均通过。114 为数组 1—10、个数 10、总和 55；115 初版分组金额算错，同一校验程序先退出 1，模型自行修正汇总后退出 0，原失败保留。其校验器未覆盖的约束另记，当前产物已独立核对。
  两路均未命中供应退避分支，不以正常任务证明供应故障或自动恢复通过。
- 前一进度策略版本的短回归 TUI 111/113 普通订单与 TUI 112 单子代理均各一条真实需求，28 次官方 MiniMax-M2.7 请求，无失败/重试；原生工具配对、实际读回、任务收口和资源归属已验。
  112 交付通过；111/113 最终订单和汇总正确，但生成校验器漏查部分约束、报告将未执行的比对写成已通过，交付质量单列，不升级为框架故障。
  三路都未出现 progress policy 记录，不宣称实际命中本次内部纯函数分支；这部分由算法等价和定向回归覆盖，旧长题失败不被短题覆盖。
- TUI 110 三路原程序各启动一次，实际记录跨度约 1339/1353/1430 秒，共同记录跨度约 1285 秒；父级完整读回与磁盘逐字相同，39 对原生工具匹配，任务和 claim 自然结束，归属采样进程均退出 0。
  交付仍失败：生成脚本的时钟、进程 CPU/内存含义不符，报告间隔计算错误；计数口径歧义和首尾记录覆盖限制另列。测试者审批准备造成的等待不计采样时长。
  此路同样没有 progress policy 落账，不能据此宣称内部策略真机命中或整个第 2 步完成。

- 用户恢复 Goal 后新增四组同题对照，仍使用当前安装版。my-agent 的官方 M2.7 与 OpenCode Flash、Codex 与 Free-Code 的官方 M2.7 均已通过真实 TUI 连通及文件工具预检。
  TUI 99/100/104 的三子代理短题数据与分工通过，自检覆盖限制单列；Codex 的 TUI 101 数值/表头失败、TUI 105 单模型目录复测的摘要哈希错误均保留。
  长题 TUI 106 保留重复启动、中途数据误判和过期哈希失败；TUI 107 核心采样通过、报告覆盖有保留项；TUI 108/109 已按实际控制中断收尾，全部本轮采样及遗留测试资源已退出，TUI 保留。
  TUI 109 的 A/B 子代理提前结束触发其宿主清理后台采样，C 完成 900 秒后又误读 A 并申请覆盖重跑，实际 TUI 拒绝并中断；准备失败和被拒绝动作不冒充已执行。
  框架和交付两栏分开记录，不把外部框架也失败当作 my-agent 自动通过。准备错误、协议差异、人工审批等待与真实业务失败分别记录，见 [对照矩阵](TESTS.md#模型与框架对照)。
- 诊断清理已发布部署：删除 `structured-repair` 场景及专用替身，保留的 runner 重试实现与原版一致；本轮未改 Gateway/子代理执行语义。30 项相关回归通过、3 项既有 xfail、1 项既有 skip，本地严格 gate 通过。
  实际 TUI 91—94 各一条原始需求、官方 MiniMax-M2.7；两路单子代理均由孩子写文件、父级读回并正确汇总，另两路订单的原始数据、汇总、哈希和报告一致，校验脚本确已执行退出 0。
  TUI 92 初版汇总数字错误，校验脚本又误跳过首条数据；两个退出 1 的回执保留，模型先修汇总、再修校验器才成功，测试者未补文件或代跑校验。四路短回归通过不关闭下面的长任务质量失败。
- 子代理声明窗口事实传递已发布并同版部署：TUI 85 的完成事件包含未满窗口及剩余秒数，原活动回合事件投影与后台完成清单会丢字段。
  现由中性完成合同统一筛选原生布尔/非负整数，不重新计时；缺失、字符串、负数及布尔冒整数均不补造事实，原 DONE、Goal 和审批保持。
  旧实现四项回归失败，新实现 446 项相关回归通过、4 项既有 xfail；真实旧事件只读回放保留两条窗口事实及三个原 DONE 状态，不能把回放算作新版实际 TUI 通过。
  新版实际 TUI 89 已核对三名孩子的后台模型上下文：冻结的剩余 962/799/100 秒与原观察一致，正文裁剪及后续消费仍保留，原 DONE 不变；此传递切片通过，完整长任务仍失败。
  另逐字比对三份脚本的工具入参与磁盘一致；原生完成预览虽被裁剪，但标记及完整报告引用保留，实际报告存在且含原始全文。此修复不解决生成程序或报告质量失败。
- 新版四路 TUI 87—90 均为官方 MiniMax-M2.7，各一条原始需求。TUI 87 模型误将阶段任务派成持续 Goal，未配置声明窗口，写完文件后反复续轮；实际 `/stop` 后孩子已取消、进程已退出，不计窗口字段覆盖。
  TUI 88 的订单数据、统计和哈希正确，但没有保存最终回复声称可运行的校验脚本，完整交付未通过；TUI 90 的对应数据、报告及实际执行的校验脚本通过。原始失败保留，测试者未补文件。
  TUI 89 三个原采样程序最终各启动一次并自然退出 0，A/B/C 为 61/60/62 条、900.214/885.903/915.098 秒。B 的生成程序只安排 59 个间隔，未满足十五分钟；三个记录窗口共同覆盖 698.865 秒。
  父级实际 `process_session list` 曾收到后台进程和日志引用，但之后未等 A 结束就把中途 51 条当最终结果，误称 A 提前终止；最终 A 为 61 条，哈希已不同。报告又将 B 判为达标、重叠写成 859.9 秒、把 A 的实际 15 秒间隔写成约 1 秒，首尾各两条也未满足。
  三份脚本的写入参数与磁盘逐字一致，原生工具记录成对；现有证据不支持将这些数字和退出判断错误归为工具丢数据。两端本轮无活动 claim 或待审批、无仍运行的测试进程，历史 TUI 保留。
- 上一安装版追加实际 TUI 84—86，三路各只有一条原生用户需求，均选择官方 MiniMax-M2.7；当时整体基线未通过，失败仍保留。
  TUI 84 的单进程程序首次写入后对关闭文件执行 flush，工具如实返回退出码 1。模型随后用补丁删除原记录并申请重启，违反保留记录及一次启动要求；测试者拒绝重启并通过实际 `/stop` 停止，最终界面已中断、原 PID 已退出。
  TUI 85 的三个后台程序各只启动一次，原审批通过；A/C 子代理先自然结束，程序仍独立运行，不能从孩子 `DONE` 推断进程退出。
  三份数据分别为 60/62/62 条，记录共同覆盖 885 秒。A 的结束写入位于文件上下文之外，约 900 秒后抛出关闭文件异常，宿主退出回执为 1；B/C 为 0，三个实际进程均已退出。
  父代理未查询后台进程或读取其日志，却把 A 的缺记录猜成宿主提前回收并将 Goal 标成 complete；这是本轮模型判断失败，不能当成宿主强杀证据。
  B 将当前 RSS 当最大内存；实际单调时钟间隔含 15.682/14.318 秒，报告按整秒墙钟写成全部 15 秒。汇总也未交代末尾结束记录的近零间隔，首尾各两条要求未满足。
  TUI 86 普通订单对照的 30 条数据、三组汇总、哈希、首尾及报告一致；模型实际执行校验程序并退出 0。该对照通过不抵消两轮长任务失败。
  工具历史及现有合同表明父子共享同一后台进程查询作用域；本轮父级没有调用已有查询入口，尚无证据支持新增进程状态或完成裁判层。
  原始程序、日志、退出回执和只读复算均留在仓库外，未由测试者修复产物；84—86 的测试进程均已退出，不重启 Gateway 或重复采样掩盖失败。
- Shell 输出行数修复已发布部署：实际工具账中的三行哈希被框架写成 `stdout_lines=4`；旧计数对任何非空文本额外加一，末尾 LF 因此重复计行。
  模型回执、截断说明、stdout/stderr 展示已共用采集行数，空行保留、末尾 LF 不虚增。此修复不改变进程、权限或输出正文。
  旧实现六项边界回归先失败，修复后 118 项定向及本地严格 gate 通过；本机 TUI 80、测试机 TUI 82 的六项原生回执和展示行数一致。
  空、无末尾换行、末尾换行、空行、中间空行及 CRLF 的实际值为 0、1、1、1、3、2；82 的 stderr 展示同样通过。四路均为官方 MiniMax-M2.7、各一条原生用户需求。
  TUI 81 三个孩子各自写文件并回传，父级读取三份实际内容后合并；150 条、逐项平方、总和及三个哈希与报告一致，但不能据此保证所有模型报告正确。
  TUI 80 未按要求生成 stderr，82 绕行修脚本后虽覆盖六项，却把未换行的非空文本误判为零行；报告结论不计通过。
  TUI 83 普通订单数据、三组汇总、哈希、首尾和模型运行的校验程序通过；报告写成“不含 header 共 31 行”，实际含表头 31 行，表述失败保留。
  上述质量错误不由测试者改产物；80—83 属于短回归，后续长任务复验见 84—86，不以短回归关闭原十五分钟采样失败。
  另核对 TUI 71：三个 `read_file` 原生结果完整，含表头文件明确到第 51 行；父级交接没有给普通孩子添加质量验证结论，报告仍误写 50 行。
  因此本修复不能抵消报告质量失败；不新增格式专用验收或隐藏最终裁判轮。
- 子代理 Goal 自动续轮遗忘同参拒绝：TUI 66 现场与确定性回归均复现；同一 attempt 重建模型参数时丢失宿主拒绝列表。
  修复 `d883fb51c` 已推送 main 并同版部署双机，宿主参数传递同一列表并覆盖前后台 Compact；批准不提升，三类控制语义保持。
  529 项相关定向及本地严格 gate 通过；两端各核对 1,186 个包文件一致，默认入口与单 Gateway 同版、配置原样且保留回滚；未查询到此提交的线上 CI 运行。
  TUI 70 在实际拒绝后的下一 Goal 自动轮重试同参，直接返回拒绝、handler 未执行且未再次弹窗，此子项通过。
  之后模型换 PTY 被再次拒绝，又通过嵌套 Shell 的后台操作符执行，产生了真实数据；前台超时及 TUI 停止后仍见所属进程存活，随后自行退出。
  这段无审批操作竞态，按新的命令执行/进程归属缺陷保留；不能用同参拒绝修复抵消。
  后续本地修复已通过 282 项主定向和 81 项相邻回归（含重跑）：字面嵌套 Shell 后台启动在执行前拒绝，外层进程退出后的原组成员仍由统一终止入口回收。
  旧实现的两个孤儿进程回归和六个嵌套后台回归先失败，新实现通过；组长出生标识变化、非独立组长和另一运行中进程组均有边界覆盖。实际 TUI 78 保留原嵌套命令并确认未启动，TUI 79 的原管道继承触发五秒超时，数据停在 6 条、十秒后无增长，后续宿主核对无所属进程；修复切片通过。
  TUI 75 普通订单对照的数字、分组、哈希及报告通过。TUI 73/74 因模型生成了不同测试条件未覆盖；76/77 多行粘贴分成多条消息，拒绝后停止，不计整轮验收；78/79 各一条原生用户消息。
  TUI 71 原平方表需求的数据、总和和哈希正确，但报告把两个源文件写为“50 行含表头”（实际各 51 行），整体质量未通过。
  TUI 72 三路各完成约 900 秒、62 条，真实共同重叠 899.59 秒，序号、哈希一致；B 错用 CPU 调度毫秒作为单调时钟。
  汇总漏报每路末尾近零间隔，报告只给首尾各一条，并误称所有偏差小于 0.2 秒；原始程序和失败证据保留，整轮未通过。
  无生产调用的旧 verifier integrity 合同及其自造字典测试已删除；实际 verification 与矩阵相关 22 项定向通过。
  TUI 66 已经实际 `/stop` 收尾；替换脚本还出现采样高速循环。B 后续 PTY 重跑覆盖数据，首次停止输入遇新审批的操作竞态单列，
  不把这一段算作权限绕过。最终各数据 PID 已退出、活动孩子已取消，保留原始失败和现有文件。
- 安装版长任务暴露子代理审批归属缺陷：父会话的展示关联被误判为主代理，具体命令在审批发布前返回 `APPROVAL_REQUIRED`。
  canonical 子代理记录优先的修复 `61d8b4673` 已发布 main，并同版部署到本机和测试机；124 项定向及本地严格 gate 通过。
  两端各核对 1,186 个安装包文件一致，默认入口和单 Gateway 同版，配置原文件与回滚副本保留；本提交未查询到线上 CI 运行。
  实际 TUI 63/64 已验子代理和孙代理弹出审批、批准后执行原调用并回传结果；孙代理使用受管 wait 等到程序运行 90 秒后正常退出。
  此结论限审批/执行链，不将能力授予当作用户批准，也不将数据或报告质量一并判为通过。
  TUI 61 实际单进程持续 930.562 秒、94 条记录、约十秒间隔，数量和哈希核对一致；实际用了 158 轮模型和一次 Compact。
  报告只列首末各一条、内存单位标错且描述了未使用的采样接口，质量未通过；工具账为 33 次 status、零次 wait，两次长 sleep 超时，不能说长等待工具本身失效。
  TUI 62 三份数据最终各约 900 秒，第三份由第二名孩子串行补做，三路共同重叠为零，并发要求未通过；旧失败保留。
  新版 TUI 65 的合并文件与 JSON 数据正确，模型自行修正 JSON 后，报告仍误写含表头 152 行（实际 151）；整体质量未通过。
  TUI 66 因生成程序依赖错误，主代理取消旧孩子并重新派工，未满足一次启动及保留原记录要求；仅继续观察其恢复链，不计原测试通过。
  TUI 67 的订单数据、分组、哈希及实际文件首尾通过。TUI 69 实际拒绝后台启动，回执为 `APPROVAL_REJECTED`、`handler_executed=false`，未生成采样数据或替代执行。
  TUI 68 的普通脚本在当前策略下直接执行，未出现审批，只记为拒绝路径未覆盖；不把它与 TUI 69 混记。
- 初始基线 `104cdd000` 已完成远端发布与双机同版部署；后续审批修复版本及真实验收见上一项。
  本地严格 gate 已通过；本提交未查询到线上 CI 运行记录，线上 CI 没有作为验收来源。旧运行环境和私有配置保留回滚。
  安装版真实 TUI 57—60 均使用官方 MiniMax-M2.7：两端订单数据通过，测试机三子代理的 150 行合并和摘要通过；
  本机合并保留三份表头，实际 153 行却声称验证通过；另一报告把五项检查写成四项。这两处质量失败保留，不能宣布全部验收通过。
  详细十步 Goal 已建立；首轮基线及后续分账结果见 [执行台账](docs/tasks/REFACTOR_PLUGIN_GOAL.md)，不以两端安装成功关闭质量失败。
- 发布前补验：全仓发现响应头等待期间的取消竞态，公共 HTTP open 已按当前结构化中断收口；未取消时仍抛原异常，不增加重试。
  修复前 6 个确定性取消用例失败；修复后 JSON/GET/SSE、实际 socket 中断及回合控制联合 180 项通过。
  相关改动的 138 个测试文件复验通过；最终候选全仓重跑 17,212 项通过、35 项既有 xfail、22 项跳过，零失败。
  该结果不替代安装版验收；本轮 main 发布与双机多 TUI 部署验证按 [执行 Goal](docs/tasks/REFACTOR_PLUGIN_GOAL.md) 继续。
- 本地提交复核：最终组合全仓首轮 17,151 项通过、1 项失败、35 项既有 xfail、22 项跳过。
  唯一失败是后台展示测试仍期待旧文案 `/stop 暂停任务`；按既定控制语义修正后，后台展示、TUI、审批与中断共 222 项定向复验通过。
  修正只涉及该断言、陈旧注释与文档，生产执行 AST 不变；没有把定向结果写成第二次全仓通过。
- 插话回执规则、账本和投影、提交、确认、恢复分别归位，共用原 storage、锁与持久格式。
  一个无调用的消费兼容包装删除；旧持久回执及确认批次迁移继续保留，旧 API 无转发。
- 52 个原定义核对，恢复索引循环拆分后按内联逻辑比较一致；16 个生产调用文件只改变领域访问。
  六个新模块可独立导入，不反向加载 Store 或调度执行器。
- 定向 2,362 项通过、28 项既有 xfail、1 项 Linux 平台跳过；结构迁移版本全仓回归 17,116 项通过、35 项 xfail、22 项跳过、零失败。
  Ruff、文档、严格尺寸、diff 与 clean-package 检查通过，尺寸基线未扩大。
- 官方 MiniMax-M2.7、单 Gateway、多真实 TUI。运行中完整中文插话在有效 running claim 下提交，
  同一回执最终 consumed，模型实际执行新增核对；20 条平方和 2870、全部八秒间隔，哈希与首尾各两条正确。
  采样程序首次文件刷新错误由模型自行修复，测试者未补产物；脚本仍清空旧文件，未满足最初可续写要求。
- 两子代理命令分别实际运行约 110 秒，各十二条、十秒间隔，采样时段重叠 109 秒；合计 650 / 6084。
  父级等待孩子时完整接收追加要求，实际运行核对程序；报告两份 SHA-256、首尾各两条及时间统计全部一致。
- 另一 Goal 在有效暂停状态下约 116 秒数据和计时不变，Compact 一代、原会话重连及恢复后核验通过。
  但提交暂停时已十四条，停稳时十五条全齐，因此不算“停住未完成采样后续采”通过。
- 已完成普通主/子会话的官方模型引用、原生 request/part 去重与用量投影核对一致。
  取消轮存在缺报，不能据取消状态推断完整模型收口。日常配置字节未变，原始日志、路径与证据留仓库外。

- **控制语义已纠正并实测**：Goal 暂停只关自动续跑，当前执行与独立 PTY 继续；回合中断不关闭独立资源，明确任务停止才收回其资源。
  旧记录在 Goal paused 后继续采样不能算资源停止失败；旧暂停入口中断当前回合才是语义混淆。
  已删除 Goal 暂停/清除的中断回调及命名 Goal 的独立清除路径，Esc 在无 Goal/paused Goal 下也不再落入任务停止。
  子代理 Goal 暂停后正常结束的回合不伪标 interrupted。PTY 执行归属、启动预留取消、进程树退出核对只接到明确资源停止。
  TUI 45 通过 `/stop` 停在第十条，另一 TUI 继续到第三十六条；这只证明明确任务停止与隔离，不能作为新 Goal 暂停语义验收。
  更早“Goal 暂停后数据静止”的记录属于旧实现，不用于证明当前三种控制边界。
- 当前控制候选 444 项定向通过；TUI 47 / 48 在 Goal paused 后原 claim 继续运行，约 35 秒内分别从 4 / 11 条增加到 11 / 18 条。
  随后 `/interrupt` 与 Esc 均结束当前回合：独立 PTY 继续，前台命令停在 26 条，目标保持 paused 且没有自动续跑。
  再明确 `/stop`，PTY 停在 27 条，两份数据约 100 秒字节不变。
- 后台默认目录复用统一命令/会话工具组，修复恢复时终端原生调用被快照拒绝的缺口；267 项联合定向通过。
  显式配置及 owner/task 限制保留。TUI 49 的终端调用进入原审批门，不再报工具不可用；后台启动仍需审批，模型自行改走子代理。
  Goal 暂停后孩子从 12 条继续到 19 条；明确停止后孩子取消、数据约 30 秒不变。恢复后新孩子运行原程序，
  同一 Goal/task 完成 30 条，原 19 条逐字节保留，脚本未改，平方和 9455、报告哈希与首尾记录一致。
- TUI 50 覆盖主代理 PTY：Goal 暂停时原 claim 继续，回合中断后独立终端从 16 条继续到 25 条。
  恢复后后台完成核对与汇报，最终 30 条、平方和 9455、原记录及报告哈希一致，Goal 由模型正常完成。
  恢复轮采用文件查询，没有再次调用终端读取，不能据此宣称“后台读取旧 PTY 句柄”已真实通过。
- 后台主代理现复用原审批账本和 TUI FIFO，按当前选定任务的精确 claim 隔离；旧内部名称删除，无新权限源。
  最新 428 项定向通过，Ruff、文档、严格尺寸、diff 与 clean-package 通过，尺寸基线不变。
  TUI 51 在 Goal paused 时仍批准原回合启动，中断后独立 PTY 保留；恢复的新 claim 对原句柄读取 41 次，
  再经真实面板批准输入“汇总”，全程只启动一次，退出码 0。终端 30 个号码与报告逐项相同，合计 148398、最大 9385、最小 872。
  TUI 52 拒绝启动后为 `APPROVAL_REJECTED / handler_executed=false / effect_outcome=not_started`，无替代运行或结果文件。
  两路均官方 MiniMax-M2.7、同一 Gateway；测试者只发初始需求、正常控制和审批按键，任务程序与报告均由模型完成。
  测试脚本进程已自然退出；TUI 窗口保留可查看，日常配置未变，原始证据只留仓库外。

本轮未通过或未覆盖的部分：

- TUI 47 原失败 Goal 被模型自行设为 200 秒，后续耗尽；编辑器只改正文，未直接改状态或预算强行恢复。
  该样本仍是 27 条，原 Goal 的恢复未完成；新无时限 Goal 的成功不抹掉这份失败。
- TUI 48 的模型脚本会清空旧 CSV，未达到可续写要求；TUI 50 报告把平均 7.9 秒写成“符合 5 秒”，属于质量失败。
  两份新采样的启动修复/恢复间隔也如实保留，不宣称全程均匀采样；测试者没有补数据、执行任务程序或修报告。
- 另一 Goal 的后台命令被要求审批后，模型改成一次性生成十二条数据，间隔不足一毫秒却报告完成。
  数据数值正确不等于持续采样成功，保留为模型履约失败，不为该样例增加文本判定或专项硬门。
- 旧切片 Audit 只发布文字要求、缺结构化来源，实际处理为零；该失败未修复，也未由测试者补来源。
- 新版证明的是审批结束后完整粘贴、运行中接收并消费；旧弹窗期间被截短的输入根因仍未证明，不能宣称已修复。
- 早期全仓 17,070 项通过属于旧源码，不能替代本轮新版本验收；最新 main 发布、部署与真实失败以本节开头为准。

## 已实现

- 仅思考响应丢失已修、真实复验中：成功停止却没有正文/工具的思考仍按原生内容保存，
  不再在适配器里作为空响应重采样；零工具续跑逐条追加，不误并工具轮。原用量、签名和密文保留。
  联合定向 455 passed、23 项既有 xfail；真实失败 SSE 复放 37,118 字符逐字保留。
  新包正常 TUI 16 模型轮、22 组工具自然收尾，8 项产物测试及两组独立复算通过；慢模型长任务仍未通过。
  新慢会话 21 请求、30 组工具，20 个完成响应历史完整，但约 30 分钟仍无产物修改，已取消留证。

- 工具结果二次截短及 Shell 人工长度拒绝已修。生产执行/归档到原生请求保留有界全文、文件版本与继续游标；
  大输出外置、脱敏、权限和输出预算不变，旧历史/缓存前缀不重写。真实正常 TUI 成功执行 8,468 字符命令，
  同会话两任务自然结束，独立 10 项测试和统计复算通过。
  两轮单路慢模型分别执行 54 / 33 组工具后仍未交付，已中断；调用/结果完整配对，未把停止计作通过。
  同一 68,214 token 失败请求开启缓存约 6 秒、完整冷算约 356 秒，均生成重复读取；关闭缓存不是已证实的修复。
  用户开启思考后的原任务新会话仍未交付，思考和工具结果均完整出站；不能把启用思考计作修复通过。
  不宣称所有慢模型或量化变体必然复读；服务端采样对照仍与底座修复分开记账。

- 渠道故障软提示不再把普通命令非零、参数/状态/权限/取消/未知错误称为系统不可用。
  依据已有错误分类选择可用性故障，按精确调用身份去重；不改变原失败、权限或执行策略。
  339 项相关定向回归通过；真实新包正常 TUI 66 组工具/回执无遗漏、无错误渠道提示，自然结束。
  8 项产物测试及原始/现有数据独立复算通过；模型中途调整样例绕路，不能把功能通过当成效率合格。
  这一误导可能放大排错绕路，但尚未证明是复读唯一原因。

- 后台缺模型已改为等待当前会话配置恢复，不再每 30 秒完整执行再失败；普通错误仍冷却，
  任务和 Goal 保留。384 项定向回归通过；真实 Goal 在 /model 改选后原任务自动完成，
  另一窗口正常任务也完成，两份产物各 16 项测试独立重跑通过。不会为恢复自动选择默认模型。
  旧失败会话在约 15 分钟观察期各只报一次配置错误，不再每 30 秒重复执行；不等同无限运行保证。

- 并行验收补修：主代理可用 read 返回的精确 `run_id` 补充同 owner/thread 已存在的旧 Todo；
  不再出现读旧账、写新账的接口死路。子代理只写自身，跨会话/未知目标拒绝；不改变旧任务状态、cwd 或 Goal。
  本地队列 TUI 的展示通道改从真实 CLI source 解析，不把自定义 owner provider 当外部 IM 脱敏；
  final、repair 和历史回放保留含 owner/request 标识的完整绝对路径，鉴权与 owner 路由不变。
  原会话更新三项已有完成备注并读回成功，六项完成状态不变；本地 TUI 与 canonical final 路径一致。
  碎片流跨路径边界和模型自行编写错误路径仍不能凭这项修复保证。
  已提交请求以持久回执为准，不再因重启瞬间 PID 不可见误报未执行；真实 TUI 在服务停止时提交，
  重启后只记录一条用户消息、一次完整执行与最终回复，五组工具往返完整、不重新入队。

- 三种接口统一显式温度开关，未配置不再由 Chat/Messages 隐式发送 0.2；单模型温度、零值、
  摘要单次覆盖和子代理冻结继承保留。采样、批次投影、状态观察、进度更新、路径及持久回执联合定向 854 passed。
  串行慢模型对照已收口：干净会话约 75 分钟、109 模型轮后仍未交付，独立测试四项失败、四项通过；
  112 组调用与结果完整，23 次进度调用没有协议失败。高缓存不代表有效进展，不能关闭模型循环风险。
  旧现场出站已确认调用/结果完整配对且包含换路提醒，仍复读；这不是仅凭温度差异即可关闭的问题。
  同一请求仅去掉温度的对照仍生成相同重复调用；原生响应对照客户端另遇服务端模板 500，不能当生成通过。
  执行事实只追加最近批次，不再每轮重复成功/失败分组；旧前缀与完整工具往返保留，不追溯改写存量历史。
  另移除系统软提示中逐动作重新验证的冲突要求，改为复用有效证据、针对改动验证；不把请求修正当作生成质量保证。
  新样本前 61 轮仍反复核对、未交付；60 对连续请求保留旧前缀，新增事实占正文约 29%，旧样本约 74%。
  两段实际轨迹不同，不将字符占比当作等任务成本节省比例；新部署改变系统指导后的首次冷请求单独记录。

- 真实慢任务抓到 Todo 只补元数据被重置为待办，覆盖清单也同源：已区分补丁与新建默认值。
  定向先复现 18 项失败；另发现已完成项的旧备注被保护规则静默覆盖，现只保护完成事实、不冻结可维护备注。
  新建默认、显式更正和完成保护保留，旧账不猜测回填；真实旧计划续写及后续读取已验。
  该缺陷会向模型和用户返回错误进度，但不据此断言它是所有复读的唯一原因。

- 正常模型并行验收也出现高频查询子代理：距上次进展秒数使原结果哈希不断变化。
  已接入现有稳定进展软观察，75 项相关定向通过；子代理自身查询也不算下级进展，不自动杀慢孩子或另加完成门。
  正常模型多 TUI 已自然收尾：多子代理任务三批六个孩子完成，第四个连续任务后独立 34 项测试通过；
  中断继续/退出恢复保留原要求、模型和用量。同题日志工具独立八项及统计复算通过。
  普通账户权限、消失文件竞态、执行器退出/写回/通知重投仍未形成完整真实故障覆盖；
  不以正常收尾替代故障验收，也不把一次提示后的行为变化当作因果证明。

- 中断/异常的原生历史保存已补齐：主代理、子代理和单次 CLI 从当前执行 IR 保存已发生的工具往返，
  后台普通续做也保存同一 transcript，静默不发正文不再等于丢执行历史。未知工具副作用仍明确未知，不自动重放。
  真实 TUI 已核对正常主代理、慢模型和被停止孩子分别保留 5、7、10 组工具往返；主代理随后继续并完成。
  两个正常模型产物独立重跑 11 / 56 项测试通过，不把这些测试数量等同于全部业务边界覆盖。
  本轮当前联合定向 547 passed；新版后台目标按 Esc 后保留 13 组往返，无再次催促自动续做，
  最终保留本片 21 组往返并公开汇报；经历 Compact 后仍能继续，native/final 不重复进入模型历史。
  旧版未写入的历史无法凭空恢复，进程硬杀/断电的每步持久化不在此保证内。

- 真实长任务发现子代理输出截断后续派报“未配置模型”：共享宿主的临时 backend 覆盖了 child 配置。
  已删除隐式捕获，恢复仍按 child thread 模型引用；显式注入按线程隔离。旧函数回放 3 项失败，
  新实现全部通过。真实协调者等待两个孙代理后自动续派，累计三个工作片完成，主代理无需催促整合及汇报，
  期间发生一次 Compact；这覆盖重新派工的模型恢复，不把它等同于所有输出截断场景已复验。

- 长运行增量联合定向 951 passed、2 项既有 xfail；正常模型多 TUI 已验证活动回合的命令完成交接、
  自然结束后的主动通知、子代理期间插话、连续 Goal、手动/自动 Compact。三个测试产物独立重跑
  18/21/51 项测试通过；产物通过与用户能收到最终汇报分开验收。
  实测追加修复：后台片开始时的旧子树快照不能抑制已经接齐结果的最终汇报；命令完成补报在队列筛选、
  claim 后准入共用权威校验，发布回执未落盘时保留通知。精确编辑冲突不再误报参数格式错误。
  新版两子代理 TUI 最终汇报可见，晚完成采样约 91 秒后自动汇报；前者旧长工作片竞态还由定向回放覆盖。
  缓存诊断在真实出站处计算，最近比较随会话用量保存；服务端缓存状态保持未知，不靠删历史提高命中。
  见 [长时间运行](docs/design/LONG_RUNNING_EXECUTION.md)。

- 当前慢模型长任务验收未通过：单路、无子代理，持续慢流/长预填充时可翻页、输入和中断；先后两段
  运行近三小时，但分别出现编辑冲突反复提交和同一成功 Shell 观测 24 次。测试者已中断失败现场，
  最近缓存约 98% 不代表有效进展。界面与等待链路通过不能冒充慢模型任务质量或循环恢复通过。

- 成功重复观测补修：每隔配置阈值继续提醒，超过 256 条仍计数；相同正文不因归档引用变化被算作进展。
  普通后台策展避让同端点前台工作片，预算传入 HTTP，取消后旧线程未退出不叠加重试。
  356 项联合定向通过、2 项既有预期失败；本机官网模型真实 TUI 已生成单文件动画并自然收尾。
  恢复连接后已核对正常模型 TUI 自然完成；慢模型后端仍在运行，前端却报等待超时。
  测试机有两次系统时钟前跳，客户端墙钟计时缺陷已复现并改为单调计时；真实 TUI 注入一小时校时仍正常收尾。
  慢模型又出现同一下载进程无输出、连续短等待；回执 uptime 变化绕过完整结果哈希。
  已补宿主稳定进展摘要，33 次空等回放按每 5 次提醒且原始哈希保持不同；日志增长和退出重新计数。
  新包正常模型 22 轮、慢模型 4 轮 TUI 均自然完成；正常任务独立 11 测试通过，慢任务合计复算一致。
  新包未触发长空等阈值，不能据此宣称模型一定采纳；正常模型把十万行内存测试外推百万行，实测峰值约 157 MiB。
  失败测试已通过原 TUI 停止并保留现场，未停止日常用户任务；不能算慢模型任务成功。
  旧现场同一成功命令实际重复 65 次；软反馈不能保证模型采纳，不宣称复读完全消失。

- 用量异常收口与前后台去重已补修：中断后追加一轮，24 次已报告调用的输入 1,300,462、输出 22,969，
  与持久会话显示完全一致，另一次缺报保持明确未知；没有按估算补账。多子代理 Goal 两个孩子完成，
  主代理插话、后台续接及一次 Compact 的分项账可核对，不将 source 变化重复收费。
- 账号登录代码已实现：ChatGPT 订阅设备码、通用 OAuth 参数、登录取消/退出/刷新，模型仍显式填写。
  通用设备码替身已在真实 TUI 验证取消、重新登录、参数编辑、短调用、过期刷新和退出；不支持工具的替身被任务准入拒绝。
  官方订阅设备码入口在测试环境返回 HTTP 403，未进入账号确认，不能据此判断套餐或宣称真实账号可用。
  另补登录启动失败清等待标记、退出后刷新当前模型可用性；这一增量仍需新版 TUI 复验。
- 新版官网模型两路 TUI 追加任务均有最终回复和实际测试结果：差旅工具修复 CSV 空值，图书工具跨两次 Compact 整合。
  图书任务的孩子被主代理停止后接手，不把它计成子代理自然完成样本。慢模型按用户要求只保留一路且不派孩子。

- 六路真实 TUI 增量验收：四路官网正常模型、两路本地慢模型，共用测试机单 Gateway、独立 owner。
  新发现 Goal 子代理返回误查用户请求的阻塞，已补精确 Goal 归属判定；原现场恢复后完成整合，
  同会话第二个多子代理目标无需人工续推也自动完成。输入/目标相关联合定向 313 passed、2 项既有 xfail。
  本地模型两种接口对后台工具 schema 均返回 grammar complexity HTTP 400；单槽排队另有首事件超时。
  健康慢流实际持续约 45 分钟且可中断，不等于慢模型复杂任务完成；新增未通过项见下表。
  追加仓库任务中，主代理收到追问后工具误用新消息编号，触发 TOOL_AUTHORITY_CONTEXT_MISSING；
  已补成对执行身份，相关两组不重叠定向 574 passed、2 项既有 xfail；原会话恢复运行 14 项测试通过，
  新组合等待两名孩子时插话、真实命令执行及继续等待均通过；孩子完成后自动接回并压缩、整合，
  最后 Goal complete、attempt done、TaskRun done 一致。

- 空白输入补全不再越界；主代理 Esc 中断本轮而不暂停 active Goal，显式暂停后普通聊天不恢复目标。
  真实 TUI 已验证编辑放弃/保存、编辑中 Esc 后续跑、后台插话被模型消费并公开回答、暂停问答后显式恢复到完成。
  后台输入沿用原任务编号；总账恢复时重开、最终正确关闭。已完成历史 Goal 不再被误当成并行冲突而阻止恢复。
- 单 Gateway 服务多个用户和 TUI；会话、任务、子代理和用量按结构化身份归属。
- 会话独立选模型、用户默认模型、可配置服务商协议、地址、密钥引用与上下文容量；子代理可显式选模型。
- 软件不预选供应商或模型，空配置可进入 `/model`；调用返回 `MODEL_NOT_CONFIGURED`。用户保存的选择不变，后台记忆整理也不因配置失效自动换模型。
- 流式正文和思考分开显示、工具结果与文件差异展示、历史分页、完整详情、子代理进入与插话。
- 长思考预览提示增加实时折叠行数和接收字符数；原文行与排版行分开，不改模型上下文，真实 TUI 已验证连续增长。
- 主子代理共用上下文压缩与会话记录；展示窗口不是模型历史的权限白名单。
- 持久目标、任务清单、停止、恢复、工具权限、用户记忆与技能读取。
- Goal 创建工具默认直接可用；正常最终回复不再因为没有工具或 Todo 而断掉持续目标。
  每个代理最多一个未结束 Goal，主子独立归属，普通派工可不附目标，Todo 可选。名称不创建额外执行器。
  TUI 已验收主子目标编辑、保存、放弃、停止及修改要求被模型消费；旧目标保留历史，前后台共用目标时钟。
- 模型统计条已实现：本次逻辑模型轮、当轮工具数、最近缓存、当前代理会话累计消耗；单 Gateway 双 TUI 已完成连续任务、多子代理、手动压缩及后续任务的真实用量对账。
- 子代理结果按 exact attempt 收口；终态写回失败保留待恢复事实，重复恢复和父级通知去重。
- 创建子孙代理后继续原工具循环，成功结果不再等全部兄弟结束；自然让出后的等待按任一新结果释放。
  忙父级在原安全点接收有界交接，根快照不重复携带已由事件交付的正文；不额外启动父执行器。
- 子代理自然最终回复也会交接本 run 工具登记的真实文件，不再要求模型返回结构化 JSON 才有产物清单。
  相对输出声明与工具共用可信 cwd，删除内部输出重定位、收口复制和声明驱动增权；缺少 cwd 时不猜宿主目录。
  追加任务发现多文件补丁只返回显示名而未登记真实文件，已补精确引用及删除记录；新包真实 TUI 已核对补丁落账、自然完成文件清单与父级汇总。
- 派工快照只报告事实，不再按孩子状态另发父级动作建议；根与递归协调者统一自身分工说明。
  协调者只加载自己的冻结角色，删除全部外包、永久禁止接手、要求亲手重写下级成果等相互冲突的旧规则。
  新包三路真实 TUI 已结束，分别验证独立工作、依赖等待自动接续和同会话追加的真实孙级交接。
- 活动提醒按首 token、流静默、工具、审批与退避分阶段，复用原心跳和投递账；提醒不改变终态，不自动强杀或重派。
  四路真实 TUI 已完成父子任务、先接入快结果和低阈值提醒送达后继续到完成；默认提醒阈值已恢复。
- JSONL 按物理换行分隔，正文内 Unicode 分隔符不再被误当多条记录。

## 当前开放问题

| 优先级 | 现象 | 已定位的边界 | 后续验证 |
|---|---|---|---|
| 已修，定向及真实工具链已验 | Messages 探针重复 `/v1`，Chat 完整接口重复后缀 | 诊断统一复用传输的 `endpoint_parts()`；实际请求行为不变，Responses 原规则保持 | 36 组地址合同及相邻测试共 228 项通过；新版官方 MiniMax-M2.7 TUI 完成写入、运行与读回。后台未保存完整探针快照，地址合同证据不冒充真实抓包 |
| P1 实测未通过 | 慢模型重复验证和改样例，未完成日志工具 | 读取正文二次截短、命令长度门与渠道失败误提示已修；开启思考及采样对照仍未交付，原生结果配对完整 | 同请求完整重算仍重复；其它客户端也有语义重复或协议阻塞。更高推理精度待授权对照，不归咎于慢速本身，不把单一采样对照当根因 |
| P1 新发现、未修 | 旧测试 owner 无模型配置仍被反复后台唤醒 | 启动后的后台调度多次遇到不可恢复的未配置模型，当前正常 TUI 模型可用 | 核对持久唤醒的失败分类和重新准入；不自动填默认模型或无限重试 |
| 已修，窄竞态定向已验 | 子代理全完成却没有最终汇报 | 开始时冻结的 child phase 在结束时仍抑制回复；事件已读后再无唤醒 | 删除旧快照完成门，保留当前子树/未读邮箱；新双子代理 TUI 可见最终汇报，长后台片交错由定向覆盖 |
| P1 底座已修、慢模型原场景待复验 | 同参数同结果被拒绝后又放行，普通任务长期复读 | 门自身未执行拒绝不再清空或挤掉原观测；无关失败不清结果哈希；原失败正文带计数和换路说明，非文本读取说明能力边界 | 长序列及真实执行器恢复已过；官网模型 TUI 连续两任务正常结束，但未触发循环门。原慢模型会话未重启，不宣称所有模型复读已修 |
| P0 验收中 | 子代理退出后假运行 | exact attempt 执行器登记和失败/未知副作用投影已落地，定向回归已过 | 新包真实多子代理、退出注入与父级通知；旧版活进程中未知线程仍不能猜死 |
| P1 验收中 | 大批收口故障后老记录恢复不到 | 未消费事件分页轮转、持久游标和不限窗口消费回执已落地 | 真实故障和重复扫描；存储持续不可写不算恢复成功 |
| 已修，旧账边界保留 | 暂停后累计用量变小、前后台切换虚增 | 统一结算与累计容器代次去重；真实中断后追加消息、后台目标及压缩已对账 | 老数据保持原样；强杀时未报告用量无法凭空补全，仍须显示未知 |
| P1 慢模型未通过 | 动画任务反复运行同一成功命令 | 原始工具参数证明同命令再次调用 9 次；成功 Shell 软提醒不等于只读动作门，后续才承认文件不完整 | 不宣称重复门计数修复已解决所有模型退化；复验换路、完整产物及消息回复 |
| P1 慢模型新增样本 | 日志工具反复覆盖同一文件，正文称追加但文件保持 280 行 | 工具流持续收到约 10k 字符参数，真实 handler 执行成功且多次 diff 为零；已持续超过一小时，无子代理 | 需核对完整请求参数及原生回放，不能仅凭模型称“写入失效”归咎写工具；产物未通过 |
| 环境边界已定位 | 本地慢模型最近缓存从约 90% 变成 0%，首字等待很长 | 推理服务单槽位中存在交错请求；75,988 token 一轮全部预填充约 566.6 秒，另记录 2,229.870 MiB 状态超过 2,048 MiB prompt 状态缓存上限而不保存；随后一轮重新达到约 96% | 0% 不代表会话记忆丢失；继续核对交错请求来源，调整服务缓存容量须结合内存，不擅自重启用户模型 |
| P1 兼容待查 | 本地 Goal 未开始工作就受阻，恢复动画时工具参数不完整 | 两种接口均有上游 grammar compiler 400；另一次多工具流中第一个工具未见 ready，整轮零执行 | 区分上游 schema 编译、排队超时和流协议不完整，保留 raw 事件，不猜补参数或降低安全校验 |
| P2 展示 | 后台目标受阻但界面缺少直接原因 | Goal 显示受阻，真实 HTTP/超时只在 claim 诊断中可见 | 沿现有错误投影显示脱敏原因和恢复入口，不把所有错误改称模型配置错误 |
| P1 存量待查 | 旧后台唤醒反复领取后失败 | 旧测试 claim 多次记录 previous_failed，原始请求消息缺失；预算终态的旧任务也出现孩子唤醒记录，未删除或伪造原消息 | 核对旧唤醒消费和终态收口，不能靠全盘删历史掩盖；本轮正常 Goal 恢复通过不关闭此项 |
| P1 待存量实测 | 老会话的多个目标误绑同一任务 | 已实现精确 ID 恢复、独立任务迁移与源快照保留，定向回归通过 | 未自动改动真实用户旧目标；需在用户显式恢复时核对现场与权限 |
| P1 兼容边界 | 更新后仍可能看不到新默认 Goal 工具 | 新配置真实 TUI 已建目标并显示；旧完整配置副本会覆盖新默认工具目录和系统提示 | 保留用户定制内容，按实际生效配置判断，不能只看 wheel 版本 |
| P1 待复验 | 历史多目标重复干活或提前宣称全部完成 | 新版本禁止一个代理创建多个未结束 Goal，历史失败样本保留；普通父子分工、逐个结果接收与长尾等待仍需独立评估 | 不能用继续添加提示、事后隐藏输出或目录锁替代执行分工修复；单 Goal 通过不代表此项通过 |
| P1 | 主代理仍可能重复实现或提前等待 | 已修文件交接与派工说明矛盾；新包根确实在孩子运行时写自己的模块，但追加任务又把本应自己负责的命令入口派给组长，并反复查状态 | 可执行并行不等于模型总能合理分工；按派工实参、调用时序与文件归属衡量，不添加目录锁或强迫忙碌 |
| 已修，极限组合待验 | 状态查询过大、会话范围丢失与内部结果误读 | 紧凑快照、会话过滤、主请求根查询和真实报告引用共用规范树 | 153 项相关定向通过；双真实 TUI 分别查到本会话两名孩子、协调者及两名孙代理，并读取真实文件/报告；大树全量恢复和分工效率另验 |
| P1 已补配置、部分真实验收 | 单槽位慢模型反复首事件超时 | 首事件预填充预算不代表上游排队；新增每模型额外排队预算，默认 0；真实超过九分钟预填充后继续流式工具调用 | 只保留一路慢模型，不派孩子；不修改用户本机日常配置，不把 grammar 400 或生成复读当作超时修复成功 |
| P2 新样本 | 中断后总结把已执行测试说成未验证，Working 与目标计时不一致 | 正常模型账本和 TUI 已保存；实际工具输出与最终表述相矛盾，计时起点也需核对 | 分别审计恢复上下文与展示时钟；不篡改模型正文或以用量修复代替解决 |
| P1 待改进 | 长思考用尽输出预算但无正式回复 | 单次输出 16,314 token，约 27 分钟，结束原因为 max-tokens；已明确保留 unfinished，没有误判完成 | 评估推理预算与输出余量配置；单纯延时不能修，不能无界自动续跑掩盖 |
| 已修，定向及真实状态流转已验 | 正常等待子代理误带 runner_error | 写回复用唯一结束映射，正常让出保留未完成标志但清当前错误；显式失败与未知/冲突结果保持 | 237 项定向通过、1 项既有 xfail；官方模型实际 TUI 的递归父级两次 PENDING/续跑到 DONE、能力阻塞后完成及明确停止已留证。报告质量失败单列 |
| P2 真实报告未通过 | 递归采样总结的时间范围与最终 CSV 不一致 | 两路各 30 条、立方值正确；主代理原生读取结果含真实时间，但未指出总结使用了错误的 A 路时间，并将 5–6 秒间隔写成固定 5 秒 | 核对原生读写和交接的时序，保留错误报告，不用正常状态流转或数值正确代替报告验收 |
| P1 主链已实测 | 孙级递归交接与故障组合 | 真实统筹与两名孙代理完成，统筹边做自己的模块边收孙代理结果，主代理最终整合；同会话追加修改也完成 | 迟到/异常组合仍待验证；分层样例仍存在重复写模块，不能称分工质量全部通过 |
| P1 已修复、定向验证 | 目标已结束仍承诺自动续跑 | 轮限说明原先只看回合残留 Goal ID；现重读精确 Goal 状态并校验任务绑定 | 完成、暂停、阻塞、预算受限、删除和外任务绑定回归通过；不以此推断多目标分工已解决 |
| P1 已修复 | Goal 完成后仍在执行的任务已提前终态 | Goal 工具原先直接关闭 TaskLink/注册表；现交还统一 finalization | 真实 TUI 已确认目标完成后两个 child 继续，均正常结束、主代理汇总、任务最终关闭 |
| P1 已修复 | 当前任务命名 Goal 后，插话无法转交原子代理 | 命名误把当前任务变成 detached，后续消息失去原父级；现按执行归属保留前台，独立后台不抢占前台 | 真实 TUI 两次 send_guidance 均成功；消息消费账、子代理最终结果和主代理汇总一致，任务正常关闭；权限边界未放宽 |
| P1 已修复、边界保留 | Goal 时间异常增长、提前碰限额 | 多个 Store 各自累计同段时间，现已共享单 Gateway 内的计时基线 | 定向多实例及新包真实目标的完成账正常；旧多记时间不倒推篡改，跨进程共享存储不作为本轮覆盖 |
| P1 | 模型自行填写未要求的 Goal 时间上限 | 存储和 schema 无默认上限；真实模型仍填写了 duration_seconds，软说明不保证遵从 | 保留真实失败，进一步明确用户预算控制与模型可填字段边界，不按自然语言猜授权 |
| P1 | 部分上游异常仍呈任务失败 | 不完整流的所有原因尚未确认 | 继续按 HTTP/协议/流式终止分类，不得用“配置错误”笼统代替诊断 |
| P1 | 多目标试跑出现重复正文直到长度限制 | 已留存真实重复输出和停止原因；尚未区分请求污染、历史累积与供应商生成退化 | 单独核对原生请求和返回，不能仅折叠显示或删除重复历史来宣称修复 |
| P1 | 极慢模型下的小时级交互稳定性尚未充分证明 | 已有慢流、重连和输入响应样本，不等于覆盖长期复杂组合 | 长等待、子代理、滚动、复制、插话和停止同时发生 |

## 发布与验收边界

恢复测试机连接后新增客户端校时回归：旧代码前跳提前超时、回拨延迟超时，新代码相关 66 项通过。
真实正常模型在客户端前跳一小时后完成七轮 CSV 工具任务并交付最终回复；底层共享 Gateway 未重启。
此增量是客户端等待修复，正常任务成功不能冒充慢模型重复轮询已消除。

空白输入与 Goal 补修包已部署独立测试机：真实连续任务、后台插话、编辑、中断及暂停后恢复均核对，最终 Goal/attempt/TaskRun 终态一致。
最新相关定向 607 项通过、6 项存量预期失败保留；Ruff、文档同步、严格尺寸、diff 与发布包检查通过。本机旧 Gateway 未重启，新开本机 TUI 已验证空白补全修复。

重复调用修复已部署至独立测试机的单 Gateway，源码包 1,136 文件一致；本机在用会话和 Gateway 未重启。
本轮相关工具、文件、原生消息、权限以及前一切片 Goal/输入联合定向 446 项通过；
Ruff、文档同步、严格尺寸、diff 和 clean-package 检查通过，未跑全仓 pytest，线上 CI 未作为本轮证据。
真实 TUI 首任务 4 模型轮、3 工具；同会话追加 12 模型轮、12 工具，一次参数错误后换路完成。
自然结束仅证明运行链路：动画产物漏掉 SVG 要求，且未做浏览器目视验收，内容质量未通过。
观测计数、连续拒绝和真实 Shell 写入后的恢复以定向测试为证据，慢模型原循环仍需新版现场复验。

本轮子代理交接定向套件 425 项：421 项成功（含 4 项已有 XPASS）、4 项已有 xfail、零失败；
另重跑锁生命周期 5 项通过。Ruff、文档同步、严格尺寸、diff 和 clean-package 检查均通过，未跑全仓 pytest。
真实 TUI 覆盖分层文件检查、同会话报销工具追加、特殊金额以及补丁修改后的父级验证；
补丁移动/删除和部分提交仍以定向测试为证据，不冒充这些组合均有真实模型验收。

后续派工一致性切片定向套件 312 项：308 项成功、4 项已有 xfail、零失败。
三路 TUI 与同会话追加完成，独立重跑报销现有 10 个边界样例及发布工具多规则 scan、diff、report、错误路径。
这里验证了链路与基本运行，不代表产物完整无缺：生成的 README 曾引用不存在的测试文件，报销重复票据的汇总口径仍需业务核对。
分层首次被模型扁平化、追加后根职责被转交，均没有被计作高质量分工通过。

资料清理与运行时修复分别记账。当前父子并行改动不等于上述全部风险已关闭，不得删除仍开放的问题或伪称已全部修复。

验收必须分清源码版本、安装包版本、Gateway 版本和每个 TUI 的模型配置；默认入口与后台服务都要核对。短启动检查不替代长任务验收。测试记录应脱敏，不提交真实用户数据、密钥或测试机私人配置。

服务升级同时检查解释器与 `WorkingDirectory`：旧安装目录作为工作目录可能遮住新版包，
仅更换 `ExecStart` 不能证明服务已经加载新代码。使用中性服务目录并复核实际导入位置；误载旧版本的测试不得计入新版本验收。
