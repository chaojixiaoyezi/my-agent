# SPEC — Simple Python Agent

## 目标
开发一个 Python3 可运行的简单 AI 智能体 CLI，参考 通道运行时/长期助手 的工程思想，但不依赖它们。

## 功能清单
- F01 Python3 标准库优先，任意有 Python3 的地方可用。
- F02 CLI 入口可用。
- F03 有记忆功能，至少支持 JSONL 持久化、检索、追加。
- F04 所有参数外置，配置文件中每个参数要有中文说明。
- F05 支持 subagent 概念：可拆任务、记录子代理思维/计划/结果。
- F06 支持动态 prompt 注入：命令行、文件、配置三层。
- F07 代码有工程意识：模块化、可扩展 backend、清晰目录。
- F08 暂时不需要 Web，但要预留 Web/API 扩展边界。
- F09 可测试：单元、CLI、真实入口、错误入口。

## 第一轮交付
可运行 CLI + 配置 + 记忆 + subagent + 动态 prompt + 测试证据。
