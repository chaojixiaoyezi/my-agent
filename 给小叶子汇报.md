# 给小叶子汇报

本文件只保留当前架构事实，不再记录旧的拆分计划。

## 当前规则

- 文件大小不再是硬门；大文件可以存在，只要主链路更清楚。
- `CODE_SIZE_REPORT.md` 只做趋势报告，当前 0 个 hard finding。
- 已删除基础 manager 薄层、空转发文件、历史路径迁移模块和历史配置开关。
- 后续优化重点是跑通真实主代理/子代理链路，而不是继续堆 fallback。

## 当前重点

1. 继续清理文档和测试里的旧兼容语义。
2. 继续合并只做转发的薄层。
3. 跑 focused tests，最后跑全量测试。
4. 真实起两个主代理验证：一个自己做，一个让子代理做并由主代理验收。
