# 代码规模与架构治理 — 给小叶子的汇报

LLM: This is a plain-language closeout for the code-size governance pass.

给人看的解释：
这轮不是加功能，而是给项目装"工程护栏"，让后续代码不再越写越大、越写越乱。

## 1. 为什么做这轮治理

项目从一个小型 CLI agent 快速长到现在 14.5 万行 Python 代码，模块已经拆开了，但出现了几个典型的大项目病：

- **11 个文件超过 600 行硬上限**，最大的 `cli/chat.py` 有 989 行
- **48 个函数超过 100 行**，最大的 `dispatch_subagents` 有 525 行
- **15 个类超过 250 行**，最大的 `SimpleAgentDispatchMixin` 有 854 行
- SubAgentManager 通过 10+ 个 mixin 拼成了"分布式上帝类"
- 没有自动检查脚本，大文件可以无限膨胀
- 没有统一的开发规范，新代码想塞哪就塞哪

如果不治理，再过几轮开发就会变成屎山。

## 2. 做了哪些事情

### 2.1 建立自动护栏

- **`scripts/check_code_size.py`** — 自动检查文件行数、函数行数、类行数、参数数量、嵌套深度、星号导入、垃圾命名、高危文件增长
  - `--mode warn`：暴露所有历史技术债，不阻断 CI
  - `--mode strict`：阻断新增违规
  - 生成 `CODE_SIZE_REPORT.md` 报告

- **`test_architecture_guardrails.py`** — 架构护栏测试，检查：
  - 不允许新增 `import *`
  - 不允许运行产物入仓（__pycache__、.DS_Store 等）
  - 关键入口文件不能继续膨胀
  - 不允许新增垃圾桶文件名（utils.py、helpers.py 等）
  - 不允许新增万能类（AgentManager、TaskManager 等）
  - 所有 Python 文件必须能编译通过

### 2.2 更新 CI

`.github/workflows/lint.yml` 现在包含：
```bash
python3 -m compileall -q agent_py_agent scripts   # 语法检查
python3 -m ruff check agent_py_agent scripts        # 代码风格
python3 scripts/check_code_size.py --mode warn      # 代码规模
python3 -m pytest test_architecture_guardrails.py    # 架构护栏
python3 -m pytest test_packaging.py test_cli_parser.py test_tooling_filesystem.py  # 核心测试
```

### 2.3 补强工程配置

- **`.gitignore`** — 补充了 `.mypy_cache/`、`**/.DS_Store`，清理了已跟踪的 DS_Store 文件
- **`pyproject.toml`** — 已有 ruff、pytest、coverage 配置

### 2.4 建立代码规模规范

- **`CODE_SIZE_POLICY.md`** — 完整的文件/函数/类行数限制表、复杂度限制、豁免流程
- **`REFACTORING_BACKLOG.md`** — 按优先级列出需要拆分的文件，含具体拆分步骤
- **`ARCHITECTURE_EXEMPTIONS.md`** — 豁免登记簿，每个豁免有到期日和重构计划

### 2.5 建立架构文档体系

| 文档 | 内容 |
|------|------|
| `docs/architecture/ARCHITECTURE_TREE_LONG_TERM.md` | 长期目录树、模块归属、迁移路径 |
| `docs/architecture/BOUNDARY_RULES.md` | 导入规则、写入规则、尺寸规则、安全边界 |
| `docs/architecture/MODULE_OWNERSHIP.md` | 每个模块的职责和禁止事项 |
| `docs/architecture/SUBAGENT_SERVICE_REFACTOR_PLAN.md` | SubAgent 服务化拆分计划 |
| `docs/architecture/CHAT_REFACTOR_PLAN.md` | chat.py 拆分计划 |
| `docs/development/DEVELOPMENT_RULES.md` | 开发规范（Python 3.10 兼容、命名、导入等） |
| `docs/development/FILE_WRITING_RULES.md` | 文件写入分类和边界 |
| `docs/development/TASK_RECORD_RULES.md` | 任务记录模板和流程 |
| `docs/development/FEATURE_SPEC_RULES.md` | 功能需求模板和流程 |
| `docs/development/TESTING_RULES.md` | 测试分类和验证命令 |
| `docs/development/CODE_REVIEW_CHECKLIST.md` | 代码审查清单 |
| `docs/decisions/ADR-0001~0004` | 4 个架构决策记录 |

### 2.6 清理工程垃圾

- 从 git 跟踪中移除了 `.DS_Store` 文件
- 运行产物目录（`data/`、`memory/`、`memory_archive/`）已在 `.gitignore` 中

## 3. 对以后开发有什么好处

1. **新代码不能继续变大** — CI 会自动检查，超过限制的 PR 会被拦住
2. **大入口有退出计划** — `REFACTORING_BACKLOG.md` 列出了每个大文件的拆分步骤
3. **新功能有规范** — 必须先写 Feature Spec，再写代码
4. **架构决策可追溯** — ADR 记录了每个重要决定的原因和影响
5. **代码审查有清单** — 不靠记忆，靠 checklist
6. **后续 AI 写代码会被约束** — 所有规范文档都标注了 "LLM: ..." 前缀，AI 工具会自动读取

## 4. 现在还有哪些历史大文件

### 超过 600 行硬上限的文件（11 个）

| 文件 | 行数 | 状态 |
|------|------|------|
| `cli/chat.py` | 989 | 已冻结，有拆分计划 |
| `agent/agent_core/dispatch_mixin.py` | 889 | 已冻结，有拆分计划 |
| `agent/memory_archive/query.py` | 839 | 已冻结，有拆分计划 |
| `agent/subagents/manager_patch.py` | 794 | 已冻结，有拆分计划 |
| `agent/settings/config.py` | 751 | 已冻结，有拆分计划 |
| `agent/log_analysis/analytics/detectors/rules.py` | 747 | 已冻结（规则表可豁免） |
| `agent/subagents/manager_base.py` | 744 | 已冻结，有拆分计划 |
| `agent/log_analysis/tools.py` | 666 | 已冻结，有拆分计划 |
| `agent/memory_archive/runtime.py` | 657 | 已冻结，有拆分计划 |
| `agent/adapter/qq.py` | 613 | 已冻结 |
| `cli/memory_commands.py` | 609 | 已冻结，有拆分计划 |

### 超过 400 行软上限的文件（30+ 个）

见 `CODE_SIZE_REPORT.md` 完整列表。这些都是历史债务，有记录但不阻断开发。

## 5. 哪些已经被护栏管住了

- **所有 11 个硬上限文件** — 已录入 `HIGH_RISK_FILES` 基线，CI 会检测是否继续增长
- **所有入口文件** — 已录入 `ENTRYPOINT_LINE_LIMITS`，架构测试会检查
- **星号导入** — 当前 0 个，测试会阻止新增
- **垃圾桶文件名** — 当前 4 个历史遗留（helpers.py、common.py、utils.py），已录入基线，禁止新增
- **万能类** — 测试会阻止新增 AgentManager、TaskManager 等

## 6. 后续最应该拆哪几个文件

按优先级排序：

1. **`cli/chat.py`** (989 行) → 拆成 command_loop、tui、slash_router、renderer、session_state、gateway_client
2. **`dispatch_mixin.py`** (889 行) → 拆成 DispatchService、RunnerService
3. **`memory_archive/query.py`** (839 行) → 拆成 query_logic、archive_io、archive_helpers
4. **`manager_patch.py`** (794 行) → 拆成 PatchService、PatchApply
5. **`config.py`** (751 行) → 拆成 model_config、memory_config、gateway_config、subagent_config
6. **`manager_base.py`** (744 行) → 拆成 LifecycleService、PersistenceService

每个拆分步骤详见 `REFACTORING_BACKLOG.md`。

## 7. 本轮测试结果

| 命令 | 结果 |
|------|------|
| `python -m compileall -q agent_py_agent scripts` | 通过（1 个 SyntaxWarning 是已有代码） |
| `python scripts/check_code_size.py --mode warn` | 通过，412 条发现，未阻断 |
| `python -m pytest test_architecture_guardrails.py -q` | 6 passed |
| `python -m pytest test_packaging.py test_cli_parser.py test_tooling_filesystem.py -q` | 51 passed |
| `git diff --check` | 通过 |

## 8. 是否存在未解决风险

1. **ruff 未本地安装** — CI 会安装并运行，本地可选
2. **11 个硬上限文件仍是历史债务** — 有冻结和计划，但尚未实际拆分
3. **SubAgentManager 服务化尚未开始** — 已有详细计划文档，需要逐步执行
4. **chat.py 拆分风险较高** — 已有计划，建议分步迁移

## 9. 本轮产出清单

| 产出 | 状态 |
|------|------|
| `scripts/check_code_size.py` 增强（高危文件追踪） | 已完成 |
| `test_architecture_guardrails.py` 增强（编译检查、万能类检查） | 已完成 |
| `.github/workflows/lint.yml` 更新（ruff + 架构测试） | 已完成 |
| `.gitignore` 补强 | 已完成 |
| `CODE_SIZE_POLICY.md` | 已完成 |
| `CODE_SIZE_REPORT.md` | 已完成 |
| `REFACTORING_BACKLOG.md` | 已完成 |
| `ARCHITECTURE_EXEMPTIONS.md` | 已完成 |
| `给小叶子汇报.md` | 已完成 |
| `docs/architecture/` 5 个文档 | 已完成 |
| `docs/development/` 6 个文档 | 已完成 |
| `docs/decisions/` 4 个 ADR | 已完成 |

**结论：Completed**
