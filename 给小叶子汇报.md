# 给小叶子汇报

LLM: This is a plain-language closeout for the code-size governance pass.

给人看的解释：
这轮不是加功能，而是给项目装“工程护栏”，让后续代码不再越写越大、越写越乱。

## 为什么做

项目已经能跑很多功能，但出现了大文件、大函数、Mixin 和 Config 膨胀的风险。继续堆功能会让后续修 bug、加功能、恢复上下文都越来越难。

## 做了什么

- `agent_py_agent/cli/parser.py` 从 729 行降到 71 行，命令注册进入 `agent_py_agent/cli/commands/`。
- `agent_py_agent/cli/chat.py` 从 1131 行降到 989 行，已抽出 history、rendering、slash commands。
- 全仓 `import *` 已清零。
- SubAgentManager 开始 facade/service 化，已抽出 persistence 和 lifecycle services。
- 新增 `scripts/check_code_size.py`，能生成 `CODE_SIZE_REPORT.md`。
- 新增 `CODE_SIZE_POLICY.md`、`ARCHITECTURE_BOUNDARY.md`、`REFACTORING_BACKLOG.md`、`ARCHITECTURE_EXEMPTIONS.md`。
- CI `lint.yml` 加入 code-size warn 检查。
- 新增 `ExtensionPlugin` / `ExtensionRegistry`，给 log_analysis、BAS、代码审计等未来插件留统一入口。

## 对以后开发的好处

- 新代码不能继续随便塞进大入口。
- 星号导入、垃圾命名、大文件、长函数会被报告抓出来。
- 历史大文件有豁免和过期时间，不能永久躺平。
- 后续拆分有明确优先级和验收命令。

## 还有哪些历史大文件

- `agent_py_agent/cli/chat.py`: 989 行。
- `agent_py_agent/agent/agent_core/dispatch_mixin.py`: 889 行。
- `agent_py_agent/agent/memory_archive/query.py`: 839 行。
- `agent_py_agent/agent/subagents/manager_patch.py`: 794 行。
- `agent_py_agent/agent/settings/config.py`: 751 行。
- `agent_py_agent/agent/log_analysis/tools.py`: 666 行。

## 哪些已被护栏管住

- `import *` 已清零并由架构测试约束。
- `cli/parser.py` 已薄入口化并由架构测试约束。
- runtime artifacts 已加入 `.gitignore` 并由架构测试约束。
- 代码规模由 `scripts/check_code_size.py --mode warn` 报告。

## 后续最应该拆

1. `cli/chat.py` 的 TUI/fallback worker。
2. `agent/settings/config.py` 的分域配置。
3. `agent/agent_core/dispatch_mixin.py` 的调度服务。
4. `agent/subagents/manager_patch.py` 的 patch service。
5. `agent/memory_archive/query.py` 的 query/filter service。

## 本轮测试结果

- `python3 -m compileall -q agent_py_agent scripts`: 通过。
- `python3 -m pytest -q`: 通过。
- `python3 scripts/check_code_size.py --mode warn`: 通过，生成 `CODE_SIZE_REPORT.md`，当前 411 条规模/复杂度发现。
- `python3 -m pytest agent_py_agent/tests -q -k "subagent"`: 通过。
- `python3 -m pytest agent_py_agent/tests -q -k "gateway"`: 通过。
- `git diff --check`: 通过。
- `rg "import \\*" agent_py_agent scripts`: 无结果。
- `ruff`: 本机未安装，未运行。

## 未解决风险

- `chat.py`、`dispatch_mixin.py`、`config.py`、`memory_archive/query.py` 仍是历史大文件。
- code-size 当前以 warn 模式运行，strict 阶段需要等主要历史豁免继续拆分后启用。
