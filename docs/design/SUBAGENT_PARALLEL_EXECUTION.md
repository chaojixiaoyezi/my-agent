# 父子并行与活动诊断

状态：并行主链、真实孙级交接、补丁文件登记与派工说明去冲突已验收；故障组合与分工质量仍开放。

## 解决问题

### 第 7 步结果链迁移边界（进行中）

`SubAgentRunnerResultService.record_runner_result` 是当前 runner 结果的唯一组装入口。它按
`run_id` 读取 canonical 子任务，先用当前 `attempt_id` 与 RuntimeDB 状态拒绝迟到或冲突结果；
再把宿主 `turn_end.reason`、能力申请和实际工具／产物事实投影到任务及输出文件。
自然回复不能改判状态，`ok=False` 的正常让出也不能自动成为失败。

收口的既有顺序是：保存结果文件和当前任务投影 → 先持久化待收口事实 → 以原 run／attempt
结算 RuntimeDB → 投递直属父级结果 → 持久标记已投递 → 清除待收口事实。结算、通知或清账失败
沿 `services/runtime_closeout.py` 的原 WAL 与恢复扫描重试；同一 attempt 的父级 wake
按原去重键和投递回执避免重发。第 7 步不得重排这些提交、扩大原锁范围或复制身份账。

当前状态裁决位于 `runner_result_state.py`，输出载荷由 `manager_runner_result_payload.py`
和 `result_processors.py` 生成，持久收口由 `services/runner_result_service.py` 与
`services/runtime_closeout.py` 持有，直属父级通知由 `runner_completion_wake.py` 投递。
第一小片只把生命周期模块里的中文活动标签映射抽成纯展示投影：输入已裁决的状态码和失败类型，
输出短标签；原调用位置再写 `current_step/current_tool`。不移动状态判定、Audit 来源岗位规则、
结果文件写入、run 结算或父级 wake；这些职责各按后续小片核对。

参考只读核对了 Hermes `agent/subagent_lifecycle.py` 的不可变 handle／status／result
公开合同、Codex `codex-rs/core/src/context/subagent_notification.rs` 的状态通知投影，
以及 Free-Code `src/query.ts`／`src/query/stopHooks.ts` 以 `agentId` 限定子代理与主会话动作的局部。
Hermes 所读部分为进程内注册，不适合替代本仓持久 run／attempt 与恢复账；这些参考仅界定职责，
不移植它们的状态库、锁或通知实现。

本地核对还发现结果服务的冲突诊断误从自身读取 `runtime_db`，而权威库实际由 manager 持有；
原冲突仍被拒绝，却缺 `closeout_blocked` 事件。定向回归先复现零事件，再改为读取原 manager 依赖；
只恢复既有诊断落账，不改状态裁决、可恢复 WAL 或父级通知。

本小片已把通用的完成正文截断、产物 refs 和交接信封构造从 `runner_completion_wake.py`
移到纯投影模块。`direct_parent_lifecycle.py` 与活动回合 guidance 直接读取该投影；
`runner_completion_wake.py` 继续拥有根父级 observation／wake 的发布、去重和错误回执。
Audit 来源岗位的专门 metadata 与内部 wake 留在原位，不借这次结构整理重写摄取链。
迁移后删除旧导出，不保留空转发；完成信封字段、截断预算、引用上限和投递时序逐项保持。

第三小片已把 `services/runner_result_service.py` 内的迟到／换代／冲突准入单独归到
`runner_result_admission.py`：只读取 canonical task 和 RuntimeDB 的 exact run／attempt，
拒绝时仍返回原快速结果，并把原 `closeout_blocked` 诊断写入 manager 持有的运行账。
依赖复核进一步把准入参数从完整 manager 收窄为原 `RuntimeRepository | None`；
只有结果服务负责取出这份依赖。文件模式的 None、exact attempt 裁决及诊断失败不放行的边界保持，
准入模块不能借整个 manager 读取工作区、配置、索引或投递能力。此收窄仍为本地候选，未进入已部署包。
结果构造、文件落盘、WAL、run 结算与父级通知维持原调用顺序；服务里的旧方法和
私有函数已删除，不通过服务转发保留第二入口。原事故和换代回归已先通过；下一片再拆实际收口编排。

第四小片已把初次 runner 结果的 WAL→RuntimeDB→直属父 wake→已投递标记→清账编排归到
`services/runner_result_commit.py`。该模块只接受已持久化的 `task/result/output_payload` 与原
`manager/params`，复用 `runtime_closeout.py` 的唯一 WAL／结算／恢复原语；结果服务在保存结果、
任务投影和索引后调用它，不由新模块重复写业务结果。恢复扫描继续只在原模块，不新增队列。
迁移时发现初次交付原本忽略 `mark_closeout_delivered` 的失败回执，仍继续清账；
现在标记写盘失败即保留 pending WAL，由原恢复扫描核对 wake 回执后补清，避免恢复事实丢失。
定向故障注入验证了首次标记失败后原 WAL 仍在、父 wake 只有一条，恢复后清账且不重发。

### 结果交接与路径一致性补修

发现工具已登记真实文件，但自然 final 不经过旧结构化输出分支，canonical 产物清单为空。
收口现读取本 run 工具 registry，投影最新 ready 文件的精确路径；不要求模型复述 JSON，
不从总结抽路径、不搜索同名文件、不代写产物。结果、父级通知与代理树复用同一投影。
工具输出归档、其它 run、删除或 missing 记录排除；读取异常保留诊断，不触发自动重跑。
同一 run 多个历史 artifact_id 指向同一路径时只用最新状态，旧 ready 不复活已删除文件。
补丁工具把预检路径与实际提交清单转换为精确 artifact_refs；新增、修改、移动、删除和部分失败
都沿统一工具归档落账。不从 `files_modified` 的显示名或成功正文反推实际路径。

删除旧输出重定位、交付映射与收口复制逻辑。相对声明与工具使用同一可信 cwd，绝对目标保留；
权限继续由继承范围和正式授权裁决，不因输出声明增权。内部 run 根与 cwd 分开展示。
真实孙级已确认一名统筹和两名孙代理均完成并交接，统筹在孙代理运行时写了自己的报告模块。
根模型仍主动等待，统筹也曾重复写孙代理负责的模块；路径交接修复与模型分工质量分别记录。

有用的独立工作不能被未完成的兄弟阻塞；已完成的结果应及时交给直属父级。
等待不等于失败，进程心跳也不等于模型或工具有进展。

## 执行与等待

- `list_agents` 先沿原 owner/subtree 裁决取得快照，再投影紧凑状态和真实 `read_order`。
  界面仍有完整恢复细节，模型正文及其归档不再携带内部 summary、checkpoint、inbox 等路径。
  实际产物优先；没有产物时仅可引用规范终态且已存在的最终报告，文件存在不把失败改判成功。
  超长视图保留有效 JSON、原样文件引用、节点及引用省略计数，复用 `read_artifact_hint` 逻辑引用恢复，
  不猜归档文件名、不放开运行时目录、不新增状态存储。查询提示复用共同分工说明，不强制父级先让出。
- 创建的 `current_turn_run_state` 只投影 canonical 状态和故障事实，不根据孩子的运行状态另发父级
  `next_action`。父级工作建议保留在创建回执既有入口；不能一面建议继续、一面要求等全体完成。
- 分工软说明同时覆盖父级与孩子；先保留自己可推进的工作，已结束产物按真实引用整合，不要求协调者
  亲手重写下级成果。用户限定主代理不能实现时仍遵从，不因为慢孩子而抢写或启动替身。
- 协调者只加载自己的创建时角色快照及可选角色索引，不再注入所有角色正文。旧角色中全部外包、
  结束后也不得接手、让模型手写内部结果 JSON 的说明已删除；自定义冻结角色不会被当前内置模板覆盖。
  这属于矛盾修复，不新增开关、工具、权限、状态库或强制忙碌检查；分工质量仍由真实任务检验。
- 创建是普通异步工具结果。主、子、孙继续相同模型/工具循环，不因创建自动结束工作片。
- 回执提供已启动身份和事件交付方式，不强制父级结束。模型根据真实依赖继续独立工作，
  没有合适工作时自然让出；不以“保持忙碌”为由反复查状态、制造文档或重复执行。
- 有孩子仍运行时，自然让出的 task-local 父级保存 exact direct-child wait marker，
  不标为 DONE；任一新结果可以解除等待，而不是要求所有兄弟结束。
- 新工作片若再次等待，只登记仍活跃孩子，已处理终态不重复触发；暂停、取消和未知副作用不绕过。
- 模型答复后、等待登记前刚到达的结果按“模型已见快照”识别，未读终态仍保留待交接；
  等待记录的释放为原子操作，两名孩子同时完成不能各自消费同一记录。
- 普通成功通知只保留已有可配置短合批窗口，期限取首条已持久通知，不随慢兄弟无限后延。
- 合批只确认本轮实际交付的信封；预算外、晚到或重复投递各走现有去重和恢复账。
- 父级忙时在安全边界接收，空闲时沿原执行车道唤醒；不能同时启动第二个父执行器。
- 主代理给子代理分工、子代理给孙代理分工使用相同纪律；不能靠解析任务正文猜依赖或另建目录锁。

## 活动诊断与控制边界

- 活动证据复用模型调用账、工具执行状态、runner session 和 exact attempt；不建影子任务状态库。
- 区分首 token 等待、流式输出、工具执行、权限等待和执行器已退出。
- 持续慢流、长首 token、无输出长工具不能仅凭经过几分钟就判失败。
- 阶段持续异常时给父级有界诊断和精确身份，去重通知；父级可查看、插话、停止或继续等待。
- 明确退出沿原退出恢复合同；副作用未知保留 UNKNOWN，不重复执行或自动派替身。
- 提醒不能构成新的机器完成验收、固定强杀期限或周期性无变化 LLM 轮询。

### 配置和持久化

配置统一在 `agent_py_agent/config/capability_config.yaml` 与 `CapabilityConfig`：

| 配置 | 默认 | 含义 |
|---|---|---|
| `subagent_activity_notices_enabled` | `true` | 开启阶段化提醒，使用原 runner 心跳，无新线程 |
| `subagent_first_token_notice_seconds` | `600` | 首 token 或供应商退避结束后长期没有事件 |
| `subagent_stream_idle_notice_seconds` | `180` | 已开始输出后静默，或模型/工具边界间没有活动 |
| `subagent_tool_wait_notice_seconds` | `900` | 工具长等待；有精确审批记录时标记等待审批 |

三个时间项的 `0` 表示关闭该阶段提醒，不是执行超时。供应商安排的退避时间不算静默。
实际 provider 超时、显式任务预算和用户停止仍按原协议处理，提醒不覆写这些边界。

诊断保存在 canonical run 的 `attributes.runtime_activity_diagnostic`，schema 为
`subagent-activity-diagnostic.v1`。`notice_key` 由 run、attempt、阶段和活动身份构成，
同一阶段没有新证据只提醒一次；活动恢复更新为 `progress_resumed`，不另开健康汇报模型轮。
旧进度快照不能抹掉独立诊断字段，旧 worker 不能给新 attempt 写诊断。
根父级走既有 observation/wake 原子对，递归父级读原直属快照和等待记录，不越级广播。
快照按当前 root 的索引选 ID、canonical 文件核对，不扫描用户全部历史；心跳时间不进入模型快照。

## 验收

先定向合同、替身与重放，再用单 Gateway 的多 TUI 真实任务。
覆盖快慢混合、两批独立结果、递归派工后本地工作、父级忙/闲、取消、重复/晚到结果、
重启恢复、慢流/长工具不误杀，以及插话不丢。核对事件消费、执行身份与真实工具结果，
不能用 Working 动画或模型口头承诺代替证据。

### 本轮证据边界

- 单 Gateway、同一已验证官方模型配置，四路真实 TUI 分别完成 Markdown 检查器、日志分析器、
  资源巡检工具和已有工具的三分钟采样；不由测试者补写业务代码或产物。
- 日志解析子代理先完成时，文档子代理仍运行，父级已读取解析结果并更新整合进度。
  资源巡检任务中，父级在孩子运行时编写自己的命令入口；完成后各路 Working 正常收起。
- 独立重跑产物现有测试：Markdown 检查器 49 项通过，日志解析器 4 项通过。
  这是基本可运行性证据，不等于完整功能与文档毫无缺陷。
- 短阈值补验记录真实 `tool_wait`、`failure_confirmed=false`、`automatic_action=none`，
  投递回执为 `handled`，同一执行代自然完成。短阈值故意增加通知，不能用它评价正常费用；
  测试完成后移除覆盖文件，发布默认值保持上表不变。
- 早期两路分层需求被扁平派工，当时未计孙级通过。后续离线发布检查工具已有真实深度二身份，
  孙代理、统筹和主代理均正常完成；统筹在孩子运行期间写自己的报告，文件进入 exact run 交接。
- 报销核对工具与同会话追加修改完成：主代理做入口，孩子做校验；追加 Decimal 和错误 CSV 导出。
  追加实测暴露补丁成功却缺少文件登记，已补通用回执与删除记录。
- 最终包报销追加验证了 BOM、NaN、Infinity 和错误文本；另一路补丁定向任务实际调用补丁工具，
  文件进入该 child registry，自然完成回执和父级汇总均含正确路径，父级正常运行错误输入与 scan/diff/report。
  补丁后模型还使用了编辑工具修正其它代码；这里不宣称它仅用补丁完成整个任务。
  移动、删除、部分提交由真实工具处理器到归档和交接的定向测试覆盖，未宣称全都有模型级 TUI 样本。
- 发布检查工具的根模型没有实际验证文件删除，并如实说明限制；基本运行通过不等于全部需求达成。
  重复实现、过早等待仍保留在 [当前问题](../../STATUS.md)，不靠目录锁或强制忙碌修补。
- 去冲突后的三路 TUI 均结束：报销任务根在两个孩子完成前写完自己的汇总与入口；真实依赖任务
  自然等待并由孩子结果唤醒；发布工具同会话追加创建一名协调者和两名孙代理并完成整合。
  首轮分层需求仍被扁平化，追加时父级又把入口交给组长，组长首次写入也晚于两名孙代理完成。
  这些是分工质量偏差，不用最终交付掩盖。后续紧凑状态切片已修查询范围和内部恢复引用误导。
- 紧凑状态新包的双 TUI 完成日志分析与目录核对，含协调者和两个真实孙代理。完成后查询
  不退回整个 owner 历史；显式主请求根不要求主代理必须存在于子代理表；精确产物与终态报告均成功读取。
  另发现正常 SUBAGENTS_ACTIVE 让出曾被填成 runner_error，后续补修见下节。

### 正常让出的失败分类补修

结果写回复用唯一结束映射：显式原因、当前状态和成功标志一致且没有明确失败时，清当前错误投影。
`PENDING / interrupted / ok=False` 仍表示未完成，不能补 `runner_error` 或把正常让出说明写入最近错误；
后续自然完成同样清当前旧错误，历史尝试、能力阻塞、取消、截断和未知/冲突结果保持原语义。
237 项定向通过、1 项既有 xfail。单 Gateway 的官方 MiniMax-M2.7 实际 TUI 捕获递归父级两次正常让出，
先处理一路完成后只等待另一路，第三次执行自然 DONE，空错误字段落在 canonical 状态与结果文件中。
另一路明确 `/stop` 保留 `CANCELLED / cancelled`，进程树终止已确认，8 条原记录在后续两分钟内未增长。
平级样本完成 8/16 条采样及正确平方和，能力阻塞也正确保留，恢复完成后清当前旧错误；模型未遵从原递归分工，不能记作递归通过。
递归样本两路各 30 条、立方值正确，但报告的 A 路时间与最终 CSV 不符；主代理读到真实时间后仍未发现矛盾。
状态修复验收与报告质量失败分别保留，未代写任务文件或修正模型报告；原始记录只留仓库外。
