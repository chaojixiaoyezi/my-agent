# 已实现能力索引

通用启动观察竞态已本地修复：host 退出后复读原 session，避免短命令提交终态被旧快照掩盖。4 项旧实现失败用例修后通过，383 项相关回归通过；原身份、取消及退出码判定保留，尚待发布和真实 TUI 复验。

发布修复本地已将唯一进程内取消令牌迁入 `common/cancellation.py` 并同步所有消费者，删除旧入口；三个旧测试夹具/断言同步当前接口。原失败及受影响链的 1,195 项回归通过；发布全仓及本地严格 gate 已通过，`6050ab600` 已同包部署双机；实际 TUI154—157 正在验收。

执行文档已增加 [逐项 TODO](tasks/REFACTOR_PLUGIN_GOAL.md#当前-todo唯一执行清单) 与按编号汇报规则；当前 4.6 拆为七项，逐项标明交付、验收条件、状态和负责人。此项仅为计划可见性改进，不新增运行能力或真实 TUI 通过结论。

第 4 步本地源码新增逐次插件工作区读取上下文：原 registry 冻结 cwd、exact 交集和原路径策略，
只有固定连接声明支持版本的插件收到 `_meta`；不改变业务参数或普通 MCP。纯路径裁决供核心和后续 SDK 共用。
后续 SDK 与 workspace-peek 已有标准 wheel/ZIP、独立 MCP 文件/目录读取及原宿主完整装卸组件验证；实际多 TUI 尚待完成，验证见 TESTS。

第 4 步本地接线：停用在原执行器和完整资源退出得到确认后，安全删除固定旧环境，再 CAS 释放原激活；已经可以重新启用。
原准备函数仍在执行时明确保留待释放状态，不等待业务锁；原 UNKNOWN 不因后来清理成功被改写。
管理结果成功保存并严格读回后才消费固定退出引用，失败保留证据，原请求重送只补消费，查询不写。
原 session v4 为准备资源固定保留策略，普通任务历史裁剪不变；旧 v2/v3 原版本读写。
卸载也已沿原管理链本地接通：释放返回准确记录，原锁 CAS 删除安装，原成功结果读回后才回收退出证明与无人引用包。
原锁读回确认已删除但刷盘/解锁返回异常时，保留确定成功和存储警告；UNKNOWN 不猜成功。
目录 v3 的安装引用由原提交派生，解决同包重装版本归一后旧目录误用问题；不新增持久代次或墓碑。
显式业务命令已沿原 HostCommand/ToolExecutor/MCP 本地接通；公共绑定器只交工具参数，宿主选择另存摘要。
原执行区间内复用一次批准，拒绝不执行；等待时撤销不能换代复活，重复请求和卸载后的查询读原结果。
握手和发现使用命令自己的取消 token，单次连接清理与业务结果分别报告；普通用户只可查自己的业务请求。
TUI/Gateway 的独立命令审批运输已本地接通，解决面板等待阻塞读流、客户端离开后审批无人消费的问题。
审批地址使用原服务根及服务端规范 owner，避免用户映射后的地址分叉；原 FIFO 不创建聊天回合。
完整多 TUI 装卸仍待实际验收，未推送部署，不进入第 5 步。
解决停用后不能重新启用、原 handler 未结束就删环境，以及结果未落账就丢退出证据的问题。

以下为前片实现边界，当前进度见首段。

本地开发：实际 `/plugins enable` 已串通原计划、隔离环境、完整 MCP 目录和候选清理，再发布同代激活。
原 Registry 在新运行边界接入工具，解决各权限视图漏目录、关闭后迟到连接复活与停用旧快照误记未知的问题。
资源清理证明沿原 session 持久保存，命令自然退出历史不被清理覆盖，迟到 host 写入不能擦除已确认事实。
实际临时 wheel/venv/MCP 与原工具执行器已验证读取和停用；这些是组件测试，完整释放/卸载及多 TUI 尚未验收，未发布部署。
当前测试结果见 [TESTS](../TESTS.md#第-4-步实际启用与新运行工具组合开发验证)。

本地开发：管理 disable 沿原管理员权限、请求/工具执行链撤销固定代次，并关闭原准备 attempt。
准备与共享 MCP 分别冻结并锁外清理，解决只停共享服务却遗漏安装进程的问题；终态准备记录也核对 host。
阻塞调用不挡管理，其他插件/任务保持运行，独立 host 创建与撤销交错有组件验证。
本片仍保留 revoked 激活和退出证据；完整启用、贡献发布、释放/消费及卸载和实际多 TUI 尚未完成，未发布部署。

本地内部实现：固定激活引用、原托管 MCP 管道和发送准入已串通，旧代理绑定原连接。
未发送拒绝不关闭共享连接，未知启动/清理保持原回执；原目录锁可取消等待，提交不被取消截断。
完整管理装卸和实际多 TUI 仍未完成，未发布部署。当前开发验证见 [TESTS](../TESTS.md#第-4-步托管-mcp-与鲜活准入开发验证)。

本地开发：原后台 host 已支持 stdio 三路字节继承，session v3 区分普通任务与共享插件代次，解决通信落日志及误停共享连接的问题。
旧 v2 原版本更新/redo 恢复，原锁/CAS/精确清理保持，共享退出证据不参与普通裁剪；20 个相关文件 523 项开发验证通过，源码无漂移。
该 stdio 片当时未接 MCP；后续内部接线见首段，完整启停仍未部署或实际 TUI 验收，见 [托管管道](design/MANAGED_PROCESS_STDIO.md)。

本地开发：唯一安装表 v3 已支持同一环境计划的激活预留、发布与撤销 CAS，解决多进程旧快照恢复原代和并发覆盖的问题。
准备、发布及待清理撤销均阻止改配置或准备另一代；撤销不等待资源配额锁，提交未知保持原分类。
20 个相关测试文件 549 项通过，本地严格 gate 与新增内容隐私检查通过；均为开发验证。
旧 v1/v2 按原字段显式迁移，原配置及迁移来源保留。完整启用、stdio 托管、调用与资源清理仍待接通，尚未部署或实际 TUI 验收，见 [激活权威](design/PLUGIN_ACTIVATION.md)。

本地开发：MCP 将生命周期、固定进程连接和协议响应箱拆开，解决旧排队/线程影响新连接及明确关闭后被自动重连的问题。
临时连接失败仍使用原退避；清理先核对出生身份和可观察进程树，未确认不启动替代连接，管道由所属读线程关闭。
目录发现和发布绑定同一连接，权限视图共用永久关闭事实。未发布部署，跨进程插件激活和真实多 TUI 验收仍待完成，见 [连接合同](design/MCP_TRANSPORT_LIFECYCLE.md)。
同批全仓核对修复工作片残留旧权限导入，解决主/子普通运行无法进入模型的问题；直接消费共享 owner 裁决，权限范围保持。

本地开发：环境准备已取消裸进程路径，改用原 operation 固定计划和原托管进程账，解决崩溃后候选/进程归属不清及交接后准备脱离期限的问题。
原工具资源权限检查可核对冻结 claim 和锁，默认 preclaim 行为保持；准备进程绑定宿主寿命，普通长期后台仍独立。
完整启用命令、MCP 贡献、撤销、发布部署和真实多 TUI 验收尚未完成，见 [环境合同](design/PLUGIN_ENVIRONMENTS.md)。

本地开发：`/plugins configure <插件> --file <JSON>` 已接 HTTP/direct 原管理与工具执行链，完整替换停用插件私有设置。
解决配置值泄漏到命令历史/工具账、旧请求覆盖新设置和无迁移记录改写安装表的问题：值只写 owner 私有表，原 schema 严格校验。
唯一安装表 v2 同次提交配置、版本和回执，明确记录 v1 来源；目录 v2 携带安装版本，旧版本不自动重放。
安装与配置复用有界授权来源读取，旧私有读取入口移除。开发验证见 TESTS；尚未发布部署或进行完整实际 TUI 验收。
下一环是启用前资源绑定、激活、MCP 发布与精确撤销，不能将配置成功当作插件可运行。

本地开发：独立 Python 环境内部准备器从固定本地 wheel 建立最终地址的 venv，验证依赖闭包、安装目标与实际内容。
解决依赖污染核心、安装器覆盖和启用前执行插件的问题；使用 PyPA 公共元数据规则与标准 pip，不新建依赖解析器或激活表。
原 owner 配额、取消 token 和精确进程退出复用；临时组件已验证导入陷阱未执行。尚未接启用命令、发布部署或完成产品 TUI 验收，见 [环境合同](design/PLUGIN_ENVIRONMENTS.md)。

本地开发：插件管理复用现有管理员和 owner 权限，经原执行器安装静态包并默认停用；HTTP、direct 与 TUI 客户端共用请求和查询语义。
解决响应丢失后的重复安装和误报未执行：原请求只执行一次，查询不依赖来源或目录，已知操作结果与未完成收尾分开，UNKNOWN 不自动重跑。
冷管理与完整代理共用 owner 权限裁决，模型配置与插件入口共用原会话 Store 组装，旧重复入口移除。配置开关、禁用工具和原磁盘配额均生效。
此片尚未发布部署；独立环境和配置的后续进度见本页首段，激活、撤销及实际多 TUI 装卸仍待完成。

本地开发：显式宿主命令沿原 RuntimeDB 原子登记独立执行身份，同一请求改变输入会拒绝，重复提交不创建新运行。
普通代理和管理动作共用同连接创建代码；原工具账回读验证 owner/task/run/attempt 与输入，终态重放不重开执行权。
重放当前未执行与原执行事实分开，损坏或矛盾结果保持未知；管理执行已由后续本地切片接通，完整装卸仍待完成，详见 [合同](design/HOST_COMMAND_EXECUTION.md)。
原收口取消尚未开始的占位时保留输入指纹、幂等身份与资源声明；严格结果查询拒绝损坏 JSON 和未知版本，不生成假的可重放回执。本项尚未发布部署。

本地开发：静态插件包描述、有界 ZIP 读取及默认停用的安装事实已实现，公共命令和严格 JSON 读取同源。
解决来源变化、并发丢记录、重试覆盖状态及提交后误报未执行的问题；同一字节快照保存到原 owner 内容地址。
后台专用锁已移为公共目录锁，原锁名和锁顺序不变；锁叶子拒绝链接，不替换原锁文件。
安装事实片未发布、未部署；后续管理入口已有本地接线，隔离环境/激活/撤销仍待完成。开发验证见 TESTS，真实 TUI 装卸留到链路接通后。

本页列产品能力，不保存逐次测试流水；“已实现”不等于所有极限环境验收完成。

宿主命令目录与提交版本（已发布同版双机，所测框架入口通过）：原 owner 解析产生只读声明快照，
HTTP、plain 与 TUI 共用结构校验与过期拒绝；Tab/命令异步请求，普通输入不访问目录。
首次选择的版本跨参数编辑和参数补全保留，换作用域或迟到响应不覆盖当前缓存；实际安装/激活贡献仍为空。
TUI 151—153 的原生键盘恢复、活动命令隔离和主/子工具链已验；旧版本拒绝仍由合同/HTTP/组件测试覆盖，不能宣称真实插件换代已验。

插件公共参数合同（已发布同版双机，所测入口已复验）：同一声明驱动管理帮助、结构化参数绑定和 TUI 候选，
支持字面路径、引号、长短选项及 `--`；无效输入不进入模型、Shell 或旧会话控制。
补全只编辑，停用插件只显示静态帮助；安装、启停、卸载和业务执行仍不在本片已实现范围，宿主目录已由后续切片接通。
实际 Enter 自动追加可选帮助旗标的缺陷已修复；TUI 146 接受候选后正常提交，两端各 13 类命令检查通过。
483 项相关回归和严格 gate 通过；原失败、普通任务及模型校验器质量分别见 TESTS，不以短任务覆盖长任务。

任务资源停止（已发布并同版部署）：解决主代理已交接的后台程序在 `/stop` 后仍运行，以及启动/恢复竞争可能复活旧轮的问题。
原执行权和派工入口先关闭，主任务与完整原子树在同一边界冻结后台/PTY 清单，锁外清理不重扫后续任务。
RuntimeDB 与 canonical 文件各自保持唯一接纳权威；准确 pending 与一次性消费阻止迟到、重复启动和旧快照回滚。
原心跳在独立 runner 内转交精确中断；宿主只读确认，终态业务历史与 UNKNOWN 锁保留，不以共享 PID 强杀整树。
插话沿原排序锁和 fresh pending 接续，direct/local 与未晋升请求持正式绑定，不把消息编号当执行身份。

后台 v2 存储与启动交接（已发布）：原 JSON 地址上共用目录互斥、CAS 和 redo；启动先预留、再绑定 host/child、最后明确交接。
交接后独立资源归 manager 持有，日志上限归 host；冷热查询同读 Store，停止只消费冻结实例，未知不发成功通知。
旧启动后登记、launcher watchdog、生命周期自由函数转发和旧全量保存分支已删除；v1 仅按原显式句柄管理，不猜执行归属。

源码 `6713c769f` 已发布 main，双机同 wheel、每端 1,210 个包文件一致；全仓及本地严格 gate 通过，线上 CI 未作为验收来源。
实际 TUI 138 验三类控制及主后台，140 验三个孩子与另一会话隔离，141 验交互 PTY，142 验真实孙代理；139 独立长采样自然结束。
历史 TUI 137 停止失败保留，新版本分项结论、模型脚本错误及未实测旁支见 [TESTS](../TESTS.md#整任务停止发布与实际-tui-138142)。
此停止修复不代表插件装卸已完成；后续参数与宿主目录已独立验收，真实插件激活和执行权限仍待实现。

公共命令首片已发布并同包部署：核心声明、显式别名、会话词法、帮助与静态补全集中到公共目录；原 CLI 目录和旧识别入口删除。错误插件命名空间在 TUI、HTTP 和文件队列前拒绝，核心控制及回执链保持。后续参数与宿主声明目录片见本页首段，真实插件装卸与业务权限仍待实现，发布部署和实际 TUI 验收另见 STATUS。

后台进度策略纯计算已独立：无进展计数与确定性失败退避仅接收标量事实，调度器保留原读取时机、租约和持久记账，删除旧实现及专属导入。
算法等价与相关自动回归通过，安装版多 TUI 的短任务、单子代理和持续并发链路已验；内部策略真机命中未证，模型交付失败仍见 STATUS，后台调度整体拆分继续进行。
供应退避状态与消费守卫已独立并部署，保持原配置组装、共享实例、失败时刻起算和 typed 错误分路；正常多 TUI、原定时工作在实际连接失败后的冷却与自动续接、另一前台会话隔离及原设置回切已验。仅操作专用本地代理，系统网络未改；此切片收口，其余路由和租约恢复继续，覆盖边界见 TESTS。
Goal 状态处理与投递地址已分离，删除混合职责的 GoalMixin；新模块只接收原领域与窄能力，观察执行仍走原 claim，能力预扫仍在原唤醒位置。353 项相关回归与独立复核通过，已同版部署双机；TUI 120—123 的长 Goal 暂停续采/Compact、普通任务、三子代理和定时投递链路已验。该路由切片所测框架范围收口，模型交付失败及未覆盖支路见 TESTS。
租约/恢复已迁移并同版部署：后台领取与结算、恢复查询与日志去重各有独立模块；共享心跳、持久租约和来源消费保持。原最终准入与收尾顺序先用旧实现合同固定，迁移后的 324 项定向回归及独立复核通过；TUI 124 真实续租/重连、128 同车道串行化和 130 原请求有序恢复已验；133 的目标控制、资源跨代复用与自然退出通过。第 2 步本轮框架验收收口，交付失败与未覆盖分支不改写。
稳定资源误标脏的既有缺陷已单独修复并发布部署：保留 STABLE 与原 DIRTY，未确认状态仍保守处理，原权限/CAS/锁不变；186 项相关回归和严格 gate 通过。旧现场不自动清理，真实覆盖边界见 TESTS。

诊断场景清理（已发布部署）：移除已停用的结果块修复入口、专用替身和失效调用，旧 case 名由参数解析器拒绝。
保留的 runner 重试场景按职责更名，11 个保留定义的语法树与原实现一致，原有收口断言失败继续记录；没有改变宿主结束协议或持久结果格式。
本地严格 gate 通过后同包部署双机；实际 TUI 91—94 的单子代理和普通文件对照通过，此短回归不替代未通过的长任务验收。

声明窗口事实传递（已发布部署）：解决子代理已发布的未满窗口及剩余秒数在前后台完成投影中丢失的问题。
中性合同统一筛选、预算裁剪保留数值；无新增计时器、调度规则或质量硬门。自动回归与旧事件回放通过，实际 TUI 89 的后台消费保留原值及终态；完整任务验收见 STATUS。
参考定向读取 Codex `context/subagent_notification.rs` 中身份和结构化状态的原样通知；对应合同索引未命中该文件，不宣称整库审阅或引入其生命周期策略。

Shell 输出行数：解决真实工具回执将三行文本写成四行的问题，模型回执、截断说明和展示事实共用采集行数。
空输出为零，末尾换行不新增空行；原文、采集完整性和执行流程不变。118 项定向和本地严格 gate 通过，已发布部署双机；TUI 80/82 实际回执和展示事实一致，报告质量另列。
参考 Codex `utils/output-truncation/src/lib.rs` 的源文本行数统计，以及本项目 `text_file_window.py`；未找到对应专门合同索引，不宣称完整审阅参考工程。
此项不解决 TUI 71 的模型报告错误，也不新增业务格式判定或自动改写模型报告。

响应头等待取消：JSON/GET/SSE 共用 HTTP open 中断边界，连接清理竞态不再把已取消请求误报为连接状态错误；
未取消异常保持原样，不重新发送。确定性竞态和实际 socket 回归见 TESTS，最新发布验收见 STATUS。

后台审批归属：子代理与孙代理优先从权威 run 读取执行身份，父会话里的任务关联不再把它们误判为 main。
主代理仍要求有效 claim；损坏记录不回退。定向回归与安装版子/孙批准原调用、子代理拒绝不执行已验；报告质量及其余边界见 STATUS。
同一执行链拒绝记忆的传递已发布部署，覆盖子代理 Goal 自动续轮及前后台 Compact；TUI 70 已验证同参自动续轮不重复询问且不执行 handler。
此结论不涵盖改参数或换工具；后续嵌套 Shell 后台启动及超时遗留进程已发布并同版部署双机，实际 TUI 78/79 复现及 TUI 75 普通文件对照已验。
语法检查移到唯一纯模块，受管后台参数与错误合同保持；进程组清理继续使用原终止入口，不新增按命令匹配的回收路径。
无生产调用的旧 verifier integrity 模块及自造数据测试已删除，离线矩阵改指真实被动验证实现与现有回归；矩阵仍只检查文件完整性。

- 会话：持久历史、前后台接续、用户插话、会话独立模型选择。
- 代理：递归创建和控制、精确身份、生命周期收口、父级结果通知。
- 工具：文件与搜索、补丁、命令、PTY、网络、MCP、权限与输出归档。
- 记忆：用户隔离、摘要归档、检索、技能发现和按需正文读取。
- 界面：流式正文、折叠思考、工具展示、历史分页、完整详情、任务清单与子代理视角。
- 运维：单 Gateway、多用户、持久调度、健康诊断、受管后台进程和部署脚本。

当前仍有开放风险，见 [STATUS](../STATUS.md)。功能操作见 [CLI_REFERENCE](../CLI_REFERENCE.md)，设计入口见 [DESIGN_LEDGER](../DESIGN_LEDGER.md)。

## 子代理正常让出的失败分类

解决正常等待被未完成布尔值误标成 `runner_error`、并写入最近错误的问题。
写回层复用已有结束协议，只在显式原因与当前状态、成功标志一致且无明确失败时清当前错误；正常完成也不残留上一轮错误。
正常让出仍为未完成，截断、取消、授权阻塞、异常及未知/冲突结果保持诊断；原调度、唤醒、历史与持久格式不变。
237 项定向通过、1 项既有 xfail；真实 TUI 的当前证据与模型分工偏差见 STATUS。

## 模型探针端点一致性

解决 Messages 诊断重复 `/v1`、Chat 配置完整接口后诊断重复后缀而误导排障的问题。
探针复用实际传输已有的 `endpoint_parts()`，保留代理前缀、完整接口及尾斜杠处理；不新增地址规则或改变请求。
三协议 36 组地址合同及相邻测试共 228 项通过；新版官方 MiniMax-M2.7 真实 TUI 自行写程序、运行并读回正确结果。
一次读取参数错误被拦截后自行修正，原错误保留；真实工具链与替身地址合同分别留证。

## 后台主代理具体工具审批

解决后台 Goal 工作片遇到具体工具审批后，用户看不到面板、无法批准原调用的问题。
主/子后台共用原审批账本与 TUI FIFO，主代理另绑定当前选定任务的有效 claim；新轮不能消费旧批准，旧内部名称无兼容转发。
Goal pause 保留当前等待，interrupt 取消当前调用，任务资源停止另沿原精确归属服务处理。
428 项定向及官方 MiniMax-M2.7 双真实 TUI 已验：暂停后审批、跨轮读写原 PTY、批准原调用、拒绝不执行。
抽签程序只启动一次，30 项输出与报告一致并正常退出；历史质量失败不因此改成通过。

## 存储领域组合首片

解决用量与计时依赖整条继承链、修改调用难追踪的问题：用量账本与 Goal 时钟改为显式组件，
JSONL 读取下沉独立 IO 模块，调用方、动态能力读取及测试替身一起迁移；删除旧类和无调用包装。
保留原路径、JSONL 格式、累计增量、追加锁和线程 CAS；不新增配置或持久源。其余存储领域继续逐步迁移。
629 项定向及新版双 TUI 的暂停计时、压缩重连、真实续采、并行插话和主子用量通过；
报告汇总与明细的一处矛盾作为质量失败保留，详见 STATUS。

后续公共目录/路径、扫描索引和执行租约也已迁为显式组件，删除基础类和 claim 继承类。
Store 从 6,054 行降到 5,142 行，调用方、动态能力读取和测试替身改为实际组件入口；不保留根路径或方法转发。
1,065 项定向通过、4 项既有 xfail；新版双 TUI 的租约、暂停/压缩/重连续采、并行插话和原生投递去重已验。
保持原路径、JSON schema、锁、租约恢复归属与归档顺序；其余领域尚待组合，报告质量边界见 STATUS。

Goal、观察、唤醒与进度策略也已独立组合，继承链剩五层。它们显式接收共享目录、时钟及所需跨域能力；
跨领域旧账归档保留原调用位置和顺序，无调用包装删除，通用 IO 不再反向依赖 Store。
208 个原函数/方法、32 个生产调用文件核对，定向 1,236 项通过、28 项既有 xfail；严格尺寸通过且基线未增。
新版双 TUI 的暂停/压缩/重连续采和并行 4,000 条中文数据核对通过；执行纪律与报告原因错误另记，详见 STATUS。

线程、消息、任务关联和 Audit 现也通过显式组件提供，Store 从 3,774 行降为 1,990 行，继承链剩两层。
保持原路径、线程与任务锁、消息幂等和修订提交顺序；八个旧包装删除，81 个生产调用文件同步迁移。
2,206 项定向通过、28 项既有 xfail、1 项平台跳过；新版实际 TUI 的暂停续采与并行长命令已验。
插话事务分层随后完成：回执、账本、提交、确认和恢复独立组合，Store 从 1,990 行降为 134 行且无领域继承。
52 个原定义及 16 个生产调用文件核对，2,362 项定向通过；无调用的消费包装删除，原锁及持久迁移保留。
新版运行中完整插话及并行长命令已验；按最新控制语义，Goal 暂停后当前执行与 PTY 继续是预期行为。
Goal 自动续跑、回合中断和任务资源停止已分离，并验证了明确停止的资源归属；恢复验收及失败见 STATUS。
Audit 来源发布、模型履约及忙碌输入的验收边界见 STATUS，不把结构迁移等同所有任务质量通过。

## Gateway 流式出口与 Compact 携带

后续请求边界已拆分为上下文准备、运行绑定、历史提交/补交与输入渲染，执行文件从 3,630 行降至 1,182 行。
完整行窗口迁入会话领域，前后台共用且保留原生 metadata；旧私有导入不留转发，三个无用参数与无引用包装已清理。
110 个定义/常量的搬移前后逻辑一致，666 项定向通过；新版真实 TUI 的暂停、压缩重连、原程序续采、
双子代理、插话与同目录后续编辑已验。报告数值准确性仍有失败样本，详见 STATUS；其余存储组合继续推进。

后续停止补修：子代理取消和宿主回收复用工具层进程树终止，覆盖另开 session 的命令，
删除旧信号/等待实现；无句柄时按 POSIX 内核状态核对退出，未确认不能报告已终止或重派。
263 项定向通过、1 项跳过；本机真实 TUI 暂停后约 78 秒无新增，另一个会话仍采集；
原会话压缩重连后真实程序补到十条，合计 3025，原三条不变。追加并行采样各八条、重叠 81 秒。
旧停止失败和首轮审批等待导致的零重叠均保留；强制终止缺 native 信封的边界未消除。

解决问题：文本缓冲、公开事件清洗和审批交互混在请求编排中，恢复追加流事件也需要反向加载执行器。
流出口按缓冲、事件投影、审批交互组合；正常与恢复从同一队列路径派生 chunk，关闭后保留迟到读取。
前后台同片超窗携带共用纯计算，mailbox 释放和 canonical 历史提交顺序不变；CLI 的不同归档语义保留。
原空关闭函数、进度清洗转发和重复携带实现已删除，调用方与补丁入口直接迁移。
定向 510 passed、3 skipped、3 xfailed，严格尺寸 hard=0、基线未改；新版真实 TUI 结果见当前状态。
Gateway 其余请求适配已完成上述拆分，存储组合仍属后续范围。

## 模型后端职责拆分

解决问题：公共模型接口、HTTP、Chat、Messages 和工厂集中，协议改动需要阅读大量无关实现。
公共合同、传输和各协议已分别归位；包级公开接口保持，内部调用方直接迁移，旧实现不保留转发。
Messages 请求使用冻结对象显式传递局部控制；清理重复和不准确注释，原请求和错误语义保持。
本地后端定向验证 522 passed、4 项既有 xfail；会话纯策略独立，相关定向 201 项通过。
真实官方 MiniMax-M2.7 的多个 TUI 已覆盖数据分析、长工具、取消续做、重连、多子代理和 Goal 暂停恢复。
发现并修复 Full Access 后台状态地址漂移与 PTY 旧解析方法调用；工具相关定向 140 项通过，真实等待和交互已验。
桌面输入修复英文丢字及中文忽略；实际保存后读回一致。认证和桌面相关定向 103 项通过，
官方登录因设备码端点 HTTP 530/403 仍未进入账号确认，不能报告完整认证通过。
详细边界见 [可维护性评估](design/MAINTAINABILITY_AND_JEV_REVIEW.md)。

## 后台上下文与历史准备边界

解决问题：调度执行器混合维护上下文预算、任务隔离和原生历史，使续跑相关修改难以局部阅读。
准备过程已独立为上下文与历史种子模块，显式请求协议只描述所需身份和唤醒字段。
沿用同一 canonical 历史和 provider 投影；任务范围、读取失败、展示字段排除及进度对账顺序保持。
执行器不保留旧转发实现，重复策略快照函数已删除；锁、事件消费和交付事务仍在原位置。
相关定向 326 项通过，真实双子代理返回、运行中插话，以及 Goal 暂停、压缩、重连后恢复已验。

## 后台单工作片执行边界

解决问题：模型调用、同片 Compact 重试和原生历史保存夹在调度、唤醒与投递中，且九项位置元组容易接错字段。
这些执行职责已归入 `background_execution.py`，只接收 Agent、唯一 store 和参数准备接口；
结果使用具名字段。压缩后读取最新线程，原模型作用域、取消、公平让出和历史提交顺序保持。
技术续跑判据归入 `turn_end.py`，CLI 与主链直接引用，旧运行时实现及转发入口不保留。
执行相关定向 259 项通过，续跑/生命周期分组 79 项及独立导入 7 项通过；分组有重叠，不相加。
五路实际 TUI 使用官方 MiniMax-M2.7：Goal 暂停/压缩/重连后六条唯一记录合计 91，
后台进程停止后保留六条，续做十二条合计 546；数据统计和隔离通过。
子代理插话与返回正常，但一条 UTC 时间错误；另一路定时采样未满足间隔，清理曾缺版本参数。
失败原样留证，十一项测试定时任务最终通过 TUI 清理；不能把上述模型质量问题称为通过。

## 后台历史提交与结果交付边界

解决问题：结果投影、外发、历史落账、冻结重试散在调度器中，改一处难以核对完整事务顺序。
交付职责已归入 `background_delivery.py`，只依赖 Agent、唯一 store、通道及实时任务状态接口。
状态仍在原抑制位置读取，外发、canonical commentary/final、审计回执和冻结顺序保持；旧方法不留转发。
v1 冻结记录仍有持久数据合同，不当作废弃代码删除；清理无调用的参数/字段及重复事件判据。
相关联合定向 206 项、相邻分组 32 项通过；四路真实官方 TUI 已验证返回、停止续做和 Goal 压缩重连。
数值及恢复通过与模型的嵌套路径、时间戳和正文范围错误分开记账，详见真实矩阵。
累计变更触发全仓检查，首轮 22 项失败修正后为 17,070 passed、0 failed；既有 skip/xfail 保留。

## 续做归档与权威执行身份一致

解决问题：同任务新请求先创建了新 run 归档，随后复用旧 canonical run，导致身份检查拒绝收尾而残留 RUNNING。
现在在原换代锁内先绑定 run/attempt，再准备归档；request 仍保存新消息身份，归档和工具共用 canonical run。
目录准备异常只结算本次新 attempt，原 CAS、取消与未知副作用保护保持；已有证据和产物记录不丢失。
补齐夹具的显式本地模型后，原 DONE 断言继续保留，并核对同一 run 的新 attempt、manifest 与 workspace 身份。
联合定向 252 项及全仓通过；新版 TUI 的同任务主 run 唯一、各 attempt 收尾、公开结果无重复已核对。
模型在后台审批拒绝后直接补记录的样本未通过程序续做要求；这是独立失败，不能用运行状态完成冒充任务质量通过。

## 首次持续目标的工作目录

解决问题：TUI 第一条直接创建 Goal 时，控制载荷未携带当前项目目录，后台任务落到 owner 默认目录。
控制链现在保留 workspace 并共用普通请求的校验，空闲新目标只初始化尚无目录的线程；
已存在目标冲突或活动任务不会被重定向。目录纳入版本化回执摘要，不同目录不能复用同一控制消息编号。
355 项相关定向通过；真实 TUI 的线程、实际 pwd 和文件目录一致，定时采样完成；
启动后将空白目录换成外部符号链接，Gateway 在模型调用和 Goal 创建前拒绝，未产生外部文件。

## 仅思考响应的续跑保存

解决问题：一次成功响应生成约 3.7 万字符思考，无正文/工具时被当成空包丢弃；
零工具续跑也漏掉助手历史，下一轮无法看到刚生成内容。
适配器保留 typed 思考与用量；续跑前把原内容追加到既有 IR，未执行工具块排除，
普通工具轮/最终回复不双写。首轮仅思考复用原空正文预算，不引入无限重试或文本完成判定。
联合定向 455 passed、23 项既有 xfail；真实失败 SSE 复放逐字保留 37,118 字符与原用量。
新包正常 TUI 自然完成，8 项产物测试和两组独立复算通过；慢模型仍需验证，不等同永不复读。

## 渠道故障与业务失败分离

解决问题：测试失败或命令返回非零也累计成“工具系统失败”，错误引导模型换来源、再验证。
现只消费已有错误分类中的明确网络/能力不可用，精确调用身份去重，不改真实错误或权限。
参数、状态、审批、取消与未知失败不触发渠道提示。339 项相关定向通过，真实正常 TUI 66 组工具自然结束，
无错误渠道提示；8 项产物测试及原始/现有样例独立统计通过。模型仍有调整样例的低效绕路，慢模型循环项仍开放。

## 有界读取正文与合法长命令

解决问题：读取器已完成分页，生产归档又把整页换成日志预览，模型丢失尾部和继续游标；
合法 Shell 脚本另被人工字符上限提前拒绝，容易诱发反复读取、拆改命令。
现在仅真正外置且未声明保留的结果使用预览，内联正文仍走原脱敏与信任投影；
Shell 的 Schema 和执行校验同时取消人工长度门，原权限、安全、超时和输出上限不变。
旧错误码保留用于历史诊断，旧历史不猜测补写，缓存前缀不重排。
生产执行/归档回归已复现并通过；真实 TUI 保留完整文件尾部，并实际执行 8,468 字符命令。
正常模型连续两任务完成、独立 10 项测试与统计复算通过；慢模型仍语义重复，整项问题未关闭。

## 三种模型接口的显式温度

后台配置依赖已与普通冷却分开：缺模型保留任务、等待该会话模型配置，而非每 30 秒重复失败。
进程内退避集中在一个有界且线程安全的组件，后端与调度共用配置判据；不改 Goal、慢模型时限或模型输入。
联合定向 384 项通过；真实 Goal 缺配置等待后通过菜单改选，原编号自动续做并 complete，CSV 产物 16 项重跑通过。
另一路日志工具独立完成、16 项重跑通过；不把这些产物测试计入底座回归数量，不等同慢模型复读已解决。

解决问题：相同配置在不同接口产生不同采样，未设置温度的兼容模型被 Chat/Messages 隐式注入 0.2。
现在三种接口共用显式启用语义，模型级值、零值及摘要的单次覆盖保留，不热改冻结工作片。
不新增模型名单、重复调用硬停、历史删改或后台请求；真实循环是否改善按串行 TUI 证据另行验收。

执行事实投影已改为最近批次，删除原每轮重复近期账本的生成路径；实际工具结果与最终全轮核验保留。
本批状态包括批准、成功、失败和未知，批次只读结构化身份；不会从旧会话里删除历史或重建缓存前缀。
定向验证批次边界、无批次字段、并行有界省略与连续请求前缀逐字节保持；真实模型行为仍分开验收。

正常模型并行 TUI 已完成多子代理整合、追加任务、自动压缩、中断后继续，以及退出再恢复同一会话。
恢复保留历史、模型选择和累计用量。代理树的稳定进展软观察保留真实结果、排除计时与查询者自身活动；
追加任务没有高频轮询，但未触发重复阈值，不能因此断言模型已经学会换路。产物检查的遗漏另行记录。

## 中断与后台原生历史

进度部分更新已修复：只补备注或元数据不再重置 Todo/覆盖对象状态，新项仍默认待办。
显式更正、完成事实保护、ID 校验与展示代次不变；原错误账本不按模型叙述自动修补。
完成事实保护不再冻结 notes；已完成项后续补写验证备注和证据可以生效，状态/结果仍遵守显式更正。
回归覆盖各规范状态、三类更正标记、空状态与新增项，并核对模型回执和 TUI 投影一致。

主代理、子代理、单次 CLI 在异常退出前将已发生的工具事实交回原宿主保存；正常停止不发送伪造助手正文。
后台普通续做使用逐片宿主编号，将原生历史与公开 final 关联去重，投递被抑制也不丢执行事实；窄审计事件保持隔离。
前台沿既有幂等和延迟修复；未知工具结果不声称成功或已压缩，不自动重放副作用。旧版漏写和硬杀不保证恢复。
前台及子代理真实 TUI 保存与后续任务已验；后台 Goal 按 Esc 后自动续接并完成，前后两片 native 与公开 final 去重已验。
本轮当前联合定向 547 passed；旧数据和慢模型循环恢复的边界见当前状态页。
追加修复后台派工隐式继承宿主 backend 导致的模型错配，child 续做仍按自己的持久模型引用恢复。

## 账号认证与用量结算增量

受管后台命令复用原 wake 队列发送去重完成通知，长等待可取消；出站缓存诊断仅保存不可逆摘要。
实现及边界见 [长时间运行](design/LONG_RUNNING_EXECUTION.md)，真实组合验收与确定性回放分开记录。

Gateway TUI 客户端等待改为单调时钟，系统校时不再伪造超时或延长失联等待。
定向校时回归与正常模型七轮真实 TUI 注入均通过；提供方预算和任务终态规则不变。

成功重复提醒改为固定间隔，完整输出摘要不受随机归档引用影响；计数超过有界窗口仍有效。
后台进程查询另带宿主进展摘要，不把时长增长算作新工作；同尾日志增长和退出仍可区分，空等仅软提醒。
普通后台策展延后至同端点前台工作结束，不丢待处理历史；预算下传 HTTP、超时取消与禁止孤儿重试
已完成定向回归。真实模型是否采纳提醒另验，不用软提醒代替任务完成证据。

`/model` 可管理订阅设备码及通用 OAuth 登录参数、取消、退出和刷新；凭据只在 owner 私有 provider，
不能跨用户共享，不读其他应用账号，既有 Chat/Messages/Responses 模型循环及 Compact 复用。
代码实现与替身测试不等于真实账号授权通过，用户确认和实际短调用的边界见 [账号登录](design/MODEL_OAUTH.md)。

已修成功/异常/中断结算不一致，以及来源切换重复计累计快照。容器代次区分跨进程重新累计，
来源不再作为新计费范围，旧账不静默重写。真实 TUI 中断后追加与两子代理 Goal/后台/压缩已对账。
单槽慢模型新增显式每模型排队预算，默认不变，只影响首事件；schema 编译和长思考输出耗尽仍未因此解决。

## 空白输入与主目标恢复

空白输入与主 Goal 控制已完成真实 TUI 增量验收：纯空格补全不越界，Esc 中断本轮但不暂停活动目标；明确暂停与恢复独立。
后台插话从原邮箱进入精确模型回合，回执消费、用户历史和公开回答一致；Goal 编辑放弃不写入，保存与中断不混淆。
恢复时总账重开并保留旧关闭事件；先后完成的历史目标不误判成并行冲突。最终目标、执行轮和总账均完成。
相关定向 607 项通过，6 项原有预期失败未计入通过；小时级慢模型、孙级及 IM 组合仍见清单。

### Goal 子代理回报的输入身份

六路真实验收复现：全新 Goal 派出子代理后，生命周期信封用持久 task 编号标识输入，
主代理却把它当作普通用户消息编号查询，导致孩子全部完成后仍不能整合。
现在先核对精确线程与 Goal 任务归属；普通请求仍逐个检查，缺失和损坏不回退、不伪造消息。
部署后原现场沿原五个子代理结果恢复并完成；同会话追加目标又创建五个孩子，自动接回后运行 38 项测试通过。
另一个目标暂停问答、恢复后运行 30 项测试通过，Goal、attempt 与 TaskRun 终态一致。
上述只是运行链路及所见测试输出的证据；绝对路径回答和部分产物边界仍有模型质量问题。

## 追加消息与工具执行身份

解决问题：等孩子时追问能收到回复，接着读取和命令却报执行身份不存在。
原主入口只回填新 attempt，临时消息编号仍冒充持久 run。现成对回填 run/attempt，request 保留消息身份；
恢复凭据和 Compact 再入不混用。真实工具门回归覆盖允许路径、旧代次与错任务拒绝；不增加别名或第二棵执行树。
原失败会话恢复后运行 14 项测试通过；另一真实会话等待两个孩子时追问并执行命令成功，孩子继续运行。
两名孩子自然结束后主代理自动接回、压缩及整合，Goal/attempt/TaskRun 最终分别 complete/done/done。
用量结算、慢模型兼容与成品质量不因此自动关闭。

## 重复工具调用的观测与换路

解决问题：相同成功调用被拒后，下次又放行；拒绝回执缺少计数及恢复说明，模型继续盲重试。
门自身的未执行拒绝不再清空、累计或挤掉有界观测；完整回执仍归档并进入模型。
原生消息和文本工具结果携带同一说明；真实写入后仍可重新检查，零阈值与未知副作用规则不变。
非文本读取给出实际能力发现途径，不自动转图或调用额外视觉服务。
定向覆盖 300 次拒绝、真实 Shell 经 Registry/Executor 的拦截和写入恢复，以及 canonical 归档到 native 消息。
新包真实 TUI 完成动画任务和同会话追加控制，首任务 4 模型轮/3 工具，追加 12 模型轮/12 工具，
一次补丁参数失败后模型自行换路恢复；无重复门触发，故只计正常链路验收，不冒充真实模型循环换路已验证。
产物未满足最初 SVG 实现要求，浏览器视觉也未验证，保留为内容质量缺口；不能将自然结束等同成品通过。
另修正一条旧回归的模型选择准备：无会话的测试使用 set_default，不绕过会话级 select 的身份要求。

## 紧凑状态与结果引用

解决问题：模型查询得到巨大界面快照，关键状态被裁剪，还被内部恢复路径引导到拒绝读取的循环。
现从同一授权快照生成紧凑状态和真实 read_order；内部摘要不作为结果，已存在终态报告可供无产物的任务读取。
大结果按原工具归档保存，模型摘要保持有效 JSON、原始路径与明确省略计数。查询不额外要求父级结束回合。
153 项相关定向回归通过；包含更新一条落后于独立子目标字段的旧 schema 顺序断言。
双真实 TUI 的完成后查询和读取已通过：会话不串其它窗口；主请求作为根时能查到真实孩子；
只读协作者没有写文件时，现有终态报告也进入可读索引。中长任务正常收尾，分工质量另评。

## 长思考实时折叠提示

预览超过字符或行数预算后，显示实际折叠行数及已接收字符数，不再只有固定提示。
81 项界面相关定向通过；真实 TUI 连续任务捕获 1,714→6,445 字符增长，跨过 4,000 字符上限仍刷新。
无换行长文本、窄终端和终态收起均有回归；原文和模型上下文不变。

## 模型统计条

Context 下方新增本次逻辑模型轮、当轮工具数、最近缓存比例、当前代理会话累计消耗；宽屏补充输出速度与输入/输出分项。
统计使用调用账与会话账，主子代理分别展示，未知和估算有明确标记，显示字段不进入模型上下文。
真实单 Gateway 双 TUI 覆盖连续任务、子代理切页、父级整合、手动压缩和压缩后继续任务；界面累计与持久用量逐笔相符。
测试发现独立压缩未落会话账，已在辅助调用收口中补齐，绑定普通请求的压缩不另记，避免重复收费。
旧版本漏记的费用不从上下文长度反推；小时级极慢模型与多次自动压缩组合仍见验收清单。

## 软件不预选模型

解决问题：预填模型名、另一份启动模型投影和后台配置失效后的回退，容易误导当前模型身份。
模型协议、名称和端点默认留空；未配置适配器只允许进入设置，不发送模型请求。
状态工具只保留当前调用配置，后台整理记忆不因失效引用而改用启动模型。用户自己保存的选择不变。
定向回归与单 Gateway 双用户 TUI 已验证空配置菜单、明确失败以及显式选定模型的真实调用；
这不保证模型口头自报永远正确，实际模型仍须以请求配置和调用事实核对。本机在用会话未重启。

## Goal 续跑承诺与执行范围

解决问题：回合参数里残留 Goal 编号时，工具轮限交接仍可能承诺继续；系统持续执行规则又把独立后台目标交回当前回合。
续跑承诺现在重读精确 thread/task/goal 的持久状态，仅 active 且归属一致才有效；完成、暂停、删除、绑定冲突或读取失败均不承诺继续。
系统规则区分本轮 Goal 与普通子代理；每代理单目标规则已替代早期的多个命名目标并行执行方案。
生命周期状态与请求配置定向测试通过；这只证明上述确定性缺陷得到修正，不代表并行分工已验收。
新包真实 TUI 已完成单目标审查、目标展示、最终收尾与后续普通追问；后续消息没有重开已完成目标。
真实用例未触发工具轮限分支，该分支仍属于定向验证范围。
真实多目标 TUI 仍出现重复核验、重复派工，保留为开放问题，不以新增提示、目录锁或机器质量门代替修复。

## 每代理单目标与目标编辑

解决问题：多个并行目标归属混淆、子代理没有独立目标，以及用户无法直接纠正目标。
每个代理线程最多一个未结束 Goal；主子各自计费、续接、停止，prompt-only 派工不自动继承父目标，Todo 可选。
TUI 方向键选中目标后 Enter 编辑，Ctrl+S 保存、Ctrl+G 放弃；主代理 Esc 中断本轮，active Goal 保留；子代理沿原停止语义。草稿不随 Esc 保存。
保存按内容版本比较，不被用量刷新干扰；后台修改或状态变化导致冲突时保留草稿，暂停编辑不恢复执行。
真实 TUI 验证父子创建、保存、放弃、停止及新要求被消费；同时修复父子邮箱串读与编辑快捷键冲突。
孙级、多次压缩与小时级慢模型组合仍须另验，详见 [持续目标](design/THREAD_GOAL_LIFECYCLE.md)。

## 父子并行与阶段诊断

已删除成功通知的全树终态屏障、创建子孙后的强制让出与相应废弃计数字段。
新结果逐项交给直属父级，忙父级复用安全点，空闲父级复用原调度车道；等待释放与迟到结果按结构化快照处理。
长等待提醒复用 runner 心跳，按首 token、流静默、工具、审批与退避区分；去重提醒不改状态，不自动强杀或重派。
定向验证与四路真实 TUI 已完成：先接入快结果、独立整合、低阈值提醒送达后原 attempt 自然完成。
测试阈值已恢复，孙级链路补验与分工质量、故障组合分别记录，见 [并行执行](design/SUBAGENT_PARALLEL_EXECUTION.md)。

## 子代理文件交接与工作目录

自然最终回复与结构化结果共用本 run 工具产物账本，父级不再因未收到模型 JSON 而拿到空文件清单。
投影保留精确身份、最新文件记录和读取诊断，排除其它 run、工具输出归档、删除及缺失记录；不代写文件。
多文件补丁把实际提交目标交给同一工具账本，移动/删除保留墓碑，部分失败不登记尚未提交目标。
删除内部输出重定位、交付映射和完成时自动复制。相对声明使用可信 cwd，绝对目标不变，声明不扩权。
缺少 cwd 时不猜宿主目录，也不据此剔除文件锁。现有权限、正式授权和恢复记录保留。
定向验证覆盖自然/结构化结果、深度一/二、记录替换、补丁部分提交及 no-follow；真实 TUI 已核对
孙级交接、同会话追加和补丁登记后的父级验证。尚存模型行为问题与覆盖边界见 STATUS。

## 派工说明去冲突

派工快照只保留各 run 的运行、终态与故障事实，删除“有孩子运行就等待”的嵌套动作建议。
协调者只加载自己的冻结角色与简短角色索引，删除全部外包、永久禁止接手和亲手重写下级成果等旧规则。
根与递归协调者共用自身分工、按需等待和无冲突整合的软说明，不增加权限、目录锁或机器质量门。
定向套件 312 项，308 项成功、4 项已有 xfail。单 Gateway 三路真实 TUI 均已结束：
独立工作时父级确实先写自己的模块；依赖任务在孩子交回后自动继续；追加任务有真实协调者及两名孙代理。
后者仍发生转交父级职责和重复状态查询，第一次分层需求也被扁平化，均作为模型行为偏差保留，不能称效率全面通过。

## 执行器退出与恢复公平性

解决问题：子代理执行器已经退出却继续显示运行中；收口失败积压时旧记录恢复不到。
真实执行区间在 exact attempt 下登记，异常退出复用正式结果与父级通知链；未知副作用保留封存，
不自动重跑。恢复事件按未消费身份分页轮转，扫描游标与消费回执分离。
定向回归覆盖进程退出、同宿主执行区间退出、慢执行存活、换代、未知工具、121 条跨页及回执去重。
真实 TUI 及故障注入状态见 TEST_CHECKLIST，不以定向通过冒充全面验收。

## 发布资料整理

解决问题：旧对照报告、机器现场记录和已失效的方案混在现行文档中，容易误导使用者及后续开发。
已清除过时资料，统一产品说明、开发入口与测试分层；示例匿名化，许可证和运行标识保留。
生产代码以 AST、配置值及打包内容比对验证；两端新安装的 TUI 分别验证启动/模型菜单与真实工具调用。
这些验证不替代长会话、多子代理与故障恢复的后续验收。
