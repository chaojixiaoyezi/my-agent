# 后续开发与验证

## 本次独立重构 Goal 已完成

四批实现与本机验收已完成，保持原 canonical 状态源、锁、CAS、权限与事务顺序：

- 单工作片执行与后台调度分离，上下文及原生历史准备显式传入。
- 历史提交与结果投递独立，保留冻结重试、原身份及耐久去重。
- Gateway 的上下文、绑定、历史、输入与流式输出按职责归位，前后台共用纯计算。
- Store 改为领域组合，聚合入口 134 行、无领域继承；插话账本、提交、确认与恢复分别持有明确依赖。

已删除核实无调用的旧入口与兼容转发。结构迁移候选全仓 17,116 项通过；后续控制修正分别定向验证。
当前控制明确分为 Goal 暂停、回合中断、任务资源停止；旧“Goal 暂停后静止”记录不作为新语义验收。
后台终端目录和主代理审批缺口已修复，审批末片 428 项定向及真实 TUI 通过。
新 claim 能读取原 PTY，并在正常审批后输入；原程序只启动一次，报告与全部 30 项输出一致。
拒绝审批不会执行 handler 或产生替代运行。每批真实验收均通过官方 MiniMax-M2.7 的实际 TUI，共用单 Gateway。

本地风险复核及提交整理已完成，后端、桌面文本输入、会话运行与存储集成分别提交。
探针诊断与实际传输已共用地址计算，修复重复版本和完整接口后缀；228 项定向及新版官方 MiniMax-M2.7 真实 TUI 工具链已验。
正常等待误带 `runner_error` 已按结构化结束原因修复，237 项定向通过、1 项既有 xfail；新版实际 TUI 已验递归等待/唤醒/完成及明确取消。
后续先核对真实递归采样报告与最终 CSV 时间不一致的读写和交接时序；保留分工偏差与报告错误，不用状态正常替代交付质量。
需要并行时按模块分工，跨模块共享文件明确写入负责人；权限、持久格式和取消语义保持原边界。
最终组合全仓首轮只有一处旧控制文案断言失败，修正后 222 项相关回归通过；完整计数及验收边界见 STATUS。
历史模型报告错误、Audit 未发布结构化来源及未覆盖场景继续见 [STATUS](../STATUS.md)，不能改记为通过。
auth 与 Jev 属于后续独立范围，不包含在此 Goal。

“拆清依赖 + 可装卸插件”共用一份十步计划：1 明确依赖与边界 → 2 后台调度拆分 →
3 公共命令声明/解析 → 4 最小插件装卸 → 5 TUI 发现/使用 → 6 并发长任务与故障验收 →
7 子代理生命周期/交接 → 8 模型工具循环 → 9 剩余 TUI → 10 约 10 个简易插件与组合验收。
Audit/摄取重构已移出本轮；现有功能和历史记录保留。新增样本环节借鉴社区功能，自行实现 Python 插件，不安装 DSH 包。
后台进度策略纯计算与供应退避状态已独立并部署；配置组装及实例寿命仍归原调度器，安装版实际冷却和续接已验。唤醒/Goal 路由已分离并同版部署，353 项相关回归和 TUI 120—123 所测框架链路通过；模型交付失败与未覆盖支路单列。
租约/恢复片已完成迁移、严格发布 gate 和同版部署，324 项定向回归与独立复核通过，覆盖领取后竞态与运行中续租；共享心跳不复制，三类租约不合并。真实 TUI 124 的运行中续租、128 的同车道排队、130 的原请求有序恢复已验。既有 STABLE 资源换代误标脏问题也已修复、通过 186 项相关回归并同包部署；133 实证取消后续代保留原状态、复用终端并自然退出。第 2 步本轮框架范围收口，覆盖限制与模型交付失败见 TESTS；第 3 步公共声明与命名空间首片已同包部署；实际 TUI 137 暴露的后台资源停止缺口已修复并由新版分项复验，参数次片也已发布部署，动态目录与宿主身份接线仍待做。
当前基线已发布 main 并完成本机/测试机同版安装；用户确认框架功能与模型交付质量分开记录，第 1 步框架基线收口，原合并内容与报告计数失败仍保留。
长任务发现子代理父会话关联导致具体审批丢失，原归属读取已修并同版部署；实际子/孙审批与原调用接续已验，报告质量仍未通过。
第 1 步依赖与副作用清单已记录，真实子代理拒绝未执行已验；框架检查与交付检查各自留证，缺陷和必测覆盖缺失仍阻断对应结构迁移。
插件接线补充核对了现有 handler 快照、共享 MCP 连接和 Skill 文件身份；17 项既有合同回归通过。后续撤销须跨权限视图，并与调用准入/资源登记保持同一边界，详见插件方案；能力仍待实施。
同一子代理 Goal 续轮重复询问拒绝调用的修复已发布部署，TUI 70 同参拒绝通过；后续嵌套 Shell 后台执行及超时遗留进程已发布部署，严格 gate 与 TUI 78/79 复现通过。
TUI 75 普通文件对照通过；十五分钟 TUI 72 的生成时钟字段、间隔与报告失败仍保留。
真实工具账又定位到 Shell 末尾换行多计一行，已统一模型回执/展示/截断说明的行数口径并同版部署；118 项定向、本地严格 gate 及 TUI 80/82 的工具事实复验通过。
TUI 81 的平方表数据与报告一致；80 漏 stderr、82 误解行数、83 报告含表头文字写错仍保留，不以短任务关闭采样质量失败。
前版长任务 TUI 84/85 复验未通过：生成程序向关闭文件写入、删除原记录或误判退出原因；实际退出回执和原生工具账已归类，普通对照 TUI 86 通过。
父子已有同作用域的进程查询入口；未发现阻断查询的框架证据，不凭错误报告增加生命周期判定，详细事实见执行 Goal。
继续核对后定位到完成事件的窗口字段在模型投影中丢失，已补齐中性合同、活动回合与后台预算消费并同版部署；446 项回归通过、4 项既有 xfail。
实际 TUI 89 的后台上下文在摘要裁剪后保留原窗口数值及 DONE；四路新版任务的完整结果见执行 Goal，不以字段传递通过关闭模型履约失败。
已删除无生产调用的旧 verifier integrity 模块和自造字典测试，真实验证入口与矩阵边界已明确，22 项相关定向通过。
第 1 步又删除已停用结果块协议的诊断场景及专用替身，同步脚本、帮助和文件职责；相邻 runner 重试逻辑保持原样，已有 xfail 未消除。清理已发布并同包部署双机，实际 TUI 91—94 的单子代理与普通文件短回归通过，长任务问题仍保留。
每步多 TUI 的详细验收、长任务和推进条件见 [执行 Goal](tasks/REFACTOR_PLUGIN_GOAL.md)。
资源停止修复的 v2 存储合同已在源码落地：原 session 地址上完成目录互斥、版本 CAS、固定批次提交与恢复。
源码已接 host 启动预留/明确交接、冷热权威刷新与精确单 session 清理；删除旧启动后登记及 launcher 长期 watchdog。
共用执行权取消与 Gateway 持久主任务冻结已在源码接入；UNKNOWN 保留锁，旧工作片不能因 Goal 恢复重获执行权，部分清理结果明确未确认。
direct/local 与无持久任务链接热请求的主链已接入源码：真实身份由运行器发布，消息编号不代替 run/attempt，旧回调不覆盖新消息；不可确认或已晋升状态不降级清理。
整树停止已进入源码：主/子同一控制边界关闭权限并固定资源，异步不再发现后代；终态业务结果保留。独立 runner 由原心跳转交原执行轮取消，宿主只读核对。
上述创建、换轮、插话预留和授权恢复的协调基础已进入源码，旧 abandon 改为最新状态窄 mutation；插话启动在树锁外。
准确 pending 身份现已沿 managed 后台/CLI/并行 runner/插话启动运输，并在 DB 原子激活处核对；旧 launch 条件更新与 session 代次守门已接入源码。
pending 插话重放已按原 mailbox 锁内最新状态和准确 DB 预留收口到源码；无数据库迟到启动已补非空身份与一次性消费，保持详情取消的既有恢复资格。累计源码全仓与严格 gate 已通过，已发布同版双机；TUI 138—142 分项验中断保留、主资源、并行孩子、PTY、孙代理与独立会话隔离。
所测修复范围通过，TUI 137 历史失败保持。Working 图标的零散反馈按用户要求延后至当前目标完成后排查。
四组按用户授权并行执行，真实模型和文件工具均已连通；my-agent 的 M2.7/Flash 同题短任务已通过。
[四组同题对照](../TESTS.md#模型与框架对照) 本轮已收口：Flash 核心十五分钟采样通过、报告覆盖有保留项；M2.7 在三个框架的长题均未全过，全部实际失败及控制动作保留。
区分框架、协议适配、模型行为和测试准备错误；Goal 当前 active，第 2 步供应退避与路由切片所测实际链路已验，租约/恢复及资源换代修复已同版部署，133 最终任务和资源已收尾。TUI 120 的统计/单位、121 的校验器/报告和 122 的读取顺序错误均保留；已核实的模型错误不要求重跑到全部答对，原整轮失败不改为通过。
插件解决可选能力难卸载、依赖拖累核心和频繁重启问题；停用不进入运行/模型目录，失败及卸载只影响其精确归属，
详细范围和验收见 [可装卸插件方案](design/PLUGIN_LIFECYCLE.md)，不把新能力混入行为等价的代码搬迁。
首批插件在第 3—6 步整体交付：先本地包和只读 Python 示例，包含使用卡、动态发现、共用调用链及故障装卸；目标为自有 Python 插件，不是接入 DSH 社区包。
依赖隔离与撤销必须首批实现；在线来源安装、更新/回退等另排后续小批，尚未实现的动作不展示为可用。
首个只读样本优先使用 workspace-peek，纳入后续 10 个包；具体来源、热度快照和功能范围见 [插件样本验收计划](design/PLUGIN_SAMPLE_ACCEPTANCE.md)。
第 9 步提供声明式展示及可撤销的只读订阅，第 10 步按三批实现并经实际 TUI 验收，最后核对组合使用及全部卸载后的核心基线。
按 `/plugins` 管理、`/plugins@插件ID` 调用的命名空间推进；先打通参数声明、公共解析与逐级补全，不新增分散的顶层插件命令。
报告与 CSV 不一致的证据核对可同步进行，确证的行为缺陷单独修复，不混入迁移；详见
[下一轮结构整理顺序](design/MAINTAINABILITY_AND_JEV_REVIEW.md#下一轮结构整理顺序待实施)。

## 本轮已验

宿主命令目录与版本绑定已接本地源码，669 项定向通过、1 项跳过，尚未计作发布或实际 TUI 通过。
它解决前端旧命令与宿主新声明含义错配的问题；只读目录沿原 owner，缓存不授予安装或执行权限。
下一步先用实际 TUI 验宿主读取、Tab/Enter、活动任务隔离与普通工具链，再收口第 3 步；第 4 步装卸尚未进入。

资源停止修复的源码已统一 PTY 与后台的访问/执行身份，删除旧定义；direct/local 单纯中断不再派发子代理取消或清退插话。
后台持久归属、启动交接、精确停止和整任务固定子树已接入原运行链；插话重放与文件模式的一次性启动均已补源码，旧快照与二次停止有定向回归。
已发布同版部署，TUI 138—142 所测停止链路通过，TUI 137 原失败不改写。
第 3 步参数与补全修复已发布同版双机；483 项相关回归及严格 gate 通过，实际 TUI 146 的 Tab→Enter 和两端各 13 类静态命令检查通过，143 原失败保留。
管理帮助可用；业务装卸未开放，动态 owner 目录和执行权限尚待接线，整步未收口。
150 的活动请求中静态命令没有进入模型插话或 Shell，原 attempt 自然完成；普通任务及校验器质量限制分列。
模型交付错误及未实测旁支见 TESTS，不提前进入插件装卸。

- **后端与会话首批重构**：后端公共合同、HTTP、Chat、Messages、工厂分别归位，后台工具策略独立。
  后续批次已分离后台上下文、原生历史种子和结果交付，后台返回、Goal 压缩重连与恢复已验。
  全仓检查发现的旧夹具与宽泛接口已清理；同任务续做的归档身份顺序缺陷已修，全仓复查 17,070 passed、0 failed。
  新版 TUI 身份与返回正常；程序续做被模型改为直接补文件、部分采样间隔不符，按失败保留。
  单片执行改为显式依赖和具名结果，技术续跑判据归入公共结束协议；本批真实暂停/压缩/重连、
  后台进程停止续做及数据隔离已验。子任务时间错误和定时采样质量问题保留，详见矩阵。
  首次 `/goal` 丢工作目录已沿持久控制回执修复；真实 TUI 的目录执行和符号链接越界拒绝均已通过。
  真实官方 MiniMax-M2.7 多 TUI 验收发现后台状态地址、PTY 和桌面输入问题，通用修复后已分别复测。
  取消恢复、重连、数据分析、并行子代理和本机中英文输入已留证；收尾双 TUI 重试仍被设备码端点拒绝，完整认证验收受阻。
  当前证据与后续拆分顺序见 [可维护性评估](design/MAINTAINABILITY_AND_JEV_REVIEW.md)。

- 文件有界正文二次裁剪及 Shell 人工长度门已修：生产归档到原生请求保留尾部/游标，
  真实正常 TUI 成功执行 8,468 字符命令；连续两任务收尾，独立 10 项产物测试及统计复算通过。
  单路慢模型两轮分别执行 54 / 33 组工具后仍未交付，已中断保留；不能关闭慢模型复读项。

- 部分更新、旧计划续写、完成备注、真实路径展示和 Gateway 重启回执已完成定向与真实 TUI 验收。
  多子代理连续任务独立 34 项通过，同题日志工具独立统计复算通过；模型自写文档仍可能出现错误路径。
  慢模型串行对照未通过长任务交付，详见当前状态；不以高缓存或界面可操作宣称完成。

- 中断/异常保存已发生的工具历史：前台与子代理真实 TUI 分别保留 5 / 10 组往返，慢模型单路保留 7 组。
  后台普通续做正常、取消、异常、静默与投递重试定向通过；真实 Goal 中断保留 13 组，自动续做后保留 21 组并汇报。
  Compact 与公开 final 不重复回放 native；旧版丢失记录不倒推伪造。

- 长等待取消、完成通知耐久去重、跨会话请求摘要定向通过；正常多 TUI 主链通过，慢模型长任务复读未通过。
  按 [长时间运行矩阵](design/LONG_RUNNING_EXECUTION.md) 收口，不把短任务当无限运行保证。

- **输入与 Goal 中断（主代理路径）**：空格补全、编辑放弃/保存、Esc 自动续接、后台消息真实消费及回复、暂停问答后恢复已验。
  顺序目标不再误判为并行冲突；最终 Goal complete、attempt done、TaskRun done 已交叉核对。慢模型/孙级/IM 组合仍另验。

## 优先修复

- **仅思考续跑漏历史（已修，真实复验中）**：真实停止响应约 3.7 万字符思考被空响应重试丢弃。
  Chat/Messages 已保留合法原生内容和用量，零工具续跑也逐条追加 IR；原预算与未执行工具边界不变。
  慢模型单次生成自身复读和整体任务完成另验，不能用此项定向通过关闭整项循环问题。

- **进度补丁误重置（已修、真实续做已验）**：只更新备注/元数据不再把 Todo 或覆盖目标重置为 pending；
  更正开关也不补造状态，新建默认和明确状态更新保留。旧数据不按文本倒推，模型换路效果另行验证。

- **等待子代理时追问的工具身份（已修、真实复验通过）**：真实消息编号被当成 run，读取和命令都被权威门拒绝。
  已在主入口同时回填真实 run/attempt，消息 request 保持不变；原会话恢复后 14 项测试通过，
  另一会话等待两名孩子时插话能实际执行命令，孩子仍继续；权限门不放宽。

- **用量异常收口与前后台去重（已修、真实增量验收通过）**：统一成功/错误/取消结算；source 不再形成重复收费边界，
  累计容器用代次区分重启。正常 TUI 中断与追加、多子代理 Goal 插话/后台整合/Compact 已对账；旧源账未篡改。
- **账号登录（已实现、真实账号授权待验）**：订阅登录与通用设备码 OAuth 参数、退出、刷新和取消。
  真实 TUI 与服务商账号权益分别验收，不把本地替身协议测试称为 Plus/Pro 账号通过。
- **慢模型失败分层**：本地工具语法编译 400、单槽排队首事件超时和多工具流不完整分别取证；
  每模型已增加显式额外排队预算，真实慢环境仍须复验；它不解决另外两类问题。
  后台 Goal 必须直接展示脱敏失败原因。健康流 45 分钟不中断不代表任务可用。

- **成功调用复读（观测链已修，慢模型长任务恢复未通过）**：重复门自身拒绝不再清空或挤掉原观测；
  新增固定间隔成功提醒、跨有界窗口计数与完整原始结果摘要；慢模型仍需真实验证是否换路。
  后台策展同端点前台让行、预算传递和超时取消已完成定向回归，外部应用缓存争用不在此保证范围。
  模型原回执保留计数与换路建议，文本读取失败说明能力边界。确定性长序列、真实工具和 TUI 分开验收，
  两个连续真实 TUI 任务正常收尾，但未触发循环门；原在用慢模型未重启，不把任务完成说成所有模型退化已解决。
  新慢模型任务已实测同一成功 Shell 命令 9 次，随后才承认文件不完整，完整产物验收未通过。
  恢复现场新增：受管进程无输出时重复 wait，uptime 变化使整包哈希持续变化；稳定进展与显示计时已分开并通过回放，
  不能因此强杀仍存活的下载、引入任务完成门或无限重试。另已补修 TUI 系统校时导致的假超时，真实注入复验通过。
  新包正常/慢模型 TUI 均自然完成，普通后台与前台可交错推进；未触发阈值，不把它算作循环恢复验收。
  本轮更长的单路慢模型复验出现 24 次相同 Shell 调用，已中断并保留失败现场；近期缓存约 98% 不代表有效推进。
  24 轮确定性回放核对了记录、Native IR 和 OpenAI 出站转换中的分次提示，但该现场没有完整出站请求，
  不据此断言具体失败全由模型忽略提示造成；继续按请求链取证，不以硬停任务或修改产物伪造通过。
  补齐中断历史后单路新样本仍重复 7 次；原生历史可见第 5 次换路提醒，其后再重复 2 次。
  这证明不是仅修历史就能解决全部复读，后续仍需区分提供方请求与生成行为。
  串行抓包确认旧现场工具调用/结果成对、换路提示已出站；发现 Chat/Messages 在未显式配置时仍发送 0.2。
  已统一显式温度语义，同一慢端点串行 TUI 对照后仍未收敛；不把请求修复等同模型循环完全解决。
  干净会话工具回执、写入内容、cwd 和进度协议已核对，须继续区分模型生成退化、模板与输入影响。
  已补修读取结果二次截短及无效长度门，干净慢会话仍语义重复；调用/结果完整配对。
  同一 68,214 token 输入分别缓存复用和完整重算，均继续读取已读文件；冷算约 356 秒，
  不通过关闭缓存解决。用户开启思考后仍重复读取未交付；思考生成/保留均已核对，不视为整体修复。
  新发现的渠道失败提示误分类已修，339 项相关定向通过；真实新包正常 TUI 66 组工具自然结束，
  无错误渠道提示，8 项测试及原始/现有样例独立复算通过。采样对照也未使慢模型交付，精度影响仍待验证。

- **旧后台唤醒的不可恢复失败（已修、真实 TUI 已验）**：缺模型不再按固定时间重复执行；
  等当前会话模型配置恢复后消费原持久唤醒。普通错误仍冷却，健康会话不阻塞，不自动选默认模型。
  代码清理先独立进程内退避，统一判据；其余大模块按 [体检计划](../DESIGN_LEDGER.md#代码体检与后续拆分计划) 分批处理。
  原 Goal 通过菜单选择模型后自动完成 CSV 工具，另一窗口正常任务独立完成；两个产物各 16 项测试重跑通过。

1. **无结果执行器退出**：修复已落地，正在做真实 TUI 验收；慢模型仍活着时继续运行，未知副作用不自动重跑。
2. **收口恢复公平性**：未消费分页与持久游标已落地，跨页/重启/去重定向回归已过，待真实故障复验。
3. **长会话核算**：请求输入、缓存读写、展示估算与累计消耗分口径记录，验证压缩和跨任务连续记忆。
   模型统计条及独立压缩入账已完成定向回归和双 TUI 连续任务、子代理、手动压缩、追加任务对账；更长组合另验。
4. **持续目标与错误诊断**：默认工具可见、零工具安全续跑、精确旧冲突恢复与前后台共享计时已修复；
   每代理单未结束 Goal、独立子目标、可选 Todo 和用户草稿编辑已落地并完成真实 TUI 主流程验收；
   父子邮箱串读及编辑快捷键冲突已修复。旧多目标记录不自动迁移，孙级与慢模型组合仍待专项验收。
   单目标和普通聊天对照、目标完成账已核对。多目标仍重复工作，不能靠完成回执补充说明就判修好；
   已修 Goal 提前关闭执行任务，真实 TUI 已完成子代理继续运行、回报和最终关闭交接。
   当前任务被 Goal 名称误转为后台，导致后续消息失去子代理父级；已修复并通过真实插话、子代理消费、最终汇总验收。
   新修轮限交接只凭旧 Goal 编号承诺续跑的问题；定向已核对当前状态、删除和外任务绑定。
   已消除系统持续执行规则与 Goal 执行归属的矛盾，但两组真实任务仍重复核验或重复派工，分工问题不能关闭。
   真实模型仍可能自行填写未要求的时限，需明确预算控制边界。保留 HTTP/协议/流式终止原因，不把所有错误归结为配置。
5. **父子并行效率（主链已落地，组合待补验）**：取消成功通知的全树等待屏障，创建子孙代理后继续当前工具循环，
   模型自然让出时保留直属等待；逐项结果进入原事件通道，父级仍只有一个执行者。
   活动异常只按模型/工具/exact attempt 事实诊断，不按静默时间杀慢模型。定向验证与四路真实 TUI 已完成，
   覆盖快结果先接入、父级独立工作、低阈值提醒投递后孩子自然完成；临时阈值已撤销。
   补验已完成真实统筹、两名孙代理与根代理的整条结果链；根模型仍可能主动等待，分工质量不能据此关闭。
   已修复自然 final 丢失工具产物引用、内部输出重定位及收口复制，父级使用 exact run 账本中的真实路径。
   追加实测发现补丁文件漏登记，已修并核对新包补丁回执、child 文件账与父级实际验证。
   输出声明不增加权限，缺少 cwd 不猜宿主目录；故障组合和长尾成本仍需另验。
   已清除状态快照与创建建议相互矛盾的等待指令、协调者全部外包/永久禁接手、亲手重写下级成果规则。
   协调者只加载自身冻结角色，定向及三路 TUI 已核对独立工作、孙级交接与自然等待；分工选择仍有偏差。
   状态查询过大、内部恢复路径误导及完成后会话范围已修，双 TUI 已验证精确树与真实文件/终态报告读取。
   长思考折叠计数真实流式更新已验；慢模型单槽位排队超时与推理耗尽输出预算仍待分别治理。
   方案见 [并行执行](design/SUBAGENT_PARALLEL_EXECUTION.md)。

发布资料清理已落地，含文档断链与旧状态说明整理；不宣称以上运行时问题已修复，当前证据边界见 [STATUS](../STATUS.md)。

## 下一轮验收

- 多用户、多 TUI、单 Gateway：不同模型、文件归属、记忆与技能调用、交叉并发。
- 连续任务：长调研、多子代理、插话、后续修改、模型切换、压缩和恢复。
- 慢模型：长首 token 等待、持续低速输出、断线重连；并行测试滚动、复制、输入和停止响应。
- 生命周期：子代理正常完成、失败、取消、执行器退出、结果写回失败、父级通知重投。
- 交付：代码实际运行、端口绑定与局域网可达；模型说已启动不等于验证通过。
- 需环境项：IM 渠道、真实桌面控制、企业规模存储和灾备按可用环境另列，不计为通过。

## 开发方式

- **可维护性与可选判断工具（四批重构已验，Jev 待定）**：解决核心文件职责集中、隐式依赖导致的修改成本。
  后端协议、会话执行/调度、Gateway 适配和存储组合已按批完成；后续 Computer Use 仍须核对对应部署条件，
  Jev 先做只读候选筛选对照。详见 [源码评估与实施顺序](design/MAINTAINABILITY_AND_JEV_REVIEW.md)。
  不把结构整理、行为修复和新模型自动路由混在同一批，不把历史桌面验收当当前主机通过。

每次按模块提交，定向回归先于真实 TUI；共享代码入口只由一个实施者修改。独立测试可并行，但不能为加速创建额外 Gateway。测试、开放问题和修复影响一起记录。
