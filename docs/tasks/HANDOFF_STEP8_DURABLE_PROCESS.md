# 第8步耐久事实与精确来源集成交接

## 范围及状态

本地组合线在 closeout／并发段基础上集成精确 refs 提交；生产按来源、进程投影分别负责，其他 Jev 工作树未合入。未发布、未切换 Gateway、未运行真实模型。

## 实现

- 纯 runtime_facts 从 agent_core 下移 tooling，旧文件删除，不保留转发。当前回复、归档、磁盘索引与恢复共用同一有界 process 字段。
- externalizer 索引可选 tool_process；carried 恢复既有信封。旧缺字段不制造成功，不复制 PID、原始诊断或工具正文；原调用身份、未知副作用保持。
- 精确 refs 按原调用身份，不靠当前 runner 补身份。独立审阅发现未知尾部 null refs 的 ID 碰撞，候选 ID 增加逐位 retained IDs。原无 refs ID 完全保持，原锁／CAS／停止顺序不变。

## 验证

磁盘恢复2项红灯→修复，四文件106项通过。48文件组合1212 passed／24既有xfail。末审未知尾部碰撞1项红灯→修复，最终相关四文件159项通过。原来源片独立336项不累计为组合结果。严格 gate 结果见本轮 TESTS 及私有发布证据。

## 限制

旧未知大历史无法安全压缩的边界仍保留；旧 reader 不可直接降级读取新混源账本，回滚需匹配运行时和数据并保留新数据。组件验证不能代替实际 TUI。

## 建议下一步

严格检查全部通过后推送、同包双机部署和既定多路原生 TUI 矩阵；只读核对和不同 TUI 可并行，同一提交／来源链保持单写，避免影响 Jev 线。
