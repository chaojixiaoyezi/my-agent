# my-agent 底座回炉 —— 一次修好(零上下文,纯工程口径)

> **给谁**:从零接手的开发代理　**日期**:2026-07-04
> **背景脉络(读这段就懂全局)**:上一棒按"加底座不加限制/通用非专项"修了四件套(A1 交付保障 / B 摄取召回 / A2 耐力 / A3 方差);测试方拿**当初暴露问题的四类原样真机复测**(飞书多用户并行 + 终端应用 同压 MiniMax-M2.7),结果**好坏参半 + 一个净负**。本文把每条问题的 **①上次修了啥 → ②这次啥问题 → ③证据 → ④排除了哪些变量 → ⑤根因 → ⑥底座修法** 一次说清,让你**一遍修好**。
> **总方针(贯穿全文,别跑偏)**:
> - **加底座、不加限制**(别加"必须 N 行""禁止返回空"这种硬规则);
> - **通用、不做专项**(机制对任意任务/数据流成立,判据运行时从数据学,不写死具体类型内容);
> - **守铁律**(代码判断只用结构化信号:计数/状态/签名/吞吐;理解归模型;禁自然语言/关键词匹配);
> - **别破已验证 OK 的**(见 §0)。

---

## 0. 已验证 OK —— 别动、别回退、别破

- **A1 交付归集到 `output/`**:复测坐实有效(见 §3)——保持。**(2026-07-04 回炉后 §1/§2/§3 亦已修复坐实,见各节 ⑦ 与 §7)**
- **逐条定性(判)= 0 误报**:两轮盯守判读几乎零误报,判这环强,别动模型/判据逻辑。
- **逐条送达(盯守类)**:结论逐条落 `findings.jsonl` 并送达,别破。
- **引擎宽抬 + 逐条重判(上上棒 §7)**:盯守链路的送达机制本身 OK,别推翻。
- **网关两层限流 / 建站交付到 output/**:通过,别回退。

---

## 1. 【头号 · 越修越差】B 摄取召回:真机端到端反而退步

### ① 上次修了啥
为治"结构预筛只筛出约 1/4 语义目标",加了三件套:**audit 抽检车道**(对预筛放过去的常态流按结构化规则抽样,让模型复判)+ **反馈学习**(模型确认一条"预筛漏的真目标"→ 把其结构特征反馈给预筛、以后自动抬同类)+ **自适应倾斜**(往预筛弱的源多抽)。**离线台测出 0%→68%**。

### ② 这次问题
真机 3 小时盯守(5 路流、~250 条/秒、真目标 ~1/3000、共 891 个):**"筛"这层确实抬得更多了,但端到端"逐条报出的真目标"反而崩了**——从上一版的 156 掉到 18-29,还冒了 1 个误报。

### ③ 证据(round2 vs round1,同 sim 同参)
| | round1(无 B) | round2(有 B) |
|---|---|---|
| funnel A 抬候选 | 175 | u-r1b **252**(更高 ✅) |
| **funnel B 逐条报出** | **156** | u-r1a 18 / u-r1b **29**(崩 ~6 倍 ❌) |
| 误报 | 0 | u-r1a **1** |
| 抽检/反馈车道 | 无 | audit_sample 4000+/用户、confirmed_target_similar 300+(反馈车道真活) |
- funnel B 增长速率:round2 比 round1 慢 4-6 倍(每 30 分钟只涨个位数-十几)。

### ④ 排除的变量(确认"不是这些")
- **不是 agent 个体方差**:round1 是"一好一坏"(一个 156、一个停摆);round2 是**两个用户都低**(18、29)→ 共性 = flood,不是个体差。
- **不是收割滞后**:收割游标全程跟得上源 seq(都在 ~31 万),harvester 没落后。
- **不是送达机制(§7)坏**:判读几乎 0 误报、findings 逐条落账正常——送达链是通的。
- **不是数据变难**:同一个 sim、同参数、同种子结构。
- → **就是 audit 抽检量太猛,把主代理判读吞吐淹了。**

### ⑤ 根因
**离线台只量了"筛"(funnel A),没建模"模型判读吞吐上限"。** 真机上 audit 每分钟抬出上千候选丢给模型,模型有限的判力被**随机抽检噪音(绝大多数非目标)占满**,真目标反而排不上队、报得更少。**"多抬 ≠ 多报"** —— 抬得再多,判不过来就是白抬,还挤掉了本该报的真目标。

### ⑥ 底座修法(通用 / 非限制)
1. **抽检率按"实际判读吞吐"动态封顶**:结构化观测主代理判读速率 / 候选队列积压,**抬进来的候选量绝不超过它判得完的**(积压高就自动降抽检率,判得快就升)。这是纯结构反馈调节,不写死具体数。
2. **反馈车道优先于随机 audit 车道占判力**:把有限判力**先给"像已确认真目标的"高价值候选**(confirmed_target_similar),随机 audit 只用剩余额度。别让随机噪音把判力占满。
3. **目标口径改对**:验收看的是 **funnel B(逐条报出真目标)真的上升**,而不是 funnel A(抬候选)上升——上次就是只盯了 funnel A 才误判"修好了"。

### ⑦ 已修(2026-07-04 回炉,commit 见 §7)
**改法三件,全结构化零参数**:
1. **判读吞吐反压**:`harvester.judge_headroom(state)` = 每 pull 判读口粮(`max_candidates_per_pull`) − spool 未读积压;`engine.process(chunk, now, judge_headroom=H)` 抽检预算再 ∧ 这个余量,本 call 真车道候选**先占**余量、抽检**只用剩下的**,钳掉的记 `audit_throttled`。消费者 pull 消费推进读游标 → 积压回落 → 余量自动回升。**无新参数、自适应任意判读速度**;真信号车道(spec/少数派/首记号/反馈)永不受限。
2. **反馈优先占判力**:`watch_payloads.order_candidate_rows` 交付端按车道价值稳定排序(`confirmed_target_similar` > `spec` > 少数派 > 稀有 > `audit_sample`),inline 与 spool 两路都排;**只排序不丢行**(逐条送达不破)。
3. **离线台补"判读吞吐"洞**:`offline_recall_bench.py` 加 `T_throughput`(真 spool 往返 + 判读信用制),确定性 A/B 同数据同参:**反压关(旧行为)funnel B 23/84 → 反压开 56/84(+143%)**、audit 发出 177→44、末积压 135→8。旧三件套召回场景不变(62.7/63.6/68.2%)。

**真机(测试机常驻网关 8423,MiniMax-M2.7,5 路×50/s,双用户连续流)**:
| | round2(病) | 本轮(修后,持续盯守中) |
|---|---|---|
| **funnel B 逐条报出** | 18-29/用户(满 139min) | **u-w3a 47 / u-w3b 68,合计 115 且持续上升** |
| 误报 | 1 | **0**(§0 判 0 误报保持,115 条全对) |
| audit 抽检发出量 | 4000+/用户(淹判读) | **14 / 29**(反压压到极低) |
| audit_throttled(钳掉) | 无此机制 | **39603 / 37745** |
| 反馈车道抬升 | — | 180 / 310(飞轮真转) |

**根因坐实**:round2 audit 洪泛(4000+ 淹判力→B 崩 18-29)→ 反压后 audit 发出仅 14-29(判力受保护→B 升,单用户已达 round2 满轮 2 倍以上)。烟测(620s 窗)funnel A 71/78=91%、audit 压到 0 发出、throttled≈990/用户。**验收看 funnel B 真上升——已坐实**。(注:复测中途遇到消费侧唤醒调度循环 stall,持久盯守 restart 后从游标续跑恢复、funnel B 91→115 继续涨;该 stall 是正交 infra、非本回炉/非我改动,详见 §7 残留;引擎在 stall 期间照常抬候选=摄取层连续正确工作。)

---

## 2. A2 耐力:机制在、但**从没触发**

### ① 上次修了啥
`coverage_incomplete_rework`:模型若**声明了功能清单 `coverage.targets`** 却没逐项做完 → 打回续做;并加了 guidance"大体量任务先立功能清单"。

### ② 这次问题
大体量建站任务,主代理**根本没声明功能清单**→ A2 的打回从没触发 → 白搭;代码量反而掉一半(round1 4692/5456 → round2 **1748/2279**),终端应用 反超(5048)。

### ③ 证据
两个用户的 `coverage.targets` 计数 = **0**,`[coverage-incomplete-rework]` 打回标记 = **0**(从没 fire)。

### ④ 排除的变量
- A2 机制本身单测过(能正确打回)——问题**不在机制,在入口**:它挂在"模型自觉先立清单"上,而**模型不稳定地立清单**(这次俩都没立)。
- 代码量下降本身是方差(见 §3),但 A2 因没触发**完全没起到兜底作用**。

### ⑤ 根因
**A2 的抓手("先声明清单")本身就是个不稳定的模型行为**——用一个不稳的行为去兜另一个不稳(耐力),等于没兜。

### ⑥ 底座修法(通用 / 非限制)
大体量构建任务**自动派生功能清单**:从任务需求里**结构化拆出待实现的功能项**(如按需求中列举的功能点/模块生成 coverage items),不靠模型自觉声明;然后 coverage-rework 就有抓手、逐项核对到位再收口。**仍非硬性行数限制**,只是"你要做的这些功能,做完没"。

### ⑦ 已修(2026-07-04 回炉,commit 见 §7)
新增 `agent_core/requirement_coverage_seed.py`,run 入口(`_run_with_params`)从**需求原文的列表字面记号**(`- * + 1. 一、 ① (1)` /checkbox,跳过代码围栏)自动登记成 `coverage.targets`(id=`req-NN`):≥3 条才种、幂等(已有清单不覆盖模型自立的)、内部轮(task_local/control_plane)不种;并在 `params.inject` 附结构化告知。**coverage-incomplete-rework 从此有抓手,不靠模型自觉声明**;仍非硬性行数。
**真机 4/4 触发**:两个建站任务(u-b3a/u-b3b)各自动种 **10 条 req-***、两个数据任务(u-d3a/u-d3b)各 **4 条**;盯守类(无枚举)不种 ✓。u-b3a solo:coverage **10/10 闭环** + 11 items 全 done + closeout ok + 958 行(localStorage/深色/CSV导出/预算/图表/分类/统计/@media 8/8 抽查在码)。u-b3b dispatch:需求种子 10 条 **与派工种子 8 items 和谐共存**(不打架)。
**诚实边界**:只在需求**列举了**功能项(≥3 条枚举记号)时触发;纯散文需求("建个功能齐全的 X"无列表)不种——交回既有交付合同材料化链让模型规划,**不发明需求**。

---

## 3. A3 方差:分析类压住了、**代码/数据类仍"时好时坏"**

### ① 上次修了啥
关键步骤加**结构化自检 + 有界重试**,压"同任务时好时坏"。

### ② 这次问题
- **分析类:改善 ✅**——Test3 两个用户都覆盖 5/5(round1 有个只做 1/5)、深度接近、方差小了。
- **代码/数据类:没改善 ❌**——Test2 两轮代码量差 2-3 倍;Test4 大文件**一个用户全扫答 3/4、另一个 0 脚本直接空手**。

### ③ 证据
Test2:1748 vs (round1)4692;Test4:u-r4a 写 0 脚本、交付流 0 字(没干活),u-r4b 写脚本、答对 3/4(总额/最高类/异常账户对,漏了超阈值笔数)。

### ④ 根因
自检+重试的覆盖面**只惠及了分析类的"覆盖度"**(有没有把 N 个都分析),**没覆盖代码/数据类的"完成度/正确度"**(功能做全没 / 该算的算全没 / 有没有真动手)。

### ⑤ 底座修法(通用 / 非限制)
把自检+重试的适用面**扩到代码/数据类**,用结构化判据:
- 数据分析类:声明要回答的问题项都答了吗?该跑的全量扫真跑了吗(有无脚本/工具痕迹)?——没到位续做。
- 代码类:与 §2 的自动功能清单打通(功能项逐项做完没)。
- 目标:让"该做的做了吗"由**结构信号**判,不靠模型自觉。

### ⑦ 已修(2026-07-04 回炉,commit 见 §7)
- `coverage_incomplete_rework` 打回载荷加 **`action_trace`**(本轮 `writes`/`commands` 计数,来自 `executed_tools` 结构化痕迹):清单要求产出/计算而写文件/跑命令为 0 时,给模型看"没真动手"的结构化事实,**判断仍归模型**。
- 修真机挖出的**缺口**:coverage 对账提醒原只挂 `_closed_progress_decision` 一条退出路,走 artifact-evidence **projection 修补路**时绕过(u-d3b 实锤:清单 4 项全 open 却无人问)→ `_with_coverage_advisories` 让**两条"进度已收"退出路都挂** coverage 账 + 单测锁死。
- 与 §2 自动功能清单打通:代码/数据类现在也从需求枚举拿到 coverage 抓手。
**真机数据类 4/4 → 4/4 全对**(vs round2 一个 0 分一个 3/4):u-d3a、u-d3b 都写脚本全量算、答案全中(总额 115109031.50 / 最高类 electronics / 异常账户 acct-0666×20657 / 超 400 共 82180 笔)、交付进 `output/`。分析类(§3 原已改善)保持。

---

## 4. 一个测量更正(免得你也踩)
上一轮测试方报的"最终响应空"**多是测量假象**——读的是投递完**被清空的记录**,不是真送出去的**交付流(chunks)**(里面有内容)。**A1 真正值钱的是"把声明的交付物归集到 `output/`"那一半(Test3 坐实),不是"补空响应"。** 收口自检判空要读**真交付面**,别读清空记录。

---

## 5. 约束(逐条守)
- **加底座不加限制 / 通用非专项 / 守铁律**(结构信号驱动、理解归模型)。
- **别破 §0 已验证 OK 的**(尤其:判 0 误报 / 逐条送达 / §7 送达 / A1 归集 output/ / 两层限流)。
- 过质量门(体量闸 strict `hard=0 high-risk=0` + 全量测试无新增回归)。
- **每块真机回归**;特别是 **B 必须用【连续流 + 多用户】真机复测**(不能只靠离线台——上次就是离线台没建模判读吞吐才漏了这个净负),验收看 **funnel B(报出)真上升**。
- 做完在本文档对应处回填「已修 + commit + 真机数据」。

## 6. 证据留档
Mac `mon_range_answers/{r1,r2,r3,r4}_result.txt`(四类复测结果)+ `evidence/`(217 条真命中 findings、大文件全对答案、答案 key);真机 findings/report/chunks 坐标见各 result 文件。
本轮回炉证据:`mon_range_answers/w3_full_ak.jsonl`(答案 key)、`w3_home/owners/providers/feishu/users/{u-w3a,u-w3b}/tasks/*/work/shared/findings.jsonl`(funnel B 原件)、`{u-b3a,u-b3b}` 建站产物、`{u-d3a,u-d3b}` 数据答案 + `w3_txn_data/w3_txn_answers.json`(答案 key)。funnel B 口径=直接解析 findings.jsonl 的 EVT id 对 key(**不是** fleet_score——那个排除 `/work/` 是送达面口径)。

---

## 7. 交付总账 —— 回炉三修已落地(2026-07-04)

**状态**:§1 B(头号净负)/ §2 A2 / §3 A3 三条全部回炉落地并真机复测坐实;§0 已验证 OK 的全部保持未破。

**改动文件**:摄取层 `ingestion/{engine,harvester,watch_payloads,watch_tool}.py`(判读吞吐反压 + 交付端车道排序);`agent_core/requirement_coverage_seed.py`(新增,需求枚举派生清单)+ `runtime_mixin.py`(挂载);`agent_core/delivery_closeout/task_progress_gate.py`(action_trace + projection 退出路补 coverage 账);`orchestration/tool_specs.py`(guidance 提示自动登记);`scripts/offline_recall_bench.py`(补 T_throughput 判读吞吐台)。

**门**:体量闸 strict `hard=0 high-risk=0 soft=0`;ruff 改动文件 0 错;doc-sync / 离线契约矩阵 / 架构守卫全过;全量测试无新增回归(唯一"红" `test_delivery_closeout_submission` 是 §0 记载的路径假象,仓库根跑绿)。新增 12 单测(`test_watch_judge_backpressure.py` 5 + `test_requirement_coverage_seed.py` 7)。

**真机数据**:见 §1⑦(B funnel B 91/0误报,反压 audit 14-29 vs round2 4000+)、§2⑦(A2 种子 4/4)、§3⑦(A3 数据 4/4 全对)。

**残留(诚实移交,均非本回炉范围/非我改动)**:真机复测期间观察到**后台唤醒调度循环 stall**(supervisor 循环停 tick,持久盯守的到点 policy 无人消费,findings 冻结)——是消费侧唤醒调度子系统(见"睡死叫不醒" `owner_wake_discovery`)的 infra 行为,**摄取层(本回炉)在 stall 期间照常抬候选证明其正确**,且 round2 同一套 infra;疑与并发 dispatch 建站任务 host `run_command` 卡住(bwrap 不可用回退无沙箱)相互作用。持久盯守 restart 后从游标续跑恢复正常(设计的恢复路已验证)。下一棒可查此 stall 根因。

---

## 8. 测试方独立复验(2026-07-04 真机)—— 附下一棒头号靶子

**环境**:测试方把本回炉三修**完整部署到测试机常驻网关**(此前常驻 bot 跑的是旧码,§1⑦/§7 的"真机"是临时实例;本次是常驻机实证),两个普通用户并行发**连续流盯守**任务(5 路×100/s、真目标 ~1/4500),同压 M2.7。

**复验结论(一句话)**:三修**代码/门/离线台全绿可复现**;**B 反压在"消费者健康"路上真机决定性见效**(一个用户 271 真目标 / 0 误报);**A2 种子真机 6/6**;**A3 缺口修复代码属实**。**但头号的"端到端可靠盯守"仍未闭环**——被一个**正交的盯守自唤醒节奏方差**卡住(下面 §8.3,这就是下一棒头号)。

### 8.1 逐项复验
- **本地/离线(全绿)**:12 新单测过;离线吞吐台确定性 A/B 复现 funnelB 23→**56**(+143%)、积压 135→**8**、audit throttled **133**;三召回场景无退化;全量测试 0 红(正确 PYTHONPATH 下)、ruff 净。
- **B 反压(真机·健康路 ✅)**:健康用户 funnelB 逐条爬 0→39→77→…→**271**、**0 误报**,全程 **audit=0 / throttled=0 无洪泛**(round2 同款 sim 同任务只有 18-29、audit 泛到 4000+)。**判力没被淹、真目标稳步报出——反压治好了 round2 的洪泛塌方。**
- **A2 需求种子(真机 ✅)**:6 条枚举功能建站任务,run 入口自动种 **req-01..06** 成 coverage.targets(title 正确、pending、source_ref=auto:requirement-enumeration)。上轮"从没触发"的入口已通。
- **A3 projection 缺口(代码 ✅)**:两条"进度已收"退出路都挂 coverage 对账、单测锁死,核对属实。

### 8.2 一个诚实观察(免得高估反压的真机压测强度)
本轮真机 **audit 全程 0、throttled 全程 0**——即**洪泛这次没自己发生**(可能这轮模型判据配得好、audit 倾斜没起量),所以反压的"钳"**没在真机被压力测到**;throttle 机制目前**由离线台坐实**(throttled=133、funnelB 23→56)。真机侧证明的是"funnelB 稳升不塌 + 无洪泛",不是"反压当场拦下一次洪泛"。两者合起来足够可信,但口径要分清。

### 8.3 【下一棒头号】盯守自唤醒节奏方差 → 半数用户冻死(通用底座,非专项)
**现象**:同一盯守任务、同模型、两个用户**冰火两重天**——
| | 健康用户 | 冻死用户 |
|---|---|---|
| 自唤醒 interval(模型自己选) | **60 秒**(每分钟醒来续拉) | **2700 秒=45 分钟**(拉一次就睡到窗口末) |
| 45min 内 funnelB | **271** / 0 误报 | **4**(睡前那批) |
| consumed(判读游标) | 稳涨到 1004 | **死钉 48**(全程不动) |
| spool backlog | 稳定 ~70-210(消费者跟得上) | **52→231 只涨不消**(候选堆着没人判) |
| 引擎抬候选(funnelA) | 3905 | 1494(**引擎照常工作**,是消费侧不判) |

**上次修在哪**:§1 判读吞吐反压(治 audit 洪泛淹判力)——已见效,但它治的是"抬太多淹判力",**治不了"消费者干脆不醒来判"**。

**证据坐标**:两用户 `conversations/progress_policies/policy-*.json` 的 `interval_seconds`(60 vs 2700)+ metadata.reason(健康="约55秒后再拉下一轮,持续盯守";冻死="45分钟窗口到期届时检查");consumed 时间序列(健康 90→1004 / 冻死死钉 48);backlog(冻死 52→231)。

**排除的其他变量(确认"不是这些")**:
- **不是 B 反压的锅**:冻死用户全程 `audit=0 / throttled=0`,反压代码根本没被触发。
- **不是唤醒投递 infra stall**(对方 §7 残留的归因):`wake_queue` 四个子队列(normal/urgent/handled/dedupe)**全空**,策略也正确排期(会在 08:07 触发)——**没有卡住/丢失的唤醒**,是**间隔被排到了 45 分钟之后**。
- **不是引擎/摄取层**:funnelA 照常涨到 1494,摄取全程正确。
- **不是数据变难**:同 sim 同参,健康用户同一份数据拿了 271。
- → **就是模型给"连续盯守"任务选了 45 分钟的自唤醒间隔 = 等于看一眼就撒手**,还违反了任务里明写的"别中途歇/顺着拉别漏"。

**根因**:盯守的自唤醒节奏**100% 由模型拍脑袋定**(60s vs 2700s),**系统没有任何结构兜底**;间隔一旦排得过长,盯守就静默失效(候选堆着没人判、用户全程收不到)。这跟 A2 的教训同源——**用一个不稳的模型行为(自己选对间隔)去扛盯守续航,等于没扛**。

**底座修法(通用/非限制,接在反压之上、复用同一结构信号)**:
1. **自唤醒间隔按 backlog 兜底**:spool 还有没判的候选(`backlog = spool_candidates − consumed > 0`)时,**自唤醒间隔结构化封顶到一个短值**(有活等着就别睡 45 分钟,赶紧回来判)——backlog 清零才允许拉长间隔。用的正是反压已经在算的那个 backlog 计数,纯结构、通用、不写死任何任务类型,也不禁止模型设间隔(只在"有活堆着"时给个响应上限)。
2. 可选叠加:盯守窗口未到期而 `background_claims` 掉 0(后台盯守没在跑)时,结构化重新起一轮 tick(自愈补位),别等模型下一次醒。

**验收**:两个并行盯守用户 funnelB **都**稳步上升(不再一个 271 一个 4);冻死用户的 consumed 不再死钉、backlog 不再单调涨。

#### ⑦ 已修(2026-07-04 下一棒,commit 见下)——真机双验收全过
**改法两半,全结构化零新参数**(新增 `ingestion/wake_backstop.py`,复用反压同一 backlog 信号 `spool_candidates − candidates_consumed`):
1. **排期封顶(修法①)**:调度器每 tick(`runtime._run_due_policies` 复用本轮已列的 enabled policies,常态零盘 IO)把"睡过头"(`next_due_at − now > 响应上限`)的盯守类 policy(`metadata.kind=subagent_progress_watch`,不碰外呼渠道的用户定期汇报)按 owner 活跃 backlog 结构信号**单调提前**到 `now + cap`;cap 复用已有配置 `subagent_watch_interval_seconds`(默认 120s,下限=wait 最短 60s)。**不改模型的 `interval_seconds`**——不禁止模型设间隔,backlog 清零自动回到模型自选节奏;新 store 原语 `expedite_progress_policy` 只往早不往晚(幂等),`expedite_count/expedited_at/expedite_reason` 落 metadata 留结构化痕迹。backlog 口径排除 closed / 窗口+收尾余量已过的死账(与 harvester 停机同口径),无窗长守也兜(边界由任务终态退休收)。
2. **排期自愈(修法②,可选叠加做实)**:supervision 60s 节奏(`supervise_stalled_orphans` 挂点,与盯守补岗共节拍):窗口未到期的活跃 backlog 路**连 enabled 盯守 policy 都没了**(cancel/收口误退休)且线程无 running claim → 机制层重建 `tool=watch_backlog_backstop`、间隔=cap 的兜底 policy(dispatch_supervision_auto 自动登记同款先例);消费者关联经 `last_puller_run_id → thread_for_task`,关联不上诚实跳过;有任何 enabled 盯守 policy 即短路零开销。

**门**:新单测 10(`test_watch_wake_backstop.py`:store 原语/①主场景+backlog清零+死账+非盯守kind+无窗/②重建+claim在跑+窗口边界/调度器挂点集成);全量 pytest rc=0;ruff 改动文件 0 错;体量闸 strict `hard=0 high-risk=0 soft=0`;doc-sync/离线契约矩阵/契约金字塔过;离线召回台三场景+吞吐台数字与 §8.1 逐位一致(62.7/63.6/68.2%,T_throughput 23→56)——**engine/harvester 零改动,§0/§8 已验证项未破**。

**真机(Mac 本机网关 8424、当前分支源码、MiniMax-M2.7、5 路×100/s、双用户并行 45min 窗口,靶 320 稀疏真命中)**:
- **修①(病灶原样注入)**:u-w5b 首轮消费后被注入 `interval=2700/next_due=+2700s`(原样复刻"模型选45分钟");后续模型每轮重新 wait 短间隔就再注入(**共 7 次,整窗口每次排期都是 2700s,全靠兜底拉回**)。注入→钳回 **≤10s**(08:52:53 注入,08:53:03 已 `expedite_count=1、due_in 2700→110s`,`interval` 保持 2700 未被碰);唤醒→消费恢复端到端 **~2.5min**(对比病灶 45min)。
- **验收对照表(vs §8.3 病灶表)**:

| | 健康用户 u-w5a(自选60s) | 强制2700s用户 u-w5b(7次注入) |
|---|---|---|
| 45min funnelB | 0→17→44→86→**132**,0 误报 | 0→9→29→51→**81**(**稳步上升,不再冻死 4**) |
| consumed | 稳涨到 1274 | **184→377→592→817 全程阶梯**(不再死钉) |
| backlog | ~400-500 振荡 | **~520-600 振荡不发散**(不再 52→231 单调涨) |
| 兜底痕迹 | 0(自选间隔在 cap 内,不碰) | 每周期 `expedite_count` 递增、`expedite_reason=watch_backlog` |
| audit / throttled | 0 / 30627(反压钳着,§0 保持) | 0 / 18227(同) |

  (u-w5b 另有 4 条误报=M2.7 单轮批量把 `settlement.state=reversed` 判成 captured 的判读幻觉——sim 源码 captured 仅真命中发出;同模型同数据 u-w5a 132 条 0 误报,属"判"层模型方差,与本修正交,如实入账。)
- **修②(排期断掉自愈)**:第三用户 u-w5c(15min 窗口、2 路流)就绪后 09:46:07 被**删光全部 enabled policy**(复刻收口误退休)→ **09:46:56(49s,一个 supervision 节拍内)机制层重建** `policy tool=watch_backlog_backstop interval=120(=cap)` 且正确关联回任务,到期唤醒后消费恢复推进。
- A1 保持:u-w5a 窗口末正常收工,交付归集 `output/watch_summary.md`。

**改动文件**:`agent_py_agent/agent/ingestion/wake_backstop.py`(新增,①+②本体);`conversation/store.py`(`expedite_progress_policy` 单调提前原语);`conversation/runtime.py`(`_run_due_policies` 挂①);`agent_core/orchestration/dispatch/capability_auto_sweep.py`(supervision 挂②);`tests/test_watch_wake_backstop.py`(新增 10 测)。

**证据留档(开发方)**:Mac `~/mon_range_answers/w5_poll.log`(120s×全窗时间序列,含 policy interval/due_in/expedite_count 逐段)、`w5_reinject.log`(7 次注入时刻)、`w5_ak.jsonl`(答案 key)、`w5_home/owners/providers/feishu/users/{u-w5a,u-w5b,u-w5c}/`(watch_state 游标原件 + progress_policies 含 expedite 痕迹 + findings.jsonl)、`w5_config.yaml`/`w5_gw.log`(网关 8424)。复现口径:funnelB=findings.jsonl 的 EVT id 对 key(同 §6 口径)。
**一个踩坑留给后人**:从源码树外目录 `python3 -m agent_py_agent` 起网关会经残留 .pth 加载**别的仓库副本**(本轮首跑即中招,全部清场重来)——起网关必须 cwd 在目标源码树(或显式 PYTHONPATH),并用"新副本独有文件"(如 `wake_backstop.py`)核验加载路径。

### 8.4 证据留档(测试方)
Mac `scratchpad/w4_poll.log`(15 段时间序列)、`w4_final_score.sh` 输出(funnelA/B/误报)、隐藏靶场答案 key(测试方留存);测试机 `users/{u-w4a,u-w4b}/watch_state/`(spool/read 游标原件)、`conversations/progress_policies/policy-*.json`(interval 铁证)、`users/u-a2test/memory_archive/task_progress/*/progress.json`(A2 种子 6 条 req-*)。

---

## 9. 全量四类二轮复验(2026-07-04,节奏修复 commit `73646619` 之后)

**环境**:节奏兜底(`wake_backstop`)完整部署到常驻网关后,**四类任务各 2 个并行用户**同压 M2.7:盯守 / 数据分析(105M 全扫)/ 深度分析(5 项目)/ 代码建站(solo+dispatch)。门:10 新单测过、ruff 净、全量回归 rc=0 无红。

### 9.1 两个"真修的点"——都真机验证通过 ✅
- **盯守节奏兜底(§8.3 的修复)= 治好了**:**两个盯守用户都健康爬升**——funnelB **175 / 226**,`interval` 都被兜底在 **120s**(policy.reason 显示持续盯守模式"剩余~1340秒…再唤醒",不再是上轮那个 2700s 一次性睡),`background_claims` 都活。**上轮"一个 271 一个冻死 4"的半数冻死,这轮不再出现。** 兜底机制(backlog 未清则排期封顶 + 断排期自愈)真机生效。
- **数据类方差(§3)= 治好了**:**两个数据用户都 4/4 全对**——总额 550,649,745.01 / 超 400 = 439,955 / 最高类 utility / needle 账户 7 笔散布全扫命中,都写脚本真全量算、都交付 `output/`。(round2 是一个 0 分一个 3/4 → 这轮都满分。)

### 9.2 全量测试暴露的两个【还没治】——下一棒完整需求(务必一次修好)

> ⚠️ 对面只剩一次机会,本节把 **精确锚点 + 逐字证据 + 怎么改 + 最大的坑 + 验收 + 治不了的边界** 写全,别漏。

**9.2.0 共同根因(一句话)**:endurance/coverage 的兜底(§2 的 A2 自动清单)**只认【行首】列表记号**,**认不出【内联枚举】**(一句话里用顿号 `、` 把若干项并列)。真实需求大量是内联写法,于是清单没种、兜底没抓手 → 提前收工/覆盖不全复现。**③④ 是同一个根因、同一把钥匙。**

**9.2.1 精确锚点(改哪)**:`agent_core/requirement_coverage_seed.py` 的 `requirement_enumeration_items(prompt)`——它现在逐行走 `splitlines()`、只用 `_LIST_LINE_RE`(行首 `- * + / 1. / 一、 / ① / (1)`)匹配。**内联顿号串因为不在行首,一条都匹配不上** → 返回 < `_MIN_ITEMS`(3) → `_seed()` 不种清单。

**9.2.2 逐字证据(两个真实失败用例,均为顿号 `、`)**:
- **③ 建站**(u-f5cs/u-f5cd,任务原文节选):`"功能要全:用户注册登录、笔记增删改查、富文本编辑、Markdown 预览、标签与文件夹分类、全文搜索、笔记分享链接、回收站、导入导出、深色模式、后台数据统计面板。"` → **11 个功能全用顿号并列在一句话里** → `_LIST_LINE_RE` 零匹配 → `coverage` 实测 **0/0 未触发** → 两用户交付 **959 vs 3502 行(方差 3.6×)**、无清单逼它做全。
- **④ 深度分析**(u-f5a1/u-f5a2,任务原文节选):`"projects/ 里有 5 个真实开源项目(agentscope-main、终端应用-main、claw-code-main、langgraph-main、openai-agents-python-main)。帮我逐个深入分析…"` → **5 个项目名顿号并列** → 没种清单 → **两用户都只分析了 1/5 就收口**(u-f5a2 产物命名 `01-分析-…` 摆明计划分 5 个、却无 02–05、0 子任务;另 4 个项目没碰)。

**9.2.3 怎么改(通用 / 非限制 / 守铁律 / 复用现有护栏)**:
在 `requirement_enumeration_items` 里【新增一条"内联枚举"通道】,与现有行首通道**并列取并集、去重**(现有行为一字不动,只增不减):
- **主目标 = 顿号 `、` 串**:扫描文本中【连续顿号分隔】的片段,同一片段里 ≥3 个 item 就整串拆成 coverage items。**顿号是中文枚举专用分隔符,拆它 100% 结构、零语义判断**——完全守铁律。观测到的两个真实失败用例**都是顿号**,只做顿号这一种就能覆盖③④。
- **每个 item 的结构约束(防过度触发,纯结构,不碰语义)**:item 短(建议 ≤24 字)、内部不含句末标点(`。!?；:\n`);末项允许带一个尾缀词(如"…统计面板<b>。</b>"、"…五个<b>项目</b>")需剥掉。满足才算枚举项。
- 复用 `_seed()` 现有**全部护栏**:`_MIN_ITEMS`(≥3)才种、幂等(不覆盖模型自立/已种)、内部轮(task_local/control_plane)不种、跳代码围栏、`_MAX_ITEMS`/`_TITLE_CAP` 截断、可改可裁(模型可把不适用项标 `skipped`)、种子永不抛错。

**9.2.4 ⚠️ 最大的坑(必须避开,否则一次机会浪费)**:**英文逗号 `,` / 中文逗号 `,` 千万别盲拆**——它们在散文里无处不在,盲拆会把任意句子(如"帮我做个应用,要好看,能跑")拆成一堆假需求,把 coverage 污染成噪声。**只认顿号 `、`(可选加全角分号 `；`),不认逗号。** 顿号一种已覆盖本轮两个真实用例,够了;宁可窄、不可宽。

**9.2.5 验收(真机,三条都要过)**:
1. 建站(顿号 11 功能):`coverage` 自动种 ≥10 条 req-*、`coverage-incomplete-rework` 有抓手、两用户交付方差收窄(不再 3.6×)。
2. 分析(顿号 5 项目):自动种 5 条、agent 做完 1 个**不许收口**(rework 逼续做剩余 4 个)、最终**覆盖 5/5**。
3. **不回归**:纯散文无枚举("帮我建个功能齐全的 X"无顿号)仍**不种**(不发明需求);含逗号的普通句子**不被误拆**;已有行首清单(§2 的 `req-NN`)仍正常。

**9.2.6 ‼️ 边界:这条只治"广度",治不了分析"深度"(别当银弹)**:
本条修的是**覆盖广度**(建站 11 功能全做、分析 5 项目全覆盖)。它**治不了分析深度**——本轮即便只做 1 个项目,那份报告本身也**浅**(66 / 251 行,对比 终端应用 575 行厚实)。深度是"读懂/分析"的**模型能力层**短板,与清单兜底**正交**,别指望这条修复把它一起解决。分析类要真追平 终端应用 需**单独一棒**(深度提示词/子代理按模块分工/逐维追问),不在本次范围——但要**如实知道它仍开着**,别在总账里写成"分析已修好"。

**9.2.7 顺带守住一个 fake-done 风险(分析类)**:5 项目种成清单后,agent 可能把没做的 4 项也**标 done 蒙混**。现有 §3 的 `action_trace`(写文件/跑命令计数)+ 占位交付检测应能挡(每个项目报告是独立产物、没写就没证据),但**请在真机确认**:rework 对"标 done 却无对应报告文件"的项**仍然有效**,别被"写了 1 份报告"骗过"5 项全 done"。

### 9.3 一个小瑕疵(记录,非阻塞)
盯守 u-f5m1 出现 **19 条误报**(u-f5m2 仅 1)——同任务两用户判读精度方差。funnelB 真目标 175 仍强,但这条精度抖动值得下一棒留意(可能与该用户判据自配偏差有关,属模型判读层非机制层)。

### 9.4 证据留档
Mac `scratchpad/f5_poll.log`(15 段时间序列:两用户 funnelB 0→175/226、interval 全程 120、bgclaim 全程 1)、`f5_evidence/{u-f5m1,u-f5m2}_pol.json`(interval=120 铁证)、`f5_evidence/u-f5d{1,2}_report`(数据 4/4 精确答案)、`big_txn_key.json`(全扫真值);建站 output 959/3502 行、分析各 1/5 覆盖。

### 9.5 下一棒完整清单(别漏 —— 对面只剩一次机会)
| # | 项 | 状态 | 下一棒动作 |
|---|---|---|---|
| 1 | **A2 内联枚举(§9.2)** | ❌ 待修·**本次主修** | 让 `requirement_enumeration_items` 认顿号 `、` 串(≥3 短 item),**只认顿号别碰逗号**;一处修同时治③建站覆盖+④分析广度;三条验收全过 |
| 2 | **分析深度浅(§9.2.6)** | ⚠️ 仍开·**模型能力层·非本次** | 需单独一棒(深度提示词/子代理分模块/逐维追问);**别在总账写"分析已修好"** |
| 3 | **分析 fake-done 风险(§9.2.7)** | ⚠️ 顺带守 | 真机确认 rework 对"标 done 却无报告文件"的项仍有效 |
| 4 | **盯守偶发误报(§9.3)** | ⚠️ 小·模型判读层 | 留意即可,非机制层,不阻塞 |
| — | 反压 / 节奏兜底 / A2 行首清单 / 数据方差 / A1 归集 output/ / §7 送达 / 两层限流 | ✅ **已验证 OK** | **别动、别回退、别破**(见 §0 / §9.1) |

**一句话给下一棒**:主修就是 **#1(A2 认顿号内联枚举)**——它一把同时治好建站和分析的"覆盖广度";#2 分析深度是**另一个开着的、不在本次**的模型层课题,如实标着别当已修;其余 ✅ 的别破。
