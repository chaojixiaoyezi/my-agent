# 十步重构与插件交付执行 Goal

## 当前 TODO（唯一执行清单）

解决问题：把内部实现进度与真正可用的交付分开，让每次推进都有明确终点，避免只汇报测试数量。
第 4—6 步的框架必测项已收口；第 5 步已发布并双机同版部署，第 6 步在同一运行包上完成故障、长任务和管理并发的原生 TUI 验收。当前第 7 步子代理生命周期与交接的前四片已从独立工作树应用到原 checkout，组合定向回归、严格 gate 与 wheel 边界检查通过，已推送远端 main，测试机默认入口与唯一 Gateway 已切换。TUI195—197 的基础任务、父子收口和产物已核对，197 原页超时、198 重连可读分列；本机切换和完整真实 TUI 矩阵待完成。旧模型交付失败及未独立核实的报告判断按下文保留。
并行的 TUI／历史修复已在另一隔离候选包由原生 TUI 验过插话顺序、重连和长记录阅读；
代码已在原 checkout 单独提交并随本轮推送远端 main，已随组合包切换测试机默认 Gateway，本机尚未切换，
也不计作第 7 步的真实子代理验收或第 9 步整步完成，边界见 TESTS／STATUS。
本节是唯一进度表；下面的分步正文保留验收合同与历史证据。每个编号只在交付物及其验收证据齐全后勾选。

**当前工作队列（按依赖排序）：**

| 编号 | 要交付的东西 | 怎样算完成 | 状态 |
| --- | --- | --- | --- |
| 5.2a | `/plugins info` 和成功启用回执展示同一张使用卡，顶层 `/help` 纠正过期文案 | 使用卡从当前包声明和设置 schema 生成，给出动作、两条中文示例、必填设置及停卸入口；旧启用请求不能宣传新激活代次，私有设置值不出现在回执 | 完成；新版 TUI168 已读回帮助、详情与启用卡 |
| 5.2b | 核对列表、逐级补全、动作帮助、配置入口都读同一目录；只修复实际发现的缺口 | 解析／展示／补全对同一动作及参数一致，Tab 只填入不执行；停用、卸载或换代后的旧候选按 revision 拒绝，不增加第二份目录 | 本地完成；列表补简介，用法共用参数声明，原旧快照回归通过 |
| 5.3a | 本地验证 | 受影响的解析、补全、管理、审批、旧快照回归通过；Ruff、文档同步、严格尺寸、diff、clean-package 全通过 | 已通过；11 文件 300 项与严格 gate 均通过 |
| 5.3b | 发布并同版部署 | 核对远端 main，保存回滚，同一 wheel 安装到目标机器；每机只有一个 Gateway，默认 TUI 与 Gateway 版本一致；与 Jev 线协调共享服务 | 完成；`689e83e85` 的同一 wheel 双机各 1,267 个包文件逐项一致、各一台 Gateway，旧运行环境和回滚证据保留 |
| 5.4a | 三路以上原生 TUI 业务验收 | 管理启停、显式参数／翻页、普通中文调用分别由真实 TUI 发起；记录模型官方端点、TUI 编号、tmux 会话、原任务／工具账和产物读回 | 完成；168 的四条管理原操作、169 的显式分页、170 的普通中文插件及内置写入、171 的本机核心任务均读回成功 |
| 5.4b | 原生 TUI 交互与负向验收 | 实际核对目录刷新、旧候选、重连、编辑／粘贴／中文、滚动／复制和长输出；补全不得自动执行或让错误命令落入聊天 | 完成；169 旧候选拒绝、目录刷新、Tab 不执行和错误命令不落聊天；169 四页读完长文件，171 bracketed paste，172 滚动，169 详细记录复制，173 原会话重连后再次实际调用插件 |
| 5.5 | 第 5 步收口 | 将框架结果、模型内容错误和未覆盖项分别写入 TESTS／STATUS；必测框架项无失败或缺证后，才勾选第 5 步并转第 6 步 | 完成；原 TaskRun／AgentRun／工具操作、磁盘产物和模型端点已核对，旧模型内容失败及后续长任务边界单列于 TESTS／STATUS |

测试机清理已确认的旧测试资料后，非保留空间约 1.3 GiB；但可用内存不足 0.1 GiB、交换区已满，重负载仍优先本机。清理时误删了测试机当前工作区的派生检索索引，已重新初始化 schema 并由新 TUI 实际调用工具确认可用；旧索引内容没有恢复，这属于测试操作失误，不能记成产品通过。第 6 步的三路并发、准确资源撤销和长任务证据已核对；下一份交付从第 7 步的子代理状态／交接职责拆分开始。

**第 6 步当前 TODO（先读合同，再做故障和并发验收）：**

| 编号 | 要交付的东西 | 怎样算完成 | 状态 |
| --- | --- | --- | --- |
| 6.1 | 核对插件端点、HostCommand、撤销、受管进程和当前测试环境 | 列出调用前、审批后、发送后及清理失败的原账状态；确认本机共享 Gateway、其他活动任务及磁盘余量，给故障夹具限定私有范围 | 合同和双机环境已核对；每次共享操作仍即时复核活动任务和安装窗口 |
| 6.2 | 构造可控的异常、阻塞和退出测试插件 | 标准本地包只用于测试，默认停用；故障由插件进程制造，不在核心按插件 ID 特判；独立校验包、工具目录和退出行为，原始包留仓库外 | 完成；私有标准包及 wheel 独立校验，随后由真实 TUI 安装并调用，原包不入仓库 |
| 6.3 | 先做对应合同／替身与受影响回归 | 停用抢先、在途取消、未知结果、原资源清理和重复请求沿现有权威账判定；若出现产品失败，先定位通用层，再做最小修复 | 受影响的停用竞态、发布、调用及宿主命令定向回归通过；尚未发现需改产品代码的确定性失败 |
| 6.4 | 至少三路原生 TUI 并发：插件任务、核心长任务、管理 | 官方模型与同一 Gateway；连续约 15–30 分钟有实际内容，核对时间重叠、独立任务身份、工具账、产物和管理响应，不用空睡眠充长任务 | 框架完成：183／184 分别持续 1,221.5／983.2 秒且重叠约 15 分钟；192／194 分别持续 3,053.6／973.9 秒，后者两条任务运行中 179 实际调用、停用、卸载独立控制插件。192 的 600 页原工具操作全成功、各页一次且返回与合成源完全相同。管理窗口本身不冒充持续 15 分钟；模型报告质量失败单列 TESTS。 |
| 6.5 | 实际异常、卡住、审批等待取消、断连、运行中撤销及清理失败 | 原生 TUI 发起与控制；旧代次不复活，未知副作用保持未知，管理入口和核心任务可用；重复停卸／再启用及精确进程退出逐项留证 | 主要场景已覆盖：执行中撤销的 5 个准确 session 确认退出；审批等待后停用使业务调用在发送前失败；私有环境清理失败保持 revoked，恢复权限后停用释放，旧 UNKNOWN 不改；原会话重连拒绝已卸载命令，卸载后新 TUI 核心任务正确。测试机审批后业务连接退出未确认仍单列，不改报已确认。 |
| 6.6 | 第 6 步证据与发布收口 | 区分框架、模型和测试准备失败；读回原请求、工具、进程、Gateway 和用户产物，清理仅限本任务资源；必要代码改动通过严格 gate、同版部署和新 TUI 复验 | 框架收口：原工具账、合成源、磁盘产物与任务终态分开核对，179 原生命令停卸两个受控包后目录仅余 `workspace-peek`，双机原 Gateway 仍在。本步未改产品代码，无需重发包；内容错误、193 供应商参数失败、旧测试准备失误和测试机连接清理提示不改报成功。 |

**第 7 步当前 TODO（子代理状态和交接，按序小片推进）：**

当前新增必修：wake 已发布而 dedupe 回执写失败时恢复重复通知，pending／handled 两个故障用例已红灯。
先完成原存储内的固定配对发布和恢复，再收窄 commit／closeout／wake 的实际依赖；
不能把正常 TUI 或此前严格 gate 算作新故障已关闭，相关实现前不进入第 8 步。

| 编号 | 要交付的东西 | 怎样算完成 | 状态 |
| --- | --- | --- | --- |
| 7.1 | 核对状态、结果、持久收口、父通知的调用方和原提交顺序 | 读当前合同与定向测试，检查成熟实现的对应局部，标出 canonical run／attempt、锁、恢复及可移动的纯投影；不动本轮范围外的 Audit/摄取 | 完成；迁移边界和原提交顺序已记入子代理并行设计，局部参考 Hermes／Codex／Free-Code |
| 7.2 | 拆状态裁决与展示／结果投影 | 新职责只取必要结构化事实，旧入口迁完调用方即删；等待不能被宽泛布尔值改为失败，原身份与配置不变 | 已应用到原 checkout：展示、完成信封、exact attempt 准入分离；旧实现／导出删除，组合定向回归通过。准入参数进一步收窄为原 RuntimeDB 的小片已通过直接回归和严格 gate，测试机已部署，199／200 真实验收中，本机和远端待同步 |
| 7.3 | 拆持久收口与直属父通知 | 沿原事务、run／attempt 和通知幂等键，结果先持久化再通知；重投只读原结果，不复制权威状态 | 已拆初次提交并保留 delivered 写失败的 WAL；新发现 wake／dedupe 半写重复通知，原文件故障用例已红灯，隔离工作树修复中。随后继续收窄 commit／closeout／wake 透传的 manager，不能只换对象名 |
| 7.4 | 合同、替身和回放验证 | 单孩子、三孩子、孙代理、等待、失败／取消、重复通知、恢复和隔离的受影响测试通过 | 离线完成；隔离线 10 个直接受影响文件 229 passed、4 xfail、1 skip，另 7 个恢复／停止／产物文件 68 passed、1 xfail；组合源码另跑 17+9 个跨线／恢复文件无失败，真实环境隔离矩阵待核 |
| 7.5 | 严格 gate、发布与同版部署 | 仅相关测试加 Ruff、doc sync、strict code-size、diff、clean-package；版本、回滚、Gateway 与入口逐项核对 | 隔离与组合源码严格 gate、wheel 包含性及发布边界通过；`c9042f5f2` 已推送远端 main；测试机 1,273 个安装文件与 wheel 一致，默认入口与唯一 Gateway 已同版切换，旧运行环境和入口保留回滚；本机同包候选安装已核对，仍有原 Gateway 执行的后台 attempt，未切换；HTTP processing=0 不代表后台空闲，部署须同时核对运行账并协调其它活动测试 |
| 7.6 | 多路真实 TUI 与证据收口 | 官方模型从原生 TUI 发起，覆盖单／多／递归孩子、等待时插话、失败取消、恢复和另一主任务隔离；核对原任务／工具账、产物、通知和资源退出 | 进行中；195／196 正常交接与数据正确；197 原页超时、198 重连可读分列。准入收窄包的199已核对主子孙及等待插话，200有效项目工作约32.5分钟已自然完成，原生62项测试通过、三孩子通知及锁已核对，上游提交来源缺证单列；201原生停止的两孩子取消、宿主退出，139秒后无复活／锁且200仍运行已核对。通知半写修复尚未部署，不用正常结果替代故障／恢复矩阵 |

长任务可由多个真实工具步骤组成；故意卡住的夹具仅作故障注入，不充当有实际工作内容的长任务。清理失败必须有可恢复、精确归属的制造方法，不能破坏共享 Gateway、旁观任务或生产状态来凑失败样本。

**第 6 步原账核对矩阵（待真实 TUI 逐项填证）：**

| 阶段 | 应核对的权威事实 | 不能据此推断 |
| --- | --- | --- |
| 审批等待、尚未发送业务调用 | 固定激活代次、原 HostCommand／TaskRun、审批状态、是否存在 `tools/call` 工具操作；停用先提交同代撤销 | 等待时可能已有 MCP 连接，不能要求零插件进程，也不能把连接启动算成业务副作用 |
| 已发送、执行中或超时 | 原 operation、effect outcome、取消事件与原进程 session；撤销只作用于固定 activation 和精确 session | 超时通知或进程停止不证明已发送的副作用回滚；不把未知改成失败或成功 |
| 业务返回、连接清理中 | 原工具结果与 connection cleanup 分开读；执行区间释放后才结算原运行，重送只读同一原请求 | 不能用已展示的 TUI 文案代替原 TaskRun／工具终态，也不能重新执行原请求 |
| 撤销或清理未确认 | 安装表保留 revoked、`cleanup_confirmed`／`released`、原 session 退出证据和错误阶段；再次启用须先完成原资源释放 | 不能因插件目录不可见、进程号消失或重连而宣称清理成功、复活旧激活 |

当前单次插件 `tools/call` 使用宿主 MCP 的约 60 秒默认期限；连续长任务要由多次实际工作组成。清理失败若采用受控目录权限夹具，只验证该激活环境回收失败，不能冒充进程终止失败；恢复仅处理夹具自身目录，旧 UNKNOWN 保留。

**第 5 步之后的交付顺序：**

| 步骤 | 先交付什么 | 必须看到的原生 TUI 证据 |
| --- | --- | --- |
| 6 | 故障与运行中撤销：阻塞／异常／断连／清理失败均按原归属结算 | 插件长任务、核心长任务、管理操作至少三路并行；取消、重复卸载、重连后的进程和原账可核对 |
| 7 | 子代理状态、结果、持久收口、父级通知按职责拆开 | 单孩子、三个并行孩子、主子孙、失败／取消／恢复和独立主任务；实际产物、原账及交接一致 |
| 8 | 模型请求、工具轮、Compact、结束决策按原执行链拆开，并处理已记录的通用交付核验缺口 | 长文件、多工具、Compact 读回、中断／恢复和普通对照；TUI164 失真不再被错误声明为已核验通过 |
| 9 | TUI 输入、状态消费、渲染拆开；插件展示和订阅可撤销 | 流式输出时切会话、中文／多行、滚动／复制、审批、重连、面板启停；核心输入持续可用 |
| 10 | 约定的十个自有简易插件分三批完成，再做混装及全部卸载 | 每个插件实际装／配／用／停／再启／卸；长任务、多子代理、视觉／浏览器按能力分别验证，卸载后新会话核心基线通过 |

每步沿下文合同细分开发、定向测试、发布、原生 TUI 和证据收口；没有上一步框架验收就不勾下一步。常规小片仅跑相关测试和严格发布 gate，不因进度焦虑重复全仓回归或重测未改动的失败样本。

### 第 4 步：先交付一个真正可装卸的文件查看插件

- [x] **4.0 装卸与调用底层本地实现。** 安装、配置、启停、卸载、显式命令、普通工具、审批运输及逐次工作区权限已接通；只是源码与组件验证完成，完整真实 TUI 验收仍在 4.6。
- [x] **4.1 SDK 可独立安装。** 标准 wheel、独立环境安装/导入和严格读取边界已验证；源码逐字节核对宿主唯一原实现，SDK 不依赖完整宿主包。开发后端依赖已显式声明。
- [x] **4.2 workspace-peek 业务实现。** 已实现 `show`、`tree`、中文空格路径、分页和配置；实际独立 MCP 验证权限拒绝、链接、文件与目录变化、扫描预算及错误恢复，不扩大读取权限。
- [x] **4.3 生成真正可安装的插件包。** 同一声明生成工具和命令，标准 wheel 连同 SDK 通过原包和依赖校验；缺依赖不发布包，未新增安装旁路。
- [x] **4.4 原链路集成与故障验证。** 实际构建包完成安装→配置→启用→显式/普通工具调用→停用→再启用→卸载；重复操作、坏配置、旧快照及卸载后内置读取均验证，相关包/启用/撤销/调用回归共 254 项通过。这一步不计为真实 TUI 验收。
- [x] **4.5 发布并同版部署。** 审查本次发布累计改动，按仓库规则通过本地严格 gate；累计改动已超过阈值，发布前追加全仓 pytest。推送远端 main 后，同一宿主 wheel 部署本机与指定测试机并保留回滚。完成条件：版本和安装文件核对一致，每机一个 Gateway，协调另一开发线后再操作共享服务。
- [x] **4.6 三路以上真实 TUI 验收并收口。** 管理、显式、普通中文和核心 TUI 的原账／产物均已核对；框架必测项有证据。164 的模型产物改错表情和行尾，交付失败单列给第 8 步，不宣称所有用户任务通过。

执行顺序：4.1 → 4.2 → 4.3 → 4.4 → 4.5 → 4.6。workspace-peek 已完成开发和原生 TUI 框架验收；三项失败修复的 `fb3857e46` 已推送 main、同一 wheel 部署双机，当前执行 5.3b。

**4.5 当前逐项交付：**

- [x] **4.5a 修复全仓的 5 项失败。** 3 个 CLI 导入越层共造成 1 项检查失败；另外为静态命令断言、装配桩缺少 root、两项 TUI 传输替身过时。修复后原失败及受影响的取消、插件和审批链共 50 个文件、1,195 项全部通过。
- [x] **4.5b 发布检查全部通过。** 原全仓结果保留，修复后的候选重新跑全仓及严格 gate；不把旧源码结果移作新版本通过。
- [x] **4.5c 快进推送 main。** 核对远端新提交，确认实际 SHA，线上 CI 单列。
- [x] **4.5d 双机同包部署。** 安装文件一致、默认入口同版、每机一个 Gateway；切换前核对活动任务并保存回滚。
- [x] **4.5e 开四路实际 TUI。** 154—157 已从新版默认入口创建，分别测本机装卸、本机插件业务、测试机装卸、测试机多子代理核心任务；官方模型来源已核对，tmux 查看命令已提供。完整结果归 4.6。

分工：主代理修复并发布；Astra max 子代理已独立复现两项 TUI 测试失败，确认只需补齐流式命令传输替身，不改产品链或增加等待时间。Jev 保持独立。
主代理负责 SDK、示例和集成写入；有明确独立审阅对象时可由 Astra max 子代理只读检查，避免多人重复检查或改同一模块。
Jev 开发线保持独立，不动其模型与用量模块；共享发布、部署和 Gateway 操作先协调。

**4.6 当前可执行 TODO（按依赖推进）：**

| 编号 | 具体交付 | 完成条件 | 当前状态／负责人 |
| --- | --- | --- | --- |
| 4.6a | 定位本机正常插件启用失败 | 原操作、准备进程终态和确定性交错复现相互印证，分清安装失败与启动交接失败 | 已完成定位；Astra max 只读复核已结束 |
| 4.6b | 修复通用启动观察竞态 | host 退出后重读同一 session 的权威终态；旧实现失败、新实现通过，并保留身份、撤销、非零退出与未知结果边界 | 已完成本地修复；383 项相关回归通过 |
| 4.6c | 新修复发布与同版部署 | 相关回归及本地严格 gate 通过；非强制发布；双机同一包，原活动资源核对与回滚齐全 | 已完成；d4d540c57 双机同版，新版 TUI158—162 已创建 |
| 4.6d | 完成真实插件管理与显式调用 | 新版新 TUI 实际安装→配置→启用→分页读取／目录→停用→再启用→卸载；错误包、拒绝、重复操作、旧快照和资源退出逐项有证据 | 163 与 166 已覆盖全部列出的动作；166 最后从空安装表重新完成有效包安装→配置→启用→显式读取→停用→卸载，原操作均成功，最终安装表为空 |
| 4.6e | 验收普通中文插件业务并分开结论 | 新 TUI 只发一次中文需求，由模型自行调用插件并交付；核对工作区、审批、工具账与实际内容 | 164 原工具账五项成功；产物把源文 🙂 改为 😂、CRLF 改为 LF，内容交付失败；归第 8 步通用交付核验，不冒充功能通过 |
| 4.6f | 完成装卸期间的核心隔离验收 | 独立 TUI 跑内置工具／多子代理任务，管理端同时装卸；核对任务重叠、身份、权限与产物，框架和交付质量分列 | 158 停用与 160 主任务重叠且三孩子 9,000 行交付读回正确；165／167 新版双机核心短任务通过，167 卸载后 40 行核心任务完成，长任务仍归后续步骤 |
| 4.6g | 逐项收口并进入第 5 步 | 必测框架项无失败或缺证；模型交付失败、未覆盖项及后续归属明确，不用“状态完成”代替内容通过 | 框架项已收口；164 内容失败归第 8 步，测试机磁盘边界已记录，开始第 5 步只读摸底 |

4.6b 与不受影响的测试机只读取证／显式调用可并行；4.6d、4.6e、4.6f 在新版部署后并行。
同一失败只安排一个定位负责人；新增小修复按本次改动范围做相关回归和严格 gate，只有再次达到仓库阈值或用户明确要求才追加全仓。
每项未满足完成条件就不勾选；遇到新失败，记录原证据、归属层和下一项修复，不反复重测同一未修改版本来碰运气。

**4.6d—g 验收明细：**

- [x] **4.6d-1 重复管理与旧补全取证。** 158 重复安装／停用／卸载原操作均 SUCCEEDED；159 原补全选择在卸载重装后提交，被目录版本检查拒绝，没有登记操作或新业务任务。重复启用 active 的准备拒绝已复现为空资源声明错误，本地已修复，真实复验归 d-4。
- [x] **4.6d-2 完整业务失败可读且不锁死后续调用。** 163 链接读取的原操作为 FAILED／UNSAFE_PATH、原锁清零；同一 TUI 随后读取正常文件的原操作 SUCCEEDED，首 64 字节与输入一致。旧 158 的 UNKNOWN 保留，断连、超时和旧 UNKNOWN 不回写。
- [x] **4.6d-3 目录连续翻页。** 测试机 166 用原游标和逐次审批连续读取 0—2、2—4、4—6、6—7，原操作均 SUCCEEDED，七项不重复且终页无游标；旧 161 第三页失败保留。
- [x] **4.6d-4 重复启用与显式拒绝。** 本机 163 已启用状态重复 enable 返回 unchanged、激活代次不变；测试机 166 在真实授权弹窗选择“拒绝”，原任务 failed、没有工具操作。
- [x] **4.6d-5 坏包／坏配置。** 166 通过真实 TUI 提交无效 ZIP 与无效配置，两次原操作均 FAILED／TOOL_INVALID_ARGUMENTS；正常配置、重新启用及再次获批读取均 SUCCEEDED。失败未把宿主锁死。
- [x] **4.6d-6 完整停卸与资源退出。** 166 真实 TUI 停用→正常配置→再启用→显式读取→再次停用→卸载，原操作均成功；安装表最终为空，停用回执五个 session 均确认退出／清理且无未解决 PID。卸载后新显式输入在目录层拒绝，未创建原任务或工具操作；167 卸载后的 40 行核心任务终态完成且数据正确。166 随后从空表重新做有效包安装→配置→启用→获批读取→停用→卸载，原操作均成功、安装表再次为空。
- [x] **4.6e-1 中文交付归属。** 164 一次中文需求的 tree／show／写入五项原工具操作均成功；源文表情为 🙂，产物写成 😂，五处 CRLF 变成 LF，却声称完整。只读核对源文件和产物，判定模型内容失真、交付失败，保留给第 8 步通用交付核验；插件工具链本身可用，不能把工具成功冒充交付成功。
- [x] **4.6g-1 第 4 步收口。** d-5、d-6、e-1、核心隔离和未覆盖项已核对；TESTS、STATUS、ROADMAP 与完成索引已更新，doc sync／diff 检查通过。仅框架必测项收口，164 内容失败仍未通过。

三项修复的最终本地验收已通过：17 个相关文件 389 项通过，本地严格 gate 通过、尺寸基线未改。
第 5 步 5.1 只读摸底已完成，下一交付为 5.2 展示改动和定向验证；不重跑没有源码变化的第 4 步失败样本碰运气。

**当前事实：** 旧版 TUI156 的显式读取先拒绝一次，再逐次批准三页；原工具账的连续三页拼接与 180 字节原文件完全一致且末页 EOF，不能计为新版目录分页通过。新版 161 目录第三页失败，158 链接读取 UNKNOWN，原失败保留。
本机正常包准备命令均退出 0，原操作却为 `preparation_launch`；已复现启动观察先读取旧状态、随后看到 host 退出而不复读终态的竞态。
历史回执未保留底层异常文本，具体现场交错是原记录与复现支持的定位。修复的 4 项旧实现失败用例修后通过；16 个文件共 383 项相关回归通过，已发布部署，并由新 TUI158／161 实际成功启用复验。
故障夹具的原启用结果为 `plugin_endpoint_failed`，卸载清理回执已核对 activation 退出码 23，预设故障点确实执行。
TUI157 三个孩子各 3,000 条有效数据，但合并仍含两处重复表头（共 9,003 行）；检查程序只验子文件，整轮交付失败保留。

### 后续顺序（第 7 步准备中）

- [x] **5：TUI 发现和使用。** 列表、帮助、补全与配置入口同源；三路以上原生 TUI 核对中文调用、显式命令及启停刷新，另验重连、粘贴、滚动／复制和长输出。原账与产物见 TESTS。
- [x] **5.1 只读摸底与边界。** `PluginCommandCatalog` 已从安装表投影动作，公共参数声明同时驱动解析、帮助和补全；TUI 显式 Tab 刷新，管理回执带新目录，旧候选按 revision 拒绝。163 实际 `/plugins info` 仅显示状态和简介，`/help` 仍误标 `/plugins@` 尚未开放；启用回执没有使用卡。第 5 步只补这些展示缺口，不复制目录或增加轮询。
- [x] **5.2 最小结构改动。** 列表补公开简介，详情和启用回执共用使用卡，用法复用公共参数声明，顶层 `/help` 更正；分级补全、配置和旧版本拒绝继续读同一目录，未发现需删除的无调用入口。普通中文与显式命令仍走原权限／执行链，补全只填入不执行；11 文件共 300 项本地回归通过。
- [x] **5.3 本地验证与发布。** 相关 300 项和 Ruff、doc sync、严格尺寸、diff、clean-package 已通过；`689e83e85` 已发布 main，同一 wheel 双机部署与唯一 Gateway 核对完成，回滚证据保留。线上 CI 未作为验收来源。
- [x] **5.4 三路以上原生 TUI。** 168 管理、169 显式短／长分页、170 普通中文插件任务、171 本机核心与 bracketed paste、172 原会话滚动、173 插件重连后再调用均有原生证据；Tab 不执行，旧目录和错误命令不进入执行链。测试机非保留磁盘可用空间为零，重负载放本机。
- [x] **5.5 证据收口。** 原任务／工具账、产物和端点逐项读回；本片框架项无失败或缺证。旧模型文本失真保留归第 8 步，后续连续长任务不在本步冒充通过；开始第 6 步。

5.1 参考范围：本仓命令目录、参数绑定、补全、客户端和真实 TUI163 的只读回执；Codex 的插件弹窗异步刷新/变更失效局部、Hermes 的进程内发现入口、Free-Code 的插件命令加载局部。它们仅用于核对交互边界，不视为全库审阅，也不移植其缓存或加载状态。5.2 的使用卡只从现有包声明与设置 schema 派生：展示能力、显式入口、两条普通中文示例、必填设置和停卸命令；不另存配置值、模型语句或执行权限。
- [x] **6：故障与长任务。** 插件、核心任务、管理三路并行；覆盖阻塞、异常、取消、运行中卸载和恢复，不靠重启掩盖故障。框架证据与模型交付质量、测试准备失误分列，原故障与未知结果保留。
- [ ] **7：子代理拆分。** 分清状态、结果、收口和通知；实际验证单孩子、多孩子、主子孙、失败取消与长任务交接。
- [ ] **8：模型与工具循环拆分。** 分清请求、工具执行、压缩和结束；实际验证长文件、多工具、Compact、中断及恢复。
- [ ] **9：剩余 TUI 拆分。** 分清输入、状态和渲染；验证流式输出中操作、插件面板及停用清理。
- [ ] **10：十个插件组合验收。** 完成约定的十个自有简易插件，逐个及混装使用、装卸，全部卸载后再验核心基线。

### 汇报与验收规则

- 每次汇报固定给出：**完成的 TODO 编号／正在做的编号／具体阻塞（没有则写无）／下一项交付**。不以单测数量代替功能完成，也不重复扩大已完成审阅范围。
- 开发测试与真实 TUI 结果分别记录。真实验收使用官方 MiniMax-M2.7 并核对来源；视觉用 MiniMax-M3。长任务与多子代理覆盖按下文各步执行。
- 启动实际验收后先报告真实 TUI 编号、所在机器、tmux 会话与查看命令；未创建的会话不报成正在运行。原始日志、密钥和个人环境信息只存仓库外。
- 本清单只细化原十步 Goal，不缩小范围，不把本地实现改记为已部署或已验收。

## 当前实现与历史证据

当前片已本地接通逐次插件工作区读取上下文：复用原 PathAccessPolicy、exact 范围及授权根，
固定连接协商后经 `_meta` 运输，不修改业务参数或共享进程 cwd。SDK 和 workspace-peek 已完成本地标准构建、独立安装及 MCP 开发验证，
完整实际多 TUI 装卸尚未验收；不进入第 5 步，十步 Goal 保持 active。开发验证见 TESTS 最新节。
前一上下文切片 19 个相关文件 316 项通过；当前 SDK/样本片 12 个相关文件 254 项通过，分别保留证据。

第 4 步本地源码已接通安装、配置、实际启用、普通工具组合、停用释放、重新启用及卸载。
卸载使用释放返回的完整记录做原锁 CAS；原成功结果持久化并严格读回后，才消费退出证明和回收无人引用的旧包。
原准备仍在执行时保留安装与环境；同包重装保留其包，旧请求不控制新安装，UNKNOWN 不因后来清理成功被改写。
公开目录 v3 派生原提交安装引用，避免同包卸载重装后旧目录再次有效；安装表和原操作历史不新增权威副本。
显式 `/plugins@插件ID` 已本地接到原 HostCommand/ToolExecutor/MCP；工具参数和宿主选择分别绑定，业务用户不借管理员资格。
审批在原执行区间等待，明确批准一次后复查固定代次；重复提交只读原结果，单次连接退出与业务结果分别报告。
TUI/Gateway 的独立命令审批已本地接通，复用原 FIFO 与文件桥；规范 owner 由原服务端明确返回，不由客户端猜测。
断连只取消本命令，结果不明仍查询原编号；完整多 TUI 装卸尚待验收，未推送部署，不进入第 5 步。
前一片 22 个相关文件 511 项开发验证通过、源码无漂移；审批运输定向验证见 TESTS，完整十步 Goal 保持 active。

以下为前片提交及其独立验收记录，当前进度以上述段落为准。

第 4 步当前片：实际 enable 已接原管理执行器，准备隔离环境并完整核对 MCP 目录；候选退出确认后发布同代 active。
普通工具沿 Registry 新运行边界接入，可信 owner 固定、各权限视图目录独立，旧快照不能改绑新连接。
完整 session 清理证据在原资源账内单调保留；未发送拒绝不留下虚假 UNKNOWN，真正缺失退出证明仍保持未知。
尚待释放/消费、重新启用、卸载、显式插件业务命令与实际多 TUI；十步 Goal 保持 active，不进入第 5 步。
当前开发验证见 [TESTS](../../TESTS.md#第-4-步实际启用与新运行工具组合开发验证)，未发布部署。
47 个相关文件 1,018 项通过、源码无漂移，本地严格 gate 通过；组件通过不代替实际多 TUI。

前片：管理 disable 已接原运行与工具执行器；先撤销固定激活，再关闭原准备 attempt，冻结并清理两类准确资源。
阻塞调用隔离、独立 host 与 revoke/freeze 交错、原请求重放、坏引用及 UNKNOWN 保留已做组件验证。
退出证据和 revoked 激活仍保留；先核验资源再 release，原操作结果持久化后再 consume 的组合尚未完成。
该片当时尚未接 enable 与贡献发布；当前组合见首段，完整装卸与实际多 TUI 仍未完成。
本片验收见 [TESTS](../../TESTS.md#第-4-步管理停用与双归属清理开发验证)，不会将假启用夹具计作真实产品启用。
39 个相关文件 899 项通过，受测源码摘要无漂移；本片尚未推送或部署。

前片：固定激活已接到原托管 MCP 启动、host 创建 child 前、交接及排队后的发送准入。
插件连接使用原资源回执；清理提交异常和未接管启动的 UNKNOWN 保留，不能靠重连重启。
本次调用权限沿原 executor 上下文传到发送前，未发送的拒绝只结束本次调用，旧代理不跟随新连接。
该片仅本地内部实现；后续管理停用进度见首段，完整装卸与实际多 TUI 仍未完成。
最终 28 个相关文件 681 项开发验证通过，源码无漂移，本地严格 gate 通过，尺寸基线不变。
开发验证见 [TESTS](../../TESTS.md#第-4-步托管-mcp-与鲜活准入开发验证)，锁与清理边界见 [连接合同](../design/MCP_TRANSPORT_LIFECYCLE.md)。

前片验证分别保留：stdio/资源归属 523 项、唯一安装表激活 CAS 549 项；更早 MCP 拆分提交的全仓 18,265 passed
只证明各自受测源码，不能累计或移作当前完整插件验收。各片均未发布部署，完整十步 Goal 仍 active。

状态：用户已明确选择框架功能与模型交付质量分开记录，Goal 已恢复 active。第 1 步及第 2 步进度策略首片相关框架验收收口，交付失败和未覆盖项分列。供应退避次片与 197 项定向回归已完成；TUI 114—119 已验正常链路、原定时工作在连接失败后的冷却与自动续接、另一前台隔离及原设置回切。唤醒/Goal 路由及 TUI 120—123 所测框架链路已验；租约/恢复迁移的 324 项回归与独立复核通过。稳定资源换代误标脏的既有问题已修复，186 项相关回归通过并同包部署，该片当时双机运行 `f78ee8db2`。TUI 130 已验原请求有序恢复，133 已验取消后续代保留 STABLE 并复用原终端；最终任务和原解释器已自然收尾，第 2 步本轮框架范围收口，第 3 步公共声明与命名空间首片已发布并同包部署；实际 TUI 137 暴露的资源停止缺口已修复并由新版 TUI 138—142 分项复验；参数与宿主目录片也已发布部署复验，第 3 步本轮框架范围收口，第 4 步静态包与安装事实已进入本地开发，装卸链仍待完成。
本地环境准备已复用原操作声明和托管进程链：计划先领取、准备进程身份持久化，运行中复查权威，交接后仍受宿主与期限限制。
这是第 4 步内部准备集成；完整启用、工具发布、撤销及实际多 TUI 装卸未完成，不进入第 5 步。

整体顺序由 [合并重构计划](../design/MAINTAINABILITY_AND_JEV_REVIEW.md#下一轮结构整理顺序待实施) 维护；本文记录执行状态和详细验收。
插件合同见 [生命周期](../design/PLUGIN_LIFECYCLE.md)，样本范围见 [10 个简易插件](../design/PLUGIN_SAMPLE_ACCEPTANCE.md)。
第 4 步独立环境内部准备器已有本地源码和临时 venv/pip 组件验证，原安装表仍保持停用；
配置已沿原管理与工具执行链本地接通，值只进唯一私有安装表；v2 配置/版本/回执同次提交，旧 v1 明确迁移，目录 v2 拒绝旧安装版本。
上述早期准备片当时尚无启用接线；当前内部组合进度见首段，完整管理装卸仍未发布或实际 TUI 验收，见 [环境合同](../design/PLUGIN_ENVIRONMENTS.md)。

资源停止修复已按用户要求参考本地 Codex 的独立控制入口、关闭派工入口及固定集合清理，未运行参考项目测试。
沿原持久权威补齐 v2 启动交接、整树权限关闭和固定资源清单；direct/local 中断分路、准确 pending 与无数据库一次性接纳同时补齐。
旧登记入口、watchdog、宽泛保存和生命周期转发已删除；终态业务历史、UNKNOWN 锁与精确恢复边界保留。
源码 `6713c769f` 已发布 main 并同 wheel 部署双机，每端 1,210 个包文件一致，默认入口与各自唯一 Gateway 同版。
累计改动触发的全仓最终 17,642 passed、35 xfail、21 skip、5 个既有 XPASS，零失败；本地严格 gate 通过，线上 CI 未作为验收来源。
新版实际 TUI 138 验主资源三类控制与恢复，140 验三个并行孩子及 139 隔离，141 验交互 PTY，142 验真实孙代理停止；均核对原资源退出和文件稳定。
139 独立程序自然运行 900.534 秒，299 条原始记录和统计读回一致；脚本错误、未覆盖旁支与被控制中断的业务样本分列，详见 [TESTS](../../TESTS.md#整任务停止发布与实际-tui-138142)。
137 原失败保持，所测修复范围通过；后续参数次片独立验收，不提前进入插件装卸。
Working 图标偶现问题按用户最新安排延期至十步目标完成后一起排查；未复现，不额外阻断本轮。

## 目标和范围

在功能和持久数据正确的前提下，降低模块间隐含依赖，提高可读性、可维护性、可扩展性及实现清晰度。
交付可安装、可停用、可卸载的 Python 插件接口与 10 个实际可用的简易插件；普通可选插件装卸不重启 Gateway。
Audit/摄取重构、auth 和 Jev 不在本轮范围，原有功能保留；共享模块受影响的既有回归仍须通过。
不要为了减少行数而削弱有效注释；删除无调用旧入口、过期注释、空转发与废弃代码，同批迁移全部直接调用方。
模块只接收实际所需能力；保持唯一状态源、权限门、工具执行链、历史与恢复协议，不能出现同义执行器或大上下文换名。

## 发布与部署前置条件

1. 审查从远端 main 到候选提交的全部相关 diff，确认当前文档与行为一致，隐私和原始测试资料不入仓库。
2. 跑相关 focused tests；发布范围的生产/测试增删超过约一万行时追加全仓 pytest。Ruff、doc sync、strict code-size、diff 与 clean-package 均通过后才能推送 main。
3. 核对远端 main 最新值，按正常快进发布；不覆盖其他人的新提交，不强推。确认远端实际 SHA，线上 CI 单独记状态。
4. 同一提交构建一个 non-editable wheel，记录校验和；本机和确认的测试机使用同一 wheel，各自保存旧 runtime、启动配置与回滚证据。
5. 在停止 Gateway 前确认活动任务和资源归属；每台机器只有一个 Gateway，默认 TUI 和 Gateway 使用同一个新 runtime。原会话、工作目录、权限和个人模型配置保留。
6. 每台机器从默认安装入口打开至少两路真实 TUI，验证官方模型、工具读写及子代理，核对实际包位置与进程；测试输出由 my-agent 自行生成。
7. 发布和同版部署后创建详细 Goal，实际 TUI 验收作为第 1 步基线；框架和交付结论分列，推进条件见本文末尾，不能因已安装就进入第 2 步。
   单纯安装成功、端口监听或旧 TUI 的历史记录不能代替部署验收。

当前基线：main `104cdd000`，两台安装包逐文件核对 1,186 项一致；全仓 17,212 项通过、35 项既有 xfail、22 项跳过。
真实 TUI 四路已启动。文件任务与三子代理任务分别核对：本机三名孩子各自数据正确，但主代理合并保留重复表头，
最终报告仍称已验证；该场景记为失败，保留文件与日志，不由测试者补产物，也不为 CSV 样本增加专项核心分支。
其余实际任务和传输/子代理身份继续核验，不能将成功调用模型或状态完成当作全部产物验收通过。

首轮安装版实际结果：TUI 57 的订单数据、汇总和哈希通过；TUI 58 的三个子文件正确，但合并内容失败；
TUI 59 的数据、分组和哈希通过，报告将五项检查写成四项，报告质量未通过；TUI 60 的三个子文件、150 行合并、统计和哈希通过。
两端请求均从官方 MiniMax-M2.7 会话执行。启动/默认配置原文件未变，测试机通过实际 TUI 增加已有官方配置并只选择测试会话。
TUI 61/62 的主代理与三子代理持续采样已结束，实际数据与失败项见下文；不能用短任务或启动成功抵充长任务。

初步代码/日志核对：`tooling/operation_verification.py` 明确仅核验工具副作用，且提示操作成功不等于任务完成；
`tool_loop/deliverable_closeout.py` 仅核对孩子声明产物存在，不判断主代理合并内容。
失败现场的原生历史保存了直接拼接三个文件的命令和 153 行结果，没有执行对合并文件全部数值行的检查。
因此目前不能把失败说成工具结果丢失，也不能把副作用成功扩成内容通过；后续先检查现有通用验证仓库和收口证据接线，不新增专项格式门。
继续核对 `verification/project_facts.py`、运行记录入口、结果 reducer 及对应回归：现有被动仓库针对代码项目已声明的验证命令，
不会把任意命令、普通数据文件或模型报告变成验收事实。参考 Hermes `verification_stop.py` 也只对代码改动有界提示；不能据此声称已有通用内容判定。

长任务定位到可复现审批缺陷：创建生命周期把孩子挂到父会话，而原审批归属把这个 link 当作 main，导致后台命令未弹窗就返回 `APPROVAL_REQUIRED`。
原归属读取已改为 canonical child 优先，只有记录不存在才读取 main claim；不扩大权限，不把 capability grant 当成具体批准。
实际创建 link 的定向测试修复前失败，子/孙批准和拒绝修复后通过；修复 `61d8b4673` 已发布并部署双机。
审批修复的 124 项定向及本地严格 gate 通过，未做无关全仓重跑；两端新包逐项核对 1,186 项一致，默认入口与 Gateway 同版。
切换前检查活动 claim、请求和子代理 attempt，本机旧长任务结束后才切换；旧 TUI、历史状态、其它任务资源与私有配置保留。
TUI 63 的三个孩子和 TUI 64 的孙代理均实际收到审批，批准原调用后执行并正常回传；孙代理 wait 回执证实进程运行 90 秒后退出。
TUI 63 审批处理间隔与孩子自行重跑影响了启动时间，不能作为三路持续重叠通过的证据；采样与报告另验，不用审批通过抵消质量问题。
TUI 61 单进程采样 930.562 秒、94 条，时间、间隔、数量和哈希一致；158 轮模型、一次 Compact，等待成本异常保留。
报告未给足首尾各两条，内存单位与实际平台不符，并描述未用的采样接口，整体不算通过。
工具账中主代理查询进程 status 33 次、未使用 wait，两次长 sleep 超时；不能把模型未调用等待工具说成等待实现损坏。
TUI 62 三份文件最终各 62 条、约 900 秒；第三份由第二名孩子串行补做，三路共同重叠为零，未通过并发要求。
TUI 65 用原平方表需求复测：三个子文件、150 行合并、JSON 数值和哈希正确；模型自行修正 JSON 行数，报告却仍留含表头 152 行，实际 151 行，整体未通过。
TUI 66 重跑原十五分钟并行需求，生成程序缺依赖，主代理取消旧孩子并重新派工；未满足一次启动及保留原记录要求，本轮失败。
继续观察其恢复链，测试者未安装依赖或补产物；不能用重新采样的数据覆盖原失败结论。
TUI 66 恢复链现已通过实际 `/stop` 停止；A 的替换数据为 61 条、约 900 秒，C 的采样程序临近结束时高速写入，达到 610,557 条。
B 也出现高速写入，随后改用 PTY 重跑并覆盖数据，停止后只有 5 条；首次提交停止命令时新审批弹窗插入，未产生停止回执，
该段存在测试操作竞态，不能作为权限绕过证据。第二次停止后的三个最终数据 PID 均已退出，子目标随精确停止暂停。
拒绝同一后台命令后，两个孩子的 Goal 自动续轮又发布相同参数审批；已用真实 run/attempt 与参数哈希定位到拒绝列表在重建参数时丢失。
当前已发布修复由宿主在同一子代理 attempt 和前后台活动回合的 Compact 中传递原拒绝列表，不传递批准、不改变 Goal 控制语义。
确定性旧实现失败/新实现通过已经留证，参数变化与孩子隔离一起覆盖；529 项定向和本地严格 gate 通过，已同版部署双机。TUI 70 的真实自动 Goal 续轮再次调用同参时直接拒绝、handler 未执行，无重复弹窗。
TUI 70 后续换工具和参数绕开原拒绝，嵌套 Shell 后台启动产生数据，前台超时及 TUI 停止后仍有所属进程存活；最终自行退出，不记整轮通过。通用修复已本地实现：字面嵌套 Shell 后台语法前置拒绝，管道未结束时保留组长并核对其独立组成员；282 项主定向与 81 项相邻回归（含重跑）及本地严格 gate 通过，已同版部署双机；两端各 1,187 个包文件核对一致，未查询到该提交线上 CI。
TUI 78 原命令保留嵌套后台操作符，返回未启动且无数据文件；TUI 79 确实继承原输出管道并触发五秒超时，两次读回均为 6 条，后续宿主核对无该任务进程；此修复切片通过。
TUI 75 普通订单对照数据、分组、哈希及报告通过。TUI 73 删除后台操作符、TUI 74 新建输出管道，未覆盖预期条件；
TUI 76/77 的多行粘贴被分成多条用户消息，已拒绝终端申请并按实际任务停止，整轮不计验收；78/79 整段提交且原生会话核对各一条用户消息。
TUI 71 原平方表需求的数据与哈希正确，报告仍把两个源文件行数写错。TUI 72 三路各约 900 秒、62 条、共同重叠 899.59 秒，序号和哈希一致；
B 把 CPU 调度毫秒写成单调时钟，汇总漏报每路末尾近零间隔，报告只给首尾各一条且误称全部偏差小于 0.2 秒；整轮失败，原记录保留。
第 1 步清理已删除无生产调用的旧 verifier integrity 合同和自造数据测试，矩阵改指真实 verification 模块，22 项定向通过；不是报告质量修复。
进一步逐调用核对：TUI 71 的主代理实际读到两个源文件第 51 行，没有读取缺失或父级自动质量认证；报告仍写成含表头 50 行，模型表述失败保留。
另发现 Shell 回执末尾换行重复计行，三行哈希输出显示四行；已修正正文、截断说明与展示的共用计数并发布同版部署双机。
旧实现六项边界回归先失败，修复后 118 项相关定向与本地严格 gate 通过，不重跑无关全仓。
TUI 80/82 六种标准输出的原生回执与展示一致，82 同时覆盖标准错误，工具事实切片通过。80 漏 stderr，82 误把无末尾换行的文本当零行，模型履约/报告失败保留。
TUI 81 三个孩子分别产出 50 条，父级读回后合并 150 条，数值、哈希和报告一致；TUI 83 订单数据与校验通过但报告含表头文字写错。
新四路各一条完整用户需求，模型为官方 MiniMax-M2.7；没有重复长采样，也不把本次短回归抵充原长任务通过。
TUI 67 订单对照已完成，数字、分组、哈希和实际文件首尾一致；TUI 68 未触发审批，不能算拒绝测试。
TUI 69 新后台启动场景实际选择拒绝，原调用为 `APPROVAL_REJECTED`、`handler_executed=false`；只有脚本，无采样数据及替代执行，拒绝路径通过。

资源停止修复的当前发布和验收结论以本页首段及 TESTS 为准；以上各历史用例的失败、观察错误与未覆盖项不改写。

## 模型与环境约定

- 所有真实功能验收从实际 TUI 进入；普通中文需求只给一次，测试者观察并执行明确的控制/审批，不代做任务或补产物。
- 常规使用官方 MiniMax-M2.7；视觉使用官方 MiniMax-M3，复用 M2.7 的私有 provider/密钥引用，只切换模型名。核对实际端点、模型和图片输入事实。
- 用户已授权增加 my-agent 的 OpenCode deepseek-v4-flash，以及 Codex、Free-Code 的官方 M2.7 同题对照，细则见 [测试矩阵](../../TESTS.md#模型与框架对照)。
  本轮按用户授权并行执行，四组真实调用可用；TUI 99/100 的 my-agent 两模型短题通过，实际数据、原生工具和生命周期均核对。
  TUI 104 的正常 Free-Code 短题通过；TUI 101 的 Codex 数值/表头失败保留，TUI 105 单模型目录复测仍有汇总哈希错误，不能归因为 my-agent 独有问题。
  TUI 106 的 M2.7 长题再次提前汇总仍在增长的文件，重复启动、过期计数/哈希及未满窗口误判均保留；读取结果与实际文件前缀相同，不能据此声称工具丢数据。
  TUI 107 的 Flash 核心采样通过、报告覆盖有保留项；TUI 108 已因重复范围过宽控制调用中断并精确停止所属资源。
  TUI 109 的 A/B 仅采样约 600/615 秒，子代理提前结束触发宿主清理；C 完成 900 秒却误读 A 并申请覆盖重跑，已通过实际 TUI 拒绝后中断。原数据和全部失败保留，测试资源均已退出。
  准备错误和观察者中断单列，不豁免历史失败；框架基线是否可推进按实际证据单独判断。
- 测试可以放在本机或测试机；每个切片明确环境、源码/安装包 SHA、实际 Python/包路径和有效配置来源。日常默认模型不静默替换。
- 每步至少两路独立真实 TUI：一路运行受改动影响的任务，一路运行普通内置任务作为对照；控制、插件装卸及并发场景增加第三路或更多。
- 开始前报告实际 TUI 编号、tmux 会话/窗口及查看命令，不能提前编造编号；每台机器共用单 Gateway。
- 短任务验证后再运行有实际处理内容的连续长任务；涉及调度、子代理、工具循环及最终组合时，包含至少一轮约 15–30 分钟的持续任务，不能只靠空睡眠凑时长。
- 开发反馈使用合同、替身、回放与定向回归；这些与真实 TUI 分开记账，不能替代最终真实验收。
- 个人路径、账号、密钥、截图和原始日志在仓库外保存；仓库只写脱敏结论、测试名称和可复现的合成数据方法。

## 十步状态与逐步验收

### 1. 明确依赖与核心边界

状态：框架依赖及基线已收口；模型交付质量仍有失败和覆盖限制，不记为全部任务通过。
子代理 Goal 续轮遗忘拒绝的已发布修复单独验收，不能用它抵消报告和采样程序质量失败。
调度、子代理、工具循环、命令、注册和 TUI 的直接调用已定向核对；读取事实、持久写入、资源归属及不可变化的顺序见
[依赖清单](../design/MAINTAINABILITY_AND_JEV_REVIEW.md#第-1-步依赖与副作用清单)。框架基线的实际证据如下，不能只凭清单完成宣布通过。
明确插件描述、贡献所有权、激活代次、精确撤销和最小能力接口，先对照已记录的成熟参考，不预建空 SDK。
当前源码的[插件接线核对](../design/PLUGIN_LIFECYCLE.md#第-1-步接线核对与迁移约束)已补齐：确认现有 handler 快照、权限视图共享连接、操作领取前后边界及 Skill 文件校验；17 项已有合同回归通过。
新生命周期必须处理审批/资源等待后的撤销及准入与资源登记竞态；此处只记录依赖约束，未实现插件，不抵消真实长任务失败。
调度依赖补明 Gateway 常驻/CLI 每轮重建、规划与执行两次读取、线程诊断隔离及内外租约不同的异常收尾；只读复核未发现新的实现缺口。
只读复核期间仅更正 `registry_invoke.py` 过期的 readiness/掉线说明并补模块边界注释；该文件语法树与基线相同，没有改变执行逻辑。
TUI：普通文件任务、一个子代理任务、一个持续任务并行建立基线；核对模型、目录、原生历史、输出及控制响应。
通过条件：依赖与验收基线有实际证据，入口能回答谁读/写/取消，未将既有失败误记为通过。

#### 框架基线与交付质量分账

用户明确选择分开记录后，按已完成的原生历史、工具回执、持久状态和宿主资源核对收口框架基线。
这不是将失败改成通过，也不是以外部框架失败证明本框架正确。

| 框架范围 | 实际证据 | 结论边界 |
| --- | --- | --- |
| 普通任务、子代理与多客户端 | 双机 TUI 91—94，M2.7/Flash 同题 TUI 99/100 | 工具读写、父子身份、交接及收口已验；孩子自检覆盖仍单列 |
| 审批、拒绝与精确资源控制 | TUI 63/64/69/70、78/79 | 已发现缺陷完成通用修复、发布及真实复验；不扩大授权 |
| 工具输出与完成事实 | TUI 80/82、89 | 行数回执及声明窗口事实传递已验；不保证模型采纳或报告正确 |
| 持续任务和并发 | M2.7 TUI 106、Flash TUI 107 | 前者工具往返及当时文件读回完整、进程自然退出；后者三路各一次启动且持续至少 900 秒。不能把前者的多次启动、提前汇总记为任务通过 |

未证实新的 my-agent 框架缺陷；尚未覆盖的后续调度分支仍须在对应切片验收，不因分账自动豁免。
M2.7 的重复启动、错误程序、过期统计与哈希仍记交付失败；Flash 长题的报告覆盖限制保留。
外部 Codex/Free-Code 的模型行为、宿主清理及人工中断也分别留账，详见 [对照矩阵](../../TESTS.md#模型与框架对照)。

#### 前版长任务复验：TUI 84—86

版本仍为 `4f2ac93ad` 的同版安装包，没有修改提示需求或由测试者补产物。各一条原生用户消息，
本机一条十五分钟单进程任务，测试机三子代理十五分钟任务与普通订单对照并行；均为官方 MiniMax-M2.7。

| 场景 | 可核实事实 | 验收结论 |
| --- | --- | --- |
| TUI 84 单进程 | 首条记录后在关闭文件上 flush，工具返回退出码 1；模型随后删除记录、请求重启。重启被实际 TUI 拒绝，随后 `/stop` 有回执，原 PID 已退出 | 失败；程序错误和破坏已有记录均保留 |
| TUI 85 三子代理 | 三个原调用批准后各启动一次；A/C 孩子先自然结束，但后台程序仍持续运行。最终三份数据为 60/62/62 条，宿主退出码为 1/0/0 | 并行执行事实成立，整轮交付未通过 |
| TUI 85 A 路原因 | 约 900 秒时向已关闭文件写结束记录，日志有明确异常；记录窗口止于约 885 秒 | 生成程序错误，不是已证实的宿主提前终止 |
| TUI 85 汇总 | 父级未调用进程查询或读取日志；猜测宿主回收 A，并将 Goal 标为 complete。B 最大内存取值错误，实际间隔偏差未如实汇总，首尾各两条缺失 | 模型核对、解释和完成判断失败；Goal complete 不是通过证据 |
| TUI 86 普通对照 | 30 条订单、分组三组、总数、实际哈希和首尾与报告一致；模型自己执行校验程序，退出码 0 | 此场景通过，不能抵消长任务失败 |

补充读取 `process_access_scope`、`ProcessSessionTool`、子代理交付索引和自然收口合同：父子处于同一
后台会话查询范围，已有 `list/status/wait` 和日志引用可查；当前证据没有显示这些事实入口被框架阻断。
参考定向读取 Hermes `tools/process_registry.py::wait` 的退出、等待和输出快照边界，没有复制策略或声称整库审阅。
持久进程索引中的旧 running 行不能单独证明仍活着；本轮通过实际 PID 和宿主退出回执核对，三个程序均已退出。
当时未新增业务质量硬门、CSV 分支或自动改写报告；原整轮失败保留，当前推进口径见上面的分账记录。

继续核对实际原生上下文：完成正文预览曾缩至约 99/174 字符，截断标记与完整报告路径保留，三个报告均存在且含完整交接；
三份生成脚本的 `write_file` 参数与磁盘逐字一致，未发现缩进被写入工具改坏。
另定位到既有窗口事实在活动回合事件和后台完成清单丢失；已本地补齐同一中性字段筛选，原始剩余秒数跨裁剪和后续轮保留。
四项旧实现失败，修复后 446 项相关回归通过、4 项既有 xfail；实际旧事件回放从零条恢复为两条窗口事实，原三个 DONE 不变。
这是事实传递修复，不代表已观察完整声明窗口，也不代替生成程序与报告质量验收。严格 gate 通过后已发布并同版部署双机，默认入口与单 Gateway 一致，配置及回滚版本保留。

#### 新版窗口事实和并发回归：TUI 87—90

四路从实际默认 TUI 进入，均为官方 MiniMax-M2.7、各一条普通中文原始需求；普通对照及三子代理长任务沿原需求，阶段结果任务独立记账。
两端同一 wheel、各 1,187 个包文件核对一致；本轮未新增配置或依赖。实际编号和 tmux 查看命令已在提交需求前报告，原始证据只留仓库外。

| 场景 | 可核实事实 | 验收结论 |
| --- | --- | --- |
| TUI 87 阶段结果 | 模型传入持续 Goal，未配置声明窗口；孩子写完目录数量后反复模型续轮，未交报告。实际 `/stop` 后孩子取消、runner 结束、原 PID 退出 | 履约失败；未覆盖窗口字段，不以停止后的状态充作自然完成 |
| TUI 88 普通对照 | 30 条数据、公式、分组、哈希及报告内容正确；校验只在内联命令执行，没有保存最终回复声称可运行的脚本 | 完整交付失败；测试者未补文件 |
| TUI 89 三子代理 | 三个原采样程序各启动一次、均自然退出 0；A/B/C 为 61/60/62 条，记录持续 900.214/885.903/915.098 秒 | B 程序只安排 59 个间隔，未达到十五分钟；完整任务失败 |
| TUI 89 窗口传递 | 三名孩子原完成观察的 962/799/100 秒进入后台原生完成清单，正文裁剪及后续消费不丢失或重算，原 DONE 保持 | 实际后台消费切片通过；前台安全点仍由本轮自动回归覆盖，不冒充已触发真实路径 |
| TUI 89 最终汇总 | A 尚在运行时读取中途记录，报告称其在 51 条时提前终止；最终 A 为 61 条且哈希不同。报告将不足十五分钟的 B 判为达标，重叠和部分间隔也算错，首尾各两条不足 | 等待、数值核验和报告判断失败；原报告保留 |
| TUI 90 普通对照 | 30 条订单、三组汇总、哈希、首尾和报告一致，实际校验脚本存在且由模型执行退出 0 | 此场景通过 |

TUI 89 的 A 路首次命令包含后台操作符，已有通用 Shell 合同返回未启动；模型自行修参后再经 TUI 单次批准，未替它修改程序。
声明窗口只是冻结的协作事实，不等于采样时长；是否达到原持续要求仍须依据实际文件、进程回执和报告复核。
父级实际调用过 `process_session list`，返回同会话内的运行进程和日志引用；没有出现父级无法查询的权限失败。
其后没有按实际进程终态等待就交付报告，记录共同窗口实际为 698.865 秒，报告却写 859.9 秒；此统计也不能混同 OS 进程精确存活窗口。
A 的序号 18→19 间隔实际约 15000.497 毫秒，报告写成约 1001 毫秒；B 的两处报告间隔同样不符，不能称为如实保留异常。
原生主级 24 组、三个孩子 5/9/4 组工具往返成对，三份脚本的写入参数与磁盘逐字一致，未由测试者修程序或补数据。
两端本轮 claim/请求队列均已空，原三个采样进程已退出；TUI 保留供查看，未关闭无关任务。
本轮文档验收另有 6 项定向及本地严格 gate 通过；生产修复沿用同一代码的 446 项回归和 4 项既有 xfail，不重跑无关全仓。线上 CI 未作为验收来源。

#### 只读复核与失效诊断清理

继续核对 TUI 89：后台进程自动完成通知按现有合同只面向主会话启动的命令，孩子内部命令由孩子等待或查询，再经父子交接。
三个命令没有主会话通知目标符合 `process_events.py` 与长任务合同，不能据此判定通知丢失。
父级六次带行号的 `read_file` 回执均是最终文件的精确前缀；最后一次 A 读取仍为中途 51 条，之后程序继续追加。
这支持“读取时文件尚未完成”的归类，没有发现工具漏掉当时已有尾行的证据。原长任务失败保留，框架判断不扩大为交付通过。

本地清理仅删除已停用 `SUBAGENT_RESULT` 修复诊断场景、专用替身、调用清单及被跳过的旧验收；正式 finalizer 只传宿主结束事实。
相邻 runner 重试场景迁为独立命名的两个文件，11 个保留定义的语法树与原实现一致，原有 xfail 未被改成通过。
正式参数解析覆盖已删除和未知 case 拒绝；自然回复、宿主结束与场景相邻回归合计 24 项通过、3 项既有 xfail、1 项既有 skip。
另有 6 项文档定向通过，本地严格 gate 通过后发布 `33751e66c` 并同包部署双机；每端 1,187 个包文件逐项一致，默认入口及各自单 Gateway 同版，配置未改且旧 runtime 可回滚。未查到该提交线上 CI，线上 CI 未作为验收来源。
更大的结果持久化与生命周期拆分仍在第 7 步处理。

#### 诊断清理安装回归：TUI 91—94

每端两路独立实际 TUI，均选择官方 MiniMax-M2.7，各一条普通中文原始需求；启动后、投递前已报告编号及 tmux 查看方式。
两路单子代理和两路订单对照并行运行，测试者只读取日志与产物，不代写或代跑业务校验。

| 场景 | 原生及产物事实 | 结论 |
| --- | --- | --- |
| TUI 91 本机单子代理 | 父级 3 组、孩子 2 组工具往返成对；孩子写 1—10 数组，父级读回后保存 count=10、sum=55；孩子自然 DONE 且活动 attempt 清空 | 通过 |
| TUI 92 本机订单 | 12 组往返成对；30 条公式与三组汇总、哈希、首尾及报告一致，实际 validate.py 执行退出 0 | 最终交付通过；前两次校验退出 1 的历史保留 |
| TUI 93 测试机单子代理 | 父级 4 组、孩子 3 组往返成对；孩子写数组与磁盘一致，父级读回并正确保存 10/55；孩子自然 DONE | 通过 |
| TUI 94 测试机订单 | 9 组往返成对；实际数据、汇总和报告一致，validate.py 运行退出 0；报告按物理行包含表头列出首两行 | 通过 |

TUI 92 初版汇总数值错误，生成校验器还对 DictReader 结果再次跳过首条；模型先修正汇总，第二次仍失败后再修校验器，第三次才退出 0。原 CSV 只写入一次，最终汇总与全量数据的独立复算一致。
两端订单各 30 条，总量 1605、金额 14625，SHA-256 一致；四路原生用户消息均为一条，全部结束后队列和有效 claim 为空，原 TUI 保留。
这是删除 CLI 诊断入口后的安装与核心短回归，不替代既有十五分钟任务、不关闭剩余质量失败。

### 2. 拆后台调度

状态：进度策略首片、供应退避次片与路由片所测框架链路已收口；唤醒/Goal 路由同版部署后，353 项相关回归和 TUI 120—123 的实际链路通过，模型交付失败单列。租约/恢复及既有资源状态问题的修复已部署，实际恢复证据及 133 的完整任务与资源收尾已核对，本轮框架范围收口；各片未命中的内部支路与模型失败保留。进度策略的无进展计数与确定性失败退避迁入 `conversation/background_progress_policy.py`；只接收标量事实，不持有 Agent、Store、时钟或可变状态。
原两处调用及已有测试直接迁移，旧函数和专属导入删除。仍在回合后读取原 metadata，失败与成功持久记账、三次失败退休和供应错误分类保持原位置。
已有后台运行与供应退避两组文件共 168 项回归通过；去除旧说明、重命名常量及标量投影后的算法体与基线语法树一致，300/3600 常量未变。只读复核确认仍在执行后读取 metadata，无新 Store 查询或旧入口残留。
本地严格 gate 通过后已普通快进发布 `680b7b4e8`，远端精确 SHA 已核对，线上 CI 列表为空。同一 non-editable wheel 部署双机，每端 1,188 项包文件一致，切换前无活动 claim 或排队请求，原配置和回滚保留。
新安装版 TUI 111—113 均各一条普通中文需求，官方 M2.7 共 28 次实际请求，无失败/重试；工具配对、读回、父子交接及自然收口已验。
112 子代理交付通过；111/113 订单结果正确，但校验器覆盖与报告措辞有质量问题，分开保留。这三路没有 progress policy 记录，内部策略支路仅记自动回归覆盖，不冒充真机命中。
TUI 110 原需求一次提交，三个程序各启动一次，实际记录跨度 1339.199/1352.899/1430.275 秒，共同记录跨度 1284.564 秒；后台 host 自然退出 0、原 PID 消失，TUI 和唯一 Gateway 保留。
父级完整读回三份 CSV，逐字与磁盘相同，实际 hash 命令结果相同；主子 39 对工具均匹配，root task、三个 child task 和执行 claim 自然收口，完成事件接续与最终投递已验。
框架观察范围通过，交付仍失败：脚本用系统时间冒充单调时钟，CPU/最大内存并非要求的采样进程事实，报告两处间隔计算错误。含 END 的报告计数正确，JSON 口径混用及首尾仅各一条单列歧义/覆盖限制。
审批准备曾等待约 11—12 分钟；只通过实际 TUI 分别批准原调用一次，没有补需求、代写产物或停止程序，该等待不计采样时长。
此路也未产生对应 progress policy，纯函数支路只记算法等价与自动回归。此首片当时未覆盖供应状态、唤醒和租约迁移，后续供应切片另列，不提前进入第 3 步。
供应次片将共享冷却状态、消费守卫及日志迁入 `background_supply_backoff.py`，配置仍由 runtime 在 scheduler 构造时读取；三类消费和就绪扫描保持同一实例。移除旧定义与空 TYPE_CHECKING，纠正额度耗尽的过期注释，配置值不变。
供应/后台/直属父级 188 项及相邻 9 项通过；执行体与剩余 runtime 的 AST 等价，独立复核确认 typed 分路、失败时刻、None 及假值报告、读取与写账顺序保持。
代码 `ef9db100d` 经本地严格 gate 后正常发布并同包部署双机，每端 1,189 项一致，配置原样且保留回滚；精确提交的线上 CI 列表为空，未作为验收来源。
TUI 114/115 各一条真实需求，官方 M2.7 共 18 次成功请求、零模型/网络重试，16 对工具匹配；本机单子代理与测试机普通订单的读回、归属及自然收口通过。
两路最终交付通过；115 初版金额错误经同一校验程序实际报错后由模型自行修正，历史错误与校验覆盖限制仍保留。两路未命中供应失败分支，不复用首片或正常任务证明实际退避恢复。
TUI 116 先完成透明 TLS 转发基线；117 只提交一次定时需求，在无其它待运行工作的窗口精确暂停本轮专用代理，原后台 attempt 经 608.895 秒既有重试后触发 typed 供应退避。原 run/wake 保留，冷却 30 秒后由第二代 attempt 自然续接，17.374 秒后完成；期间 Gateway 不重启、无追加业务需求。
TUI 118 的整个普通前台任务在 117 冷却截止前 5.760 秒自然结束；这证明本次前台隔离，不扩大为其它后台入口或跨 owner 公平性。117 的平方表、汇总、哈希及报告正确，时区和报告转义两处模型自行修正及修复后未再读回报告的限制保留。
一次性 job 按既有合同保留为 paused/one_shot_finished，原 run 归档且仅一条；失败 attempt 与用量不清空。系统网络、系统代理、默认路由和 SSH 未改；只在自然结束且空闲后回切原配置、完整环境并关闭专用代理，新开 TUI 119 官方模型读写通过。细节和未测分支见 TESTS；本轮不充作活动任务的 Gateway 重启恢复。
路由片已迁出 Goal 异常结算/续跑和后台地址选择，窄依赖明确列出三个持久领域与精确回调；旧 GoalMixin 与旧路由方法删除。观察执行保留独立调度函数，能力预扫归原 Wake；不改模型上下文、去重发布入口或持久格式。
353 项相关回归和独立审阅通过，路由默认目标、优先级和惰性读取已补合同覆盖；尺寸守卫无 hard，未放宽基线。参考源码范围与安装版覆盖见设计和 TESTS。
路由切片当前部署 `f9a1088d3`，两端各 1,191 项包文件核对一致，原环境、配置及除 runtime 外的启动参数未变。TUI 120 的同一程序采样 960.000 秒、97 条，暂停/恢复、3 次 Compact、全部执行代次及自然退出通过，报告统计与内存单位失败保留。121 工具链和最终数据通过、校验器与报告失败；122 三子代理并行及父级汇总链路通过、提前读取违反需求；123 定时唤醒与交付通过。
四路业务已自然结束，原生工具与精确归属均已核对；双机无活动 claim 或待处理请求，历史 TUI 保留。该路由片所测框架范围收口，开始租约/恢复的下一片准备；其它通道回退、异常分路和活动任务重启恢复仍须按相关合同及新安装版实际验收，详细分账见 TESTS。
租约/恢复已同版部署：claim 领取/结算与恢复查询/日志指纹分别迁入独立模块，原私有执行方法删除；原共享心跳和三类持久租约保留。25 项新增合同先在旧实现通过，迁入新入口后补其它来源与日志边界，最终 324 项定向回归及独立复核通过；新版 TUI 124 的真实续租、客户端重连和暂停续采已验，125/127 普通任务链路已核对，模型交付失败另列。128 已证同车道排队与释放后接手，不扩称内部 acquire 拒绝分支真机命中。126 的稳定资源误置 DIRTY 已收窄原事务条件，186 项相关回归通过并同版部署，原失败和历史脏账保留。130 在零工具副作用时完成原请求的有序 Gateway 恢复；133 的 STABLE 记录在取消后续代保持，并实际复用原 PTY，旧等待操作 UNKNOWN 保留。131 未派工，能力申请分支未覆盖；130 的产物目录和 132 的校验/读回失败仍单列，133 自行退出原解释器，27 对工具、26 次官方调用、锁清理和完整覆盖边界见 TESTS。

按纯准入/退避、进程内退避状态、唤醒/Goal 路由、租约/恢复依次小片迁移；不改变读取时机、锁、持久格式和写账顺序。
TUI：持续多文件任务与普通问答并行；Goal 暂停时当前回合可继续，中断回合与停止任务资源分别验证；恢复后不重复启动旧进程。
补充定时/唤醒、供应错误恢复、两任务竞争与隔离；会话断连重连、Gateway 重启恢复按同一实例顺序进行。
断连测试只作用于明确归属本轮测试的连接或进程；不得关闭系统网络、网卡或 Wi-Fi，不得修改路由器、系统路由或全局代理。旧专用代理实验已结束，不重跑旧控制程序。
通过条件：原状态及资源归属正确、无框架造成的重复执行/漏唤醒，真实持续与并发链路有证据，旧直接入口删除。模型未履约单列；必测框架分支未命中或归因未明时继续补证，不算通过。

### 3. 公共命令声明与解析

宿主目录片 `3730497fc` 已发布并同包部署双机，每端 1,219 个包文件一致，各一个 Gateway；669 项相关回归与本地严格 gate 通过，1 项既有跳过。TUI 151—153 所测输入、命令隔离和普通主/子工具链通过，完整证据及模型交付失败见 TESTS。第 3 步本轮框架范围收口，第 4 步尚未开始。`/client/plugins` 和原 HTTP 命令分路共用可信 owner、
完整声明及内容版本；客户端按显式模式读宿主，原候选版本不随参数补全升级。当前贡献为空，不以此代替装卸和执行权限验收。

状态：公共声明与命名空间首片已发布并同包部署，实际控制验收暴露的停止缺口已修复复验；结论见[命令首片边界](../design/PLUGIN_LIFECYCLE.md#第-3-步首片接线与实现边界)。参数与补全修复 `faa12068a` 已发布 main 并同 wheel 部署双机，483 项相关回归及严格 gate 通过；当时动态 owner 目录与执行身份仍待接线，后续宿主目录片见本节首段；实际插件执行授权仍待生命周期实现。HTTP、文件队列与 TUI 共用命名空间判据，不新增插件控制类型。

实际 TUI 134/136 的保留命名空间和普通工具链已验，模型校验器及读回缺口保留；135 验暂停保留执行，未测全部控制。
137 另验恢复和活动中断；明确 `/stop` 后原程序仍运行至少 265 秒，后来才自然结束，停止资源失败不能改为通过。
修复已沿受管后台进程合同补齐执行归属、跨进程启动/停止竞态及固定子树清理，并校准 direct/local 中断边界。
同版部署后的 TUI 138—142 分项覆盖主后台、三孩子、PTY、孙代理与独立长任务隔离；源码竞态、模型失败和实际旁支边界见 TESTS。
本轮所测停止链路通过，继续参数声明；旧 TUI 137 不改记为成功。
参数次片已接入源码：同源动作/参数声明、字面词法、完整及部分绑定、静态帮助、CLI/HTTP 回执与 TUI 补全。
实际 TUI 143 发现 Enter 被可选候选截获；新版已区分自动补全与显式 Tab，146 的原生 Tab→Enter 复验通过，原失败保留。
本机 146 与测试机 148 各 13 类命令均静态返回且未创建任务；146 文件任务和 147 两孩子并行通过，148 数据正确但校验器漏检，分层结果见 TESTS。
结构化参数已可表示显式宿主描述的动作目标，但当前实际目录尚无热装卸贡献；不伪造已安装插件或执行结果。
149/150 已验普通正文原样保留插件示例句；150 已验活动请求首秒的错误/帮助静态分流，同一 attempt 自然完成，无模型插话或 Shell 副作用。
参数片所测框架范围通过，测试准备失误、校验器限制与完整计数见 TESTS；后续宿主目录片已接通声明与原版本提交，真实激活代次及执行身份留第 4—6 步。
实现 `/plugins` 管理与 `/plugins@ID [动作] [参数]` 的结构化公共解析，身份由宿主绑定；帮助与参数从同一声明生成。
TUI：两路分别测试有效命令和普通聊天；覆盖未知插件/动作/参数、缺值、引号、空格路径、短开关、`--`、中文和错误恢复。
核心 `/goal`、`/interrupt`、`/stop`、`/model` 仍可正常使用；错误插件命令不能落入聊天或 Shell。
通过条件：TUI/plain/Gateway 判据一致；未实现的业务入口不假装可用，原控制语义无变化。

### 4. 首个只读插件与装卸链

状态：开发中；本地已有静态包描述、有界归档校验、默认停用安装表、并发 CAS 及原子提交回执，未发布部署。
管理授权、HTTP/direct 命令、配置验证、隔离环境、启停释放、重新启用与卸载已有本地组合；完整目录匹配与普通工具调用已做组件验证。
显式业务命令、原执行区间内的审批恢复及 TUI/Gateway 交互审批运输已在本地接通；尚无本步新增真实 TUI 验收，不能据此进入第 5 步。
显式宿主命令登记、执行和收口沿原 RuntimeDB 与工具执行器；新旧代理共用运行树创建，开关关闭后只读查询仍可用，完整装卸未发布。
重送不换代、不新建模型轮；终态重放保留原执行事实、本次未执行，UNKNOWN 不被当作无副作用，详见 [宿主命令身份](../design/HOST_COMMAND_EXECUTION.md)。
原 Repository 未启动取消的领取信息已在本地保留，三类收口之后可精确回读；损坏结果保持原文并拒绝回放。它是持久记录开发验证，不是新的实际 TUI 漏停复现。
原操作重放、提交前后异常、双故障、跨 owner 和锁路径已做开发合同验证；包读取不等于真实 wheel 安装，进程隔离不等于安全沙箱。
详细合同与参考边界见 [本地插件包](../design/PLUGIN_PACKAGES.md)。
以 workspace-peek 实现显式本地安装、默认停用、配置校验、隔离执行、贡献原子发布、版本绑定及精确撤销；依赖不污染核心环境。
TUI：管理会话完成安装/启用/调用/停用/再启用/卸载，另一会话持续执行内置文件任务；核对未安装、停用、启用及卸载后的目录。
覆盖包/配置无效、启动失败、重复管理请求、权限拒绝、旧快照撤销和进程退出事实。
通过条件：完整功能实际运行，管理不被业务调用阻塞；卸载不影响其他任务、历史或用户产物。

### 5. TUI 发现与使用链

状态：待开始。
实现插件列表、逐级补全、静态帮助、配置和使用卡；普通中文与显式命令共用工具/权限链。
TUI：一路中文请求使用文件预览，一路输入显式参数，第三路切换启停；验证目录刷新、停用帮助、过期目录及重新连接。
同时检查编辑、粘贴、中文输入、滚动、复制和长输出翻页，补全选中只填输入不执行。
通过条件：两种业务入口结果一致、参数/帮助/目录同源，核心输入与日常命令保持正常。

### 6. 插件故障与长任务

状态：待开始。
三路及以上 TUI 同时运行插件长任务、内置长任务和管理操作；受控测试包制造异常、阻塞、断连及清理失败。
验证运行中停用/卸载、等待审批时取消、重连不能复活、重复卸载、错误后再启用，核对专属进程及订阅清理。
通过条件：管理与无关任务可用，装卸不重启 Gateway，已发生副作用和未知结果如实保留；失败修复并重测后才进入下一步。

### 7. 子代理生命周期与交接

状态：待开始。
分开状态裁决、结果投影、持久收口及父级通知，保持 run/attempt 身份和唯一终态来源。
TUI：覆盖单子代理、三个以上并行子代理、主子孙递归、父级等待时插话、孩子失败/取消、结果通知重复及恢复后继续。
另一路主任务持续运行；核对并发重叠、实际文件、原生工具账、父级交接和取消后的资源退出，不能仅看模型总结。
通过条件：正常等待不误标失败；无漏交接、冒用身份或重复执行；长任务完成且产物与报告一致。

### 8. 模型与工具循环

状态：待开始。
沿现有 tool_loop 分离模型请求/响应、工具轮执行、Compact 协调与结束决策，不新建模型执行器。
TUI：多轮长文件处理、工具报错后修正、多工具/多子代理混合任务、Compact 后读回旧事实、回合中断和断线恢复。
对照一路普通任务；涉及视觉的片段用同密钥引用的 MiniMax-M3，核对图片进入实际请求及工具结果，而非只看模型名。
通过条件：历史成对完整、压缩提交正确、权限及取消不旁路；结果、用量与执行事实各守原权威来源。

### 9. 剩余 TUI 整理

状态：待开始。
分开输入/快捷键、状态消费和渲染；插件仅提供受限展示描述和可撤销只读订阅，队列有界。
TUI：多任务流式输出期间反复切会话、滚动/复制、输入中文/多行文本、审批、中断、重连；同时打开/关闭插件面板。
验证插件事件乱序、订阅积压、渲染错误和停用不会冻结核心输入或残留回调。
通过条件：屏幕读公开事实，输入和控制持续可用，插件代码不在 TUI 进程中任意执行。

### 10. 十个简易插件与最终组合验收

状态：待开始。
按样本计划完成 workspace-peek、context-inspector、savepoint-lite；worktable-lite、activity-line、status-pet、genui-lite；design-lite、image-text、browser-lite 三批。
每个包都实际安装、配置、使用、停用、再启用及卸载；提供工具的包增加普通中文调用，展示包直接用 TUI 操作验证。
组合长任务覆盖文件分析、页面/图表生成、图像理解、浏览器表单、多子代理汇总；同时另开内置任务和管理 TUI，逐个及批次卸载。
视觉使用 MiniMax-M3 并复用现有密钥引用；外部资源使用测试网页、合成图片与专属浏览器上下文。
通过条件：10 个包各自有真实结果；混装不串用户或资源，全部卸载后的新会话核心基线通过，原产物和历史保留。

## 每步推进与完成规则

每片记录源码/部署版本、结构变化、相关自动验证、真实 TUI 身份、模型/端点、任务时长、工具/子代理事实、产物读回及失败项。
每步完成前复核依赖方向、公共接口、重复状态、废弃代码、文档、配置及隐私；修复本片发现的相关问题并对受影响场景重测。
用户已确认两条验收记录：框架功能记通过/失败/未覆盖，模型交付质量记通过/失败/覆盖受限；原始整轮结果同时保留。
本步必需的框架检查未通过或缺证据时不进入下一步；归因未明且可能影响框架正确性的失败继续定位，不能默认归为模型问题。
已核实的模型程序、计算、核对和报告失败可单列后继续结构重构，不再要求反复重跑直到模型全部答对；仍须完成相关真实多 TUI 与长任务框架覆盖。
不能改报告、扩大尺寸基线、代写产物或用自动测试冒充真实 TUI 来宣布通过。
与本步无关的既有问题单列，不伪称本次重构已修；若影响必测主链，先按通用层定位修复。
发布时严格 gate 通过后才更新远端；需要更新运行代码时构建同版包并有序部署，不把修改源码目录等同安装版升级。
Goal 暂停只停止目标驱动的自动续跑；中断回合停止当前模型/工具执行链；停止任务资源只关闭精确归属于任务的进程、终端或子代理。
完成全部十步且证据齐全后才能将 Goal 标记 complete；遇到缺失信息按真实阻塞规则处理，不因费时或上下文不足停止推进。
