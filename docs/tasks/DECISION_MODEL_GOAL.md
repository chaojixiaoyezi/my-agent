# 决策模型完整执行 Goal：P1—P5

状态：active（2026-09-23 起由用户指定的接手代理继续；原代理按用户要求暂停并留下[完整交接报告](DECISION_MODEL_TAKEOVER_HANDOFF.md)）。完整 P1—P5 目标不缩减；接手后的 Goal、并行边界与顺序调整见文末“2026-09-23 接手记录与当前 Goal”。
设计合同：[决策模型接入计划](../design/DECISION_MODEL_INTEGRATION.md)。本文件记录阶段完成条件与证据，不是运行时事实源。
当前阶段：01—11 已完成各自本地切片，12 窗口/缓存已完成本地与真实验收；13真实接口、14召回前与记忆写入建议、15其它决策点、16主会话模型选择、17自主对照和18最终集成进行中。隔离 Gateway TUI 的 Jev/官方 MiniMax、子代理自动采用官方 M3 与 OpenCode DeepSeek 已各有真实链；主会话 apply 又验了超时、need_data 与选择原模型的安全保留，真实异模仍待验；14/15/16/17 各有首片，但整体均未验收。早期全仓回归已修本线回归、旧测试调用和决策错误码漏登记；13 处导入层级问题现已移位消除，完整严格 gate 仍未过。

## 可见进度 TODO（2026-09-22，持续更新）

**当前：12 项完成、6 项进行中、0 项未开始，共 18 项。** 12 已完成本地与真实验收；13已实际调用Jev；14-B/15-C/16-A/17-G0 的首片已本地接通；18 的早期集成 gate 已开始但未通过，均不能按整项完成计算。发现的问题修完后复测，不以少量连接通过作为完成。
不按代码行数或测试数量计算完成比例；组件通过不等于菜单、业务入口或真实使用已完成。
本表是下文完整清单的进度投影，不新增另一份 Goal 或任务账。

| 顺序 | 状态 | 具体交付 | 完成时用户能得到什么 / 验收点 |
| --- | --- | --- | --- |
| 01 / P1-A | ✅ 已完成（本地） | 决策模型配置与普通 LLM 分开 | 可保存 decision 配置，主/子 LLM 不会误选；提交 `17530b518` |
| 02 / P1-B | ✅ 已完成（本地） | Jev 原生协议与短期限 HTTP | 不安装 harness，能发原生请求；坏响应/超时拒绝；提交 `4e33f7593` |
| 03 / P1-C/D | ✅ 已完成（组件） | 有界等待、准确取消、资源保留 | 超时即返回，旧请求未退出不堆积，给普通 LLM 留名额；组件已联合验证并通过本地严格 gate |
| 04 / P1-E | ✅ 已完成（本地服务） | 一套设置服务 | 总开关、逐点模式、可调秒数、恢复继承及 CAS 读回；原 user_config 已接通；完整菜单属于 11 |
| 05 / P1-F | ✅ 已完成（本地组件） | 原账本与用量 | 终态不复活、真实 worker 精确保留、快照补账；同一行显示决策输入，输出留白、无价格、未知不补零；真实调用接线归 06 |
| 06 / P1-C/D/G | ✅ 已完成（本地服务） | 第一条可用决策链 | 原配置→本地 HTTP→原账本→活动 TUI 用量行组合通过；冷却、在途关闭及写锁争用已验，见 P1-F/G 交接 |
| 07 / P2-A/B | ✅ 已完成（本地业务接缝） | 子代理创建前选模型 | 已接建议与显式优先/幂等合同；最终必须由宿主自动核验并选定模型，无逐子代理用户操作；完整容量证明及真实异模派工归 12/13，尚未验收 |
| 08 / P2-C/D/E/F | ✅ 已完成（本地业务） | 记忆整理前置标注 | 原 Curator 临时建议与提取/提交/游标组合通过；关闭、非选择、失败和 lease 头寸对照见 Curator 交接 |
| 09 / P3-A/D | ✅ 已完成（本地业务） | 召回后重排与结果复用 | 在原权限/来源内排序，关键材料不丢，失败恢复原顺序；复用不另建记忆库 |
| 10 / P3-B/C | ✅ 已完成（本地业务） | Skill/tool/插件能力推荐与实际减量 | 原prompt/native schema减量、搜索恢复、插件撤销、HTTP超时及在途关闭已验 |
| 11 / P4-A/B/C | ✅ 已完成（本地及一条真实中文链） | 原设置入口和 agent 代操作接线 | 可新增/编辑/测试/开关/调时间；普通 user owner 原工具可见性、可信线程与 CAS 预览已修，真实中文 read→thread patch 成功、事务回执含生效值并准确回复；其它 owner/故障组合仍归 13 |
| 12 / P3-E、P4-D/F | ✅ 已完成（本地及真实验收） | 窗口、缓存与并发故障验收 | Jev两层容量门与目录代次已本地通过；原生计量/主子采用153项、三宿主展示接续及相关回归236项通过；同轮不重决策、新轮重置、后台真实身份已验；12.1—12.7 全部完成：12.4 的 2a、2b、流式读取、媒体集成、媒体越压缩点修复都已合入并真实验收，请求准备阶段压缩也已在真实 TUI 触发（main `3933b1db1`，见[真实验收](DECISION_MODEL_REAL_VALIDATION.md#124-请求准备阶段压缩的真实触发2026-09-24main-3933b1db1)）；媒体屏障主链（旧媒体归档引用，片 A）已由主线 owner 合入 main `6bb0467b1` 并真实验收，视觉摘要（片 B/C）是增强，由主线 owner 的[媒体压缩策略](../design/COMPACT_MEDIA_POLICY.md)跟踪 |
| 13 / P4-E/G | ✅ 已完成（真实验收与汇总报告） | 真实 Jev + MiniMax TUI 验收 | 隔离 Gateway/官方 M2.7、子代理官方 M3 与受限候选 OpenCode DeepSeek、普通中文配置 read→patch、主会话真实跨模型采用都有成功样本；多 owner 下的断网、挂起与真实端点瞬时故障有真实样本；挂起时每个冷却窗口后多付一个期限的问题已改为连续失败翻倍冷却（main `3933b1db1`，真实复测生效）；断网/DNS/TLS/5xx/慢响应/额度/计费由离线故障矩阵钉住（main `5cb75eaf8`）。子代理提交阶段原因码（main `42f7e57e1`）与能力推荐观测（main `bd4bca4d8`）已合入。真实额度耗尽样本无法在不消耗真实额度、也不伪造供应商的前提下制造，只由离线矩阵覆盖，不宣称已实测。累计官方 Jev 尝试 86 次；延迟分布、各点采用与判断质量、输入 token 与遗漏清单见[P4-G 汇总报告](DECISION_MODEL_P4G_REPORT.md) |
| 14 / P5-A/B | ✅ 已完成（首片 + 真实收益验收） | 召回前判断与记忆写入建议 | A：默认关闭的补充查询已接原正式上下文，136 项离线回归通过；只用词面检索时按构造补不回事实；召回来源已写进上下文包（main `ab23a2666`）；语义召回（MiniMax embo-01）下的真实实验中，3 条已知漏召回样本 2 条稳定补回（4/4 次），答复由"查不到"变准确，另 1 条 Jev 稳定选错片段，对照样本无副作用。B：关系提示先用真实 Jev 验了 12 对短样本；真实 Curator 两对 off/apply 中，提示让 M2.7 稳定把更新类事实归入原条目类型并指向原条目，所有建议仍经原提取、校验与晋升，正式事实未被改动。两点默认关闭；片段选择材料、主模型无长期事实检索工具、重复嵌入、关系对按顺序截取四个缺口已登记台账，见[真实验收](DECISION_MODEL_REAL_VALIDATION.md#14-p5-a-召回前补充查询语义召回下的真实收益2026-09-25main-ab23a2666) |
| 15 / P5-C | 🔄 进行中（五点已接，自学习来源待补） | 其余决策点逐个接通 | 已归档 web_fetch 阅读顺序与现有 Todo 优先级软建议已接，均有隔离 Jev 成功/回退证据但实际业务收益待验。交付复核焦点 `delivery_quality` 已合入并做过真实 TUI 样本：链路成立，但没有实际追加提示，无收益证据；它只在新的验证事件上触发，"改后未复核就交付"的触发设计已登记台账；样本还暴露并修复了验证账漏记 `cd … &&`、管道冒充通过等分类问题。自学习 S1（提案/确认链）与 S2（审核顺序 `skill_proposal_review`）都已合入 main（`e9ead5ae3`、`1132fd9d0`）。但真实验收发现子代理没有结构化方式上报 lesson，S1/S2 在当前产品里无法由真实子代理触发；独立工具 `record_lesson` 的修复正在实现（分支 `claude/subagent-lesson-ledger`），完成后做端到端真实验收。S1 的 lesson 来源现已改为结构化通道：子代理调用 `record_lesson` 写本 run 的 `lessons.jsonl`，宿主收口时并入 `lessons` 并生成带 task/run 与账本引用的 `subagent_lesson` 候选（该分支本地实施、离线验证通过，真实验收待做）。动作候选需先由插件 SDK 提供宿主可验证的观察候选结构（设计已登记），尚未接入 |
| 16 / P5-D | ✅ 已完成（真实采用与保留样本） | 主会话自动选模型 | 原线程版本及 Gateway 观察已接；完整请求验证、发送前 CAS 和失败保留的 fake HTTP 片已通过。用途标签 `usage_tags` 与采用模式问题说明已合入 main。真实样本共 8 个：前 5 个（超时、need_data、三次选当前模型）暴露了候选材料和说明的问题并已修正；第 6 个超出窗口的长资料任务由 Jev 选 M3，宿主核对容量与工具后自动采用并答对（同一任务在 M2.7 上两次答错）；第 7、8 个普通短任务 Jev 选保持 M2.7，没有误采用。只在新工作片的首个完整请求前尝试一次，原历史与上下文保持正确。限制：样本少，视觉、推理密集等任务类型需要候选带对应的用途或模态事实后再验，见[真实交接](DECISION_MODEL_MAIN_MODEL_LIVE_HANDOFF.md) |
| 17 / P5-E/F/G/H | ✅ 已完成（正向晋升缺真实样本） | 有预算的自测与调参 | 设置恢复原语（G0）；E1 授权信封与原账预留；`/experiment` 授权入口、经验输入上界与连接前单次发送硬门（main `dfa8e498b`，两次真实授权发送的结算额都等于计费，比例 0.43）；E2 对照记录与 F1 证据评估、授权内经原 CAS 自动晋升（main `84d873c9b`）。真实验收：同一线程三次 `/experiment apply`，记录、授权链与按实际结算都成立，因没有可收起的插件工具，三次都按"无节省"继续观察、不晋升，符合"没有收益不开启"。**显式缺口**：正向晋升（applied）路径没有真实样本覆盖——隔离 owner 没有插件，收起数为 0，规则正确拒绝晋升。补样本需要一个装有插件的隔离 owner，不用用户的真实 owner；在此之前该路径只有组合测试证据。当前 F1 是用户授权的永久晋升，不做临时试验，所以不需要 G1 自动收尾；到期或撤销不回滚，`reset` 恢复继承。各点的收益边界见 [P4-G 汇总](DECISION_MODEL_P4G_REPORT.md) |
| 18 / 最终集成 | ✅ 已完成（集成、全仓回归与交接） | 集成、文档和完整交接 | 决策线全部分支都经本地严格门后由主线 owner 合入并部署双机。收尾全仓回归（main `5fca6a194`）21,406 passed、4 failed，全部已归因：实验回执的 `**fields` 违反架构守卫已修（`0bb6f3a88`），插件代理激活复核的两项测试已由插件线修复（`a5514f473`），剩下一项是全仓负载下的时序偶发（单独 3/3 通过）。最终交接见 [决策模型最终交接](DECISION_MODEL_FINAL_HANDOFF.md) |

### 暂停前历史分工（当前代理已停止）

以下为历史分工与未完成事项，不代表这些代理仍运行；新接手者按交接重新分工。

| 负责人 | 当前项 | 已有进展 | 剩余交付 |
| --- | --- | --- | --- |
| 子代理 A（收尾中） | 12 子代理自动采用 / 13 真实验收 | 三候选自然建议官方 M3、受限候选建议 OpenCode DeepSeek 均被自动采用并完成工具轮；六父任务十 child 全部完成，20 次 Jev 账已核 | 故障矩阵、第四轮 selection_changed 原因及多 owner 样本仍未关闭 |
| 子代理 B（已交接） | 16 主会话选模 | Gateway 车道内完整请求验证、发送前自动采用与失败保留本地302项联合通过；本片34项通过 | 主线复核，隔离 Gateway 真实跨 provider 主会话验收 |
| 子代理 C（收尾中） | 11/13 普通 owner 设置真实复测 | user_config decision-only、可信 thread 与 CAS 回执预览修复；原 TUI 一条中文 read→thread patch 成功并准确回复 | 主线复核与组合测试；其它 owner/故障场景仍属 13 |
| 子代理 E1（已交接） | 17 有界试验原语 | 设置 v2 授权信封及撤销、原 ModelCallLedger 同锁预留、TUI 能力开关，本地组合 356 项通过 | 宿主用户授权与原操作回执、可靠输入上界、发送前硬门；联网保持失败关闭 |
| 主代理 | 14-A、12/13整合及 17-G0 | P5-A 正式上下文补充查询136项离线通过；Jev协议窗口、隔离TUI与三子代理异模执行、设置恢复原语已接 | P5-A 真实效果、主会话异模和故障矩阵；17 的授权、硬预算及可信对照 |

### 10 的可核对小项（不重复计入18项总数）

- [x] 10.1 Skill名卡展示子片已交接：实际减少名卡，原授权/搜索保留；尚未代表整条业务链完成。
- [x] 10.2 工具schema展示子片：收起可选工具，原搜索可找回，显式和已加载工具保留。负责人：max。
- [x] 10.3 决策消费与设置：每工作片一次决策、设置可调、采用前核对撤销/期限。负责人：主代理。
- [x] 10.4 组合验收：证明实际模型输入减量、关闭等价、超时恢复原输入、插件停用不复活。负责人：主代理整合。

后续进度只按交付节点更新；测试数量和审查轮数不增加完成项。10证据见[完整交接](DECISION_MODEL_CAPABILITY_HANDOFF.md)，当前没有外部阻塞。

### 紧接着交付的节点

第 12 项按以下验收点推进；它们只是上表第 12 项的细分，不另计总完成数：

- [x] 12.1 首请求候选沿原完整 prompt、provider builder、目录代次和线程 CAS 自动采用；未知或失败自动保留原模型。本地通过，真实主会话跨模型仍归 13/16。
- [x] 12.2 原生预检和出站共用消息清扫、引导及 ToolChoice；旧校准失效，主子候选 options 与实际发送一致。六文件153项通过。
- [x] 12.3 Gateway、child 和后台在同一 turn 的 Compact 中保留已评估展示；新轮重置、失效清除不重发决策。后台既有主 run 身份沿原发布回传；宿主及相关回归236项通过，真实缓存收益另验。
- [x] 12.4 Compact 候选复用真实恢复准备的完整输入，并与恢复后首个实际 payload 对照。Gateway overflow 已本地接通：原请求只准备一次，候选与实际HTTP逐字对照，CAS/取消/未知/摘要瞬时错误不发送恢复业务，第二工具轮沿新历史继续。child overflow已接公共完整恢复并对照两协议实际HTTP；后台已完成一次准备/纯渲染与范围冻结，v3作用域检查点及精确调用覆盖底座、后台scope/view选择和摘要注入/隐藏已本地实现，后台transcript与carried活动归档已接公共完整恢复，Gateway/child同视图准备也已统一；同片范围冻结、候选CAS后准确view回填保持。Gateway/child无transcript活动归档已迁移至同入口；混合transcript+carried候选已同时摘要、完整计量并经单次CAS提交，选中工具全部读取原模型投影，未知身份仍保留。混合来源前片32文件738项和本地严格gate通过。首次Gateway/child/后台已接同次完整准备与选模前安全点，手动按车道内全线程来源和准确历史估算处理；本次联合验收见TESTS。真实native ToolCall/ToolResult的同源分区、完整摘要和精确保留投影已本地实现，39文件1039项及本片严格gate通过；外层溢出原生IR carry已本地接三宿主，44文件1154 passed、4既有xfail及严格gate通过；完整正文、精确输入释放、身份和唯一摘要均有回归。该片后来随319004926在独立测试机安装，不按整项完成。媒体已本地整合，未知模态保留原模型，强制Compact在摘要/CAS前拒绝，文字前缀与媒体后缀分区55文件1419 passed、4既有xfail及本地严格gate通过；集成安装版文字TUI三轮及供应商缓存回执已验，集成媒体、超大历史及Compact后缓存仍待验，不能用摘要请求缓存面替代下一轮输入。 原MessageStore固定尾界/显式字节页和幂等流式扫描底座已本地153项通过；显式scope来源已接两遍固定EOF范围筛选，后台先读事实后选正文（含recent_limit=0）；12文件326项通过，另4项主线独占fake Store签名待适配。来源仍保留ID位置及未压正文，旧入口、分段和覆盖链尚未完全有界；保留历史二次裁剪已本地接三个宿主的完整投影，73项联合及416项相邻回归通过（含重叠，不累加）；原容量门保持，未知超量拒绝不丢来源。摘要序列化和分段已本地改为只读顺序窗口，原估算流式等值，纠正预算及发送/EOF取消补齐，15文件316项及非pytest守卫通过；原messages和覆盖链仍驻留，不视为全链有界。检查点读取随后改为同FD固定EOF行索引及逐条验证，summary view只留适用正文与覆盖元数据；9文件98项通过，旧摘要内存峰值显著下降，但精确覆盖IDs/选中messages仍随历史增长，整项不勾选。后续native投影减少已隔离正文的重复深拷贝，匿名重复行输出仍各自隔离；移入主线b4ffb3475的小JSON有界编码修复，两片三文件72项通过，全链来源驻留仍开放。此前4项后台fake Store缺口现经owner授权适配测试接口及真实临时消息域，整文件162项通过；不改变生产链或据此消除其它历史失败。（2026-09-24 收口：2a、2b、Gateway 流式读取、媒体集成与越压缩点修复均已合入并真实验收，请求准备阶段压缩已在真实 TUI 触发；建循环首次物化按实测不做，媒体屏障主链由主线 owner 的片 A 完成。）
- [x] 12.5 候选接受同时核对原 Compact 阈值与实际输出预留（本地合同与组合验收）。三宿主、Chat/Anthropic完整冻结候选保留当前要求与50K输出cap，Gateway包含真实工具schema；低于180K原trigger但超过150K剩余输入时零业务发送、零checkpoint/CAS，合法候选与实际payload一致。Responses普通出站max_output_tokens与OAuth未知cap另验，等号和未知投影沿既有边界测试。十文件187项联合通过；新测试增强schema/失败码断言后另14项通过（重叠不累加）。这是原估算口径的容量门验证，不是供应商精确计量；12.4超大来源、12.6跨窗/故障和12.7真实缓存仍开放。
- [x] 12.6 跨窗口、协议与模态未知、连接变化校准失效及并发故障的本地组合验证。六文件123项通过：近窗口完整要求/原工具历史/schema保留，小窗候选拒绝仍使用原模型；双会话两种窗口方向下关闭/显式选择/准入忙/未知协议相互隔离；连接及窗口变化后旧校准不复用，活动快照保持。未知模态沿原Gateway/child/容量测试联验。真实供应商计量与跨模型缓存另归12.7/13/16，不据此声称真实自动选模已验。
- [x] 12.7 真实供应商usage的缓存对照（限定已验组合）：固定5e5122b04安装版同一原生TUI完成M2.7两轮→原/compact提交1代→M2.7续聊、on4建议保留、原菜单显式切换官方M3和OpenCode DeepSeek并各完成真实工具轮。七条业务done/completed，主/摘要/决策按原账分别统计；压缩后M2.7缓存读20222，M3后续工具轮59276（2/2报告），DeepSeek36352（1/2报告），缺报保留未知。Jev新增8次、累计65次。此片证明实际缓存回执及显式跨模型续接，不证明供应商间共享缓存、净token收益或Jev真实异模自动采用；后者仍归13/16。见真实验收记录。

1. **12 窗口/缓存/并发**：集中检查完整子上下文、切换与故障；缓存收益以真实usage为准。
2. **13 真实模型与TUI**：已获凭据和测试授权，继续按验收矩阵；不把接口小样本或按键管道冒充完整质量。
3. **14—18 完整后续范围**：召回前/写入、其余点、主模型选择、受控调参及最终集成继续保留。

12.4最新进展：来源到摘要首片已本地实现并通过26文件530项，完整消息改为同源地址重放，摘要不驻留全量provider数组，完整hash/精确覆盖及故障不提交有证据；三宿主旧请求、无scope入口及轻量索引/landmark边界仍开放，未部署，不能据此勾选整项。见[容量审计](DECISION_MODEL_CONTEXT_AUDIT.md#选中正文生命周期方案2026-09-23首片本地验收)。

### 当前阻塞和进度口径

- 10已本地完成；12先独立调查完整输入预算，没有外部阻塞。
- 11已支持原菜单和原 user_config 代操作；普通 user owner 一条真实中文 read→thread patch→持久回执已在隔离 Gateway 通过，未发生独立第三次 read，真实质量与多 owner 故障组合继续归 13。
- 07完整child prompt/模态容量仍属12；创建前估算不冒充完整容量证明。
- 子代理决策现支持用户/agent 经原设置服务修改结构化候选 profile ID 列表；空数组仍列出全部可用 `agentic` 模型，非空数组只缩小范围。隔离测试目录只保留用户指定的官方 M2.7、官方 M3 与 OpenCode DeepSeek 候选；正式候选设置和跨模型真实切换仍待验，不能按模型名称猜官方来源。
- 13已获真实Jev凭据与充分测试授权；隔离配置不改日常模型，六轮子代理累计20次、主会话新三轮5次、普通中文配置两轮各1次 Jev HTTP，新增集成安装版三轮3次，全线累计57次。官方 M3 是三候选自然选择，DeepSeek 是候选受限条件，均非选中率或质量统计；主会话三轮均保留 M2.7，不能算真实异模采用。失败先定位修复再复测。
- 无新失败不重复扩大底层测试；每次只报告新完成项、当前项、具体阻塞和下一交付。

06 已完成自动活动用量刷新；原持久显示由下一模型边界保存，不为可选显示阻塞会话写锁。
本地 HTTP 8 项组合覆盖正常/观察/关闭/错误/超时/在途关闭/写锁忙，不代替真实 Jev 判断质量。
本线未发布/部署；并行未交接文件不计入已完成分片的本地提交或发布验收。
08 已完成原 Curator 117 项与父侧身份/HTTP/消费者 62 项组合，后台用量不伪造为前台会话。
06/08 已分别提交 `e131b1f6e`、`0dc960034`；消费前公共配置/期限复核补充提交 `3fc3485b7`，四文件组合 95 项通过。
07 已改用原 ToolExecutor 的 ToolRuntimeSnapshot；真实 RunParams 不补字段。当前仓库工具协议仅支持 native，保持原 child runner 探测，不要求候选事先有成功探测缓存。
09 的预算后重排/来源撤销、完整准备入口与本轮复用已通过父侧验收；07/09 父侧组合 83 项通过。

- 11已完成：原菜单/薄客户端/Gateway/原生HTTP/原账本及agent工具入口联合243项通过；不等于安装版或收费模型已验。
- 10已完成本地原HTTP及真实生成输入投影；下一交付节点是12，真实模型质量仍属于13。
- 07 不在创建锁内等待网络；08 不冒用历史消息身份、不改变记忆写入和游标权威。
- 12若需改共享运行入口，继续先与插件线核对精确文件/函数归属。
- 没有新失败证据时，不重复扩大基础层测试或继续增加可选功能。
- 每次完成一个验收项更新本表；汇报只说完成编号、当前编号和具体阻塞。
- 真实凭据与授权已核对，首批7次及真实失败证据见[真实验收记录](DECISION_MODEL_REAL_VALIDATION.md)；没有等待用户确认的阻塞。

“11/18”是清单项数，不是工作量百分比。组件、本地业务链、真实模型和实际安装版 TUI 分别验收。

## 总目标与完成条件

在 my-agent 原生模型体系中加入可选决策模型，保留完整基础能力。用户和 my-agent 按用户意愿共用设置，
能调整时间、选择决策模型、逐点开启/观察/关闭；超时、没额度和错误不拖垮原执行、工具或记忆流程。
复用原配置、授权、取消、资源、调用账本、子代理、记忆和上下文机制，不迁入整个外部 agent/harness。
P1—P5 全部逐项实现并验收，文档与真实限制同步，才可以关闭整个 Goal。
未具备凭据、请求测试授权、接口稳定点或测试环境的项目必须明确留待验证，不能用 fake 结果计为真实通过。
质量不足的接入点继续默认关闭，失败证据保留；不得为宣称成功放宽权限、遗失必要材料或制造效果统计。

## P1：配置、协议、时限与故障隔离

- [x] P1-A：原 provider/model schema 增加 decision 用途与 Jev 协议；显式持久迁移，原模型 ID/凭据/选择保持；主子执行模型不能误选 decision。仅本地源码/合同，不表示已能发起决策请求。
- [x] P1-B：Jev 请求与各题结果的严格协议校验，候选 ID、版本、来源和 usage 保留；无依赖也能启动基础产品。
- [x] P1-C（本地）：复用请求预算、取消和传输，单次/阶段时间可调；默认前台 2/4 秒、后台 4 秒，完整请求总期限、默认零重试。
- [x] P1-D（本地）：错误即时保留原方案；精确请求取消、迟到结果失效、未退出资源有界；冷却及恢复不禁用主模型、不新增后台守护。
- [x] P1-E：共用读取、字段级修改、恢复继承及有效值读回；主开关、逐点模式、时间覆盖、版本冲突、临时/长期范围已本地实现；原 user_config 已接，完整菜单归 P4。
- [x] P1-F（本地）：同一 ModelCallLedger 与并发准入组件接线；决策不进入 USD 价格估算或 owner/run 成本累计，失败、取消、未知输入用量不报零；状态查询不泄露密钥，不改稳定模型前缀。
- [x] P1-G（本地）：关闭等价、配置双入口、真实慢 HTTP 假服务、错误类型、竞态与资源回收的本地定向验收。

退出条件：最小链有真实可调用入口；开启正常、关闭等价、短期限失败和不可用配置均有证据，不能只有孤立协议类型。

## P2：子代理模型选择与记忆前置标注

- [x] P2-A（本地）：子代理创建前一次决策；显式模型优先、父子孙继承保持、批量共用阶段预算、单项失败保留原选择。
- [ ] P2-B：宿主在主代理派工时自动完成授权/模态/工具/窗口客观核验、候选与最终配置冻结；重复请求和恢复不重新选择或重复创建，不要求用户逐子代理确认、选模型或补决策资料。
- [x] P2-C：原 Curator 批次提取前增加临时标签和优先级，保留原始材料与来源；不建立另一后台代理或记忆库。
- [ ] P2-D：各点 off/observe/apply；need_data、not_needed、no_match、abstain 和请求失败分开，局部结果独立处理。
- [x] P2-E：原记忆提取、验证、晋升、未解决集合与游标不由决策输出直接控制；失效建议不落盘。
- [ ] P2-F：关闭/成功/超时/额度/取消/恢复对照，继承与幂等、原始批次完整性、候选引用和输入 token 归属验收。

退出条件：两个真实业务入口可配置、可观察、可关闭，失败实际沿原路径执行，定向和实际场景证据分开保存。

## P3：召回与能力推荐

- [x] P3-A（本地）：原召回后候选重排/筛选，保持 owner 和来源权限、必要材料及原文可恢复；故障回原排序和原预算。
- [x] P3-B（本地）：Skill/tool 索引推荐，沿现有快照和按需读取；实际执行仍有正确 schema/正文，推荐不扩权。
- [x] P3-C（本地）：插件接口稳定后接能力推荐，目录/激活变化复查，旧建议不能复活已撤销工具；不复制 Registry。
- [x] P3-D（本轮复用）：结果复用先依原 operation/run；有明确收益时增加有界进程缓存，权限/输入/模型/策略版本变化失效。
- [ ] P3-E：稳定前缀、工具集合变化、跨模型上下文与输入 token 对照；缓存命中以实际 usage 为准。

退出条件：不因推荐隐藏必要工具或丢失关键记忆，插件故障/停用与普通核心任务并行互不越界。

## P4：完整交互与端到端验收

- [x] P4-A（本地交互）：原模型设置入口完整支持 decision 新增、编辑、选择、测试、模式、时间覆盖及恢复继承；保存不隐式发模型请求。
- [x] P4-B（一条真实中文链）：用户普通中文要求经主模型转换为宿主配置动作；可信 user owner 已自主 read→thread patch，原事务回执与持久文件可核。更广的 owner/故障组合归 P4-F/G。
- [x] P4-C（本地范围/运输）：不同 owner、不同 TUI、进行中修改和关闭、服务失效仍可操作配置；明确设置值与实际有效值。
- [x] P4-D：250K/1M 等不同窗口、token 计数差异、输出/推理预留、工具历史完整、既有 Compact 与稳定缓存验证。
- [x] P4-E：先本地合同/fake/replay，再少量真实 Jev + 官方 MiniMax-M2.7 实际 TUI；核对真实 provider/端点及原账本。
- [ ] P4-F：正常、慢响应、断网、硬额度、取消、关闭、服务恢复、批量派工、Curator 与插件并行的组合证据。2026-09-24 本地故障矩阵（分支 `claude/decision-fault-matrix`）已经真实传输栈钉住八类故障的回退、冷却与恢复边界，并修复同 owner 多会话并发时设置读取互挤成 `settings_busy`（12 个会话只发出 1 个）的缺陷；真实 Jev 故障样本已由第 13 项补齐（多 owner 断网、挂起、瞬时故障与退避复测）；仍缺：Gateway/TUI 关闭时在途决策的主动取消（当前只有设置变更会取消，在途调用靠期限收尾，最长等一个期限）、Curator 与插件点并发的组合。
- [x] P4-G：中文质量、延迟分布、关键遗漏、实际采用率与全任务输入 token/请求次数报告；功能链和模型判断质量分别结论，不估算决策价格。（见[汇总报告](DECISION_MODEL_P4G_REPORT.md)）

展示约定（用户明确）：现有用量行沿用 LLM 风格，额外显示决策输入 token；不显示价格，输出位置预留且当前留白。
这不删除供应商原 usage 或内部预算事实；缺少用量仍是未知，不能为了展示补零。

退出条件：直接用户操作和 agent 代操作均可复现；缺少真实凭据/预算/环境时不宣称完成实测。

## P5：全部后续接入点与受控自动调整

- [x] P5-A：召回前判断，保留用户显式查历史要求；不确定或故障时沿原召回。（首片：只追加、不跳过原召回；语义召回下已有真实收益，默认关闭，见第 14 行）
- [x] P5-B：记忆候选分类/合并/写入建议，经原验证与晋升；不能直接删除、覆盖人格或将分数变成事实。（首片：提取前关系提示，真实 Curator 对照已验，默认关闭，见第 14 行）
- [ ] P5-C：检索/外部材料排序、规划派工建议、DOM/OCR/工具动作候选、交付质量提示、自学习候选筛选逐点接线与独立开关。（自学习：S1 提案/确认链已合入 main；S2 待确认提案审核顺序 `skill_proposal_review` 已本地实施待审、默认关闭，尚无真实 Jev 验收；DOM/OCR 动作候选仍未接。）
- [x] P5-D：主会话自动模型选择仅在用户授权候选范围和新工作片边界生效；原历史、上下文与进行中调用保持正确。（真实跨模型采用与短任务保留均已验证，见第 16 行）
- [x] P5-E：有范围、有时间/请求次数/输入 token 预算的对照试验，复用原任务与调用记录；记录基线、实际结果、来源和配置版本。（`/experiment` 授权、经验上界、发送硬门、E2 请求记录；真实验收见第 17 行）
- [x] P5-F：基于证据提出设置差异；用户授权范围内可自动应用，人工后续修改优先，版本冲突不覆盖，撤销停止未来试验。（F1a 评估与 F1b 授权内 CAS 晋升；正向晋升的真实样本需装插件的 owner）
- [x] P5-G：恢复前值使用原配置服务及并发校验，不回滚已执行业务、不重放未知副作用；授权外变更不应用。（G0 恢复原语；晋升回执 `promoting` 遗留按不确定处理，不重试、不反向恢复）
- [x] P5-H：每个接入点关闭等价、故障隔离、输入/消费关系和收益边界验收；没有收益的能力不默认开启。（见 P4-G 汇总报告；所有点默认关闭）

退出条件：P5 不再作为“以后再说”的总待办；各点有实现和验证结论，自主试验/调参有明确授权及可撤销的预算边界。

## 最终集成与交接

- [x] 每片同步中文配置、双层注释、设计、树与模块测试文档，删除不再使用的中间实现。
- [x] 与插件线精确集成，不覆盖其文件或文档；共享入口只有一个实施者，独立 worktree 本地验证先行。
- [x] 远端提交/合并前通过 focused pytest、Ruff、doc sync、strict code-size、diff、clean-package；全仓频率遵守 AGENTS.md。
- [x] 如需部署/重启，提前与插件线同步目标和验收窗口，保留回滚证据，同机保持一个 Gateway。（部署由主线 owner 执行；决策线真实验收只用测试机隔离目录与 8431）
- [ ] 汇总源码、离线、真实接口、实际 TUI、模型质量各自证据与限制，不混为整体通过。
- [ ] 最终按 HANDOFF_TEMPLATE.md 给出文件、验证、风险和建议下一步；所有必做项完成才把应用 Goal 标为 complete。

## 当前认领与执行记录

2026-09-22：用户授权 P1—P5 完整实施。应用 Goal 已 active，并引用设计文档。
“模块重构”已明确确认 P1-A 配置/后端范围无重叠，允许本线独立推进；其当前插件命令审批与传输文件不触碰。
本线公共 HTTP/取消实现仍先审查，实改前再报函数；模型菜单和 Gateway 接线暂不占用对方公共 TUI 文件。
主代理处理 P1-A 模型 schema/配置；最高档子代理只读审查总期限与资源取消，高档子代理只读调查配置双入口。
用户已明确最高推理档为 **Astra max**，不是 ultra；复杂跨模块状态、并发和模型切换审查使用 max，普通独立工作使用 Astra high 或 GPT-6 Sol high，较难的独立开发可用 GPT-6 Sol xhigh。
收到纠正时已中断在用 ultra 的 HTTP 子任务，保留文件并交给新建的 Astra max 子代理继续。
当前未安排真实模型、部署或 Gateway 重启。

P1-A：本线已 rebase 到插件线已提交的 `4974fe760`，保留双方共享文档段落，未接触其未提交改动。
原模型目录 v3、用途显式解析、共享/子代理隔离和只读迁移已实现；10 个相关测试文件 213 项通过。
只读复核后修复旧表单丢失 decision 用途及旧 v1 迁移丢失自定义头；原管理表单支持新增/编辑决策配置。
范围与证据见 [P1-A 交接](DECISION_MODEL_P1A_HANDOFF.md)，其余阶段不能借本片验收提前勾选。
首轮既有迁移测试仍预期 v2，已随实际 schema 升级修正；没有真实模型、日常设置或部署变更。
参考核对：OpenAI Agents 的 `models/interface.py`、`model_settings.py` 与对应索引；Codex 的 `config/edit.rs`
与对应索引，借鉴用途接口与结构化字段修改，未复制实现或增加依赖。
独立审查确认：现有 HTTP 有两层重试、短超时下限和无界错误正文；Curator worker 超时仍额外等待，
准入不能让可选调用占满主模型名额。P1-C/D 将从原单次调用原语迁出复用，不能仅在外面套线程超时。
Python 线程方案只承诺按时放弃等待、残留数量有界与迟到结果不采用，不承诺阻塞 DNS 在期限内强制退出。

P1-B 与 P1-C 传输部分：已实现原生 state/questions、输入深快照、宿主来源和版本绑定、逐题答案校验，
以及独立 `decide` 适配器；复用原 BackendOptions、身份头和 HTTP。协议/适配器 59 项本地测试通过，
与严格 HTTP、原 Gateway/预算及模块边界联合 7 个文件 216 项通过。真实本地 HTTP 验证完整中文请求、
慢响应、重定向及响应关闭；不是供应商真实模型验收。见 [P1-B 交接](DECISION_MODEL_P1B_HANDOFF.md)。
逐题隔离仅针对合法 JSON 内的答案错误；重复字段/非有限数/编码损坏按原 strict_json 整包拒绝。
输入编码前同时计算字符串和整数位数上限；未知用量不补零。结构检查发现两处深嵌套已按校验职责拆分，未放宽尺寸基线。
原 HTTP 严格总期限、零重试和响应上限由 max 子代理接续完成；严格 JSON 先做 64 层传输资源预检，
领域校验仍归适配器。有界 worker/准入/取消唤醒/账本仍未接线，P1-C/D 不能借传输验证勾选。

P1-C/D 组件已实现：从 Curator 迁出原有界调用，原旧线程/队列/集合实现已删除；
精确句柄保留启动前取消，唤醒不等待慢连接关闭，worker/cleanup 实际退出前继续占用有限资源。
原准入器新增非阻塞可选领取与普通名额保留，联合 18 个文件 315 项通过；
没有实际决策服务/冷却/配置/账本接线，P1-C/D 整项仍不勾选。见 [组件交接](DECISION_MODEL_P1CD_HANDOFF.md)。
另一任务明确确认 llm_scale/concurrency.py 也无重叠。
参考核对原 Codex 合同索引与 guardian/review_session.rs 的 try_acquire 生命周期，只借鉴非阻塞领取，
不迁入其旁支执行策略；普通请求和可选请求共用原准入计数。

P1-F 只读调查已用无网络小复现确认：原账本 failed/timed_out 可被迟到 first_token 重新打开；
只含 output usage 时原封装会把估算输入标为 provider 已报；thread-local attempt observer 不随 copy_context 传到 worker。
后续须通用修复终态单调性、逐字段用量来源，并由实际 worker 安装原 observer；当前没有接决策调用账本。
原显示还把辅助调用计入轮次，且独立 usage_scope 可能被主请求缓存的旧历史基数覆盖；应优先共用原 request/run 账，
决策刷新只改用量，不覆盖主模型的 pending、工具数、最近 cache/tps，不能另开一份统计账。

建议下一步：先完成12的完整child输入、窗口/输出预留、缓存/Compact及并发验收；max独立查复用接缝，主代理整合，精确缺口可交high并行。
插件线独立继续；共享入口先明确归属，本线不修改日常设置、不重启 Gateway，不用本地假服务结果代替真实供应商验收。

## 2026-09-23 接手记录与当前 Goal

状态：active。原代理已按用户要求暂停，交接提交为 `46ac29601`；用户指定新的接手代理按[交接报告](DECISION_MODEL_TAKEOVER_HANDOFF.md)继续推进，并让接手者自行设定 Goal。

**当前 Goal（接手者设定，范围不缩减）**：按[设计合同](../design/DECISION_MODEL_INTEGRATION.md)和本文件，完成 P1—P5 全部 18 项，每项按“合同单测 → fake → replay → 少量真实 TUI”验收。同时遵守交接报告 §1.2 列出的 12 条用户后续要求，主要包括：
- 决策默认关闭，失败时立即沿用原方案。
- 不做价格计算，只显示决策输入 token。
- 子代理模型由宿主自动选定，用户不需要逐个操作。
- 普通真实模型用官方 M2.7，M3 走官方原生入口，OpenCode 只用 deepseek-v4-flash。
- P5 不能缩成“以后再说”。

源码、离线、真实接口、安装版 TUI 和模型质量这几类结果分开报告，局部通过不能写成整项完成。

**并行边界（已与“模块重构”接手者逐条确认）**：
- 本线只在本 worktree（分支 `codex/decision-model-integration`）写代码。主 checkout、远端 main、本机 Gateway 和主线测试机都归主线 owner。
- 本线的真实验收只用决策线独立测试机，沿用原来的隔离目录和端口。第三台测试机磁盘已满，上面还有其他工作线的资源，双方都先不碰。
- 共享函数沿用原约定：本线只改 `runtime/loop_support.py::_native_provider_history_messages` 和 `_tool_loop_service.py::_text_conversation_history_section`，动手前再向主线确认。native Compact 的 plan/commit、live-tool 来源/CAS、active_turn_compact 和 process-index 投影仍归主线。
- 集成方式：本线先在本分支吸收 main，解决冲突并完成验证，再把分支名和 SHA 交给主线 owner 合入。本线不直接推 main，也不部署主线环境。

**顺序调整及原因**：交接建议下一步做 12.4 第二片 2a。接手核对时发现几个问题：
- 主线已发布 `66a598cf3`。与本分支试合并有 33 个冲突文件，其中 13 个是核心生产文件，集中在 Compact 来源追踪和工具循环。
- 两线的 Compact 来源设计不同：主线是 v2 checkpoint 加逐调用 ToolCallRef，本线是 v3 scope checkpoint，需要调和。
- 2a/2b 要动的请求周期，主线第 8 步已经重构过。

因此调整顺序：先完成 18 中“吸收 main”这一步（记为 18-A），再在合并后的基线上做 12.4 的 2a/2b，然后推进 13—17，最后完成 18 的整体集成与交接。这只是顺序调整，任何一项都不缩减。

**当前进行**：18-A，吸收 main `66a598cf3`。合并前已对两侧快照分别跑全量基线，合并后逐项对照，作为冲突归因依据。

**18-A 进展**（本地合并分支，尚未交主线 owner）：
- 33 个冲突文件已全部解决。Compact 来源统一为 v3 加四元身份，主线三元 `call_ref.py` 已删除；模型轮参数改为显式交回。设计与兼容、回滚边界见[依赖拆分合并节](../design/TOOL_LOOP_DEPENDENCY_SPLIT.md#两线合并后的来源身份与模型轮结果决策分支吸收-main2026-09-23)。
- 合并后全量结果：20,623 passed，29 failed。其中 24 项在决策基线或 main 基线已失败；新增的 5 项已修复：4 项是主线新测试改用本线来源接缝和 v3 字段，1 项是改名前的旧用例。
- 已完成的后续步骤：
  1. 依次吸收 main `0d02bb272`、`7c467a4d3`、`3b72c4d7b`；提示文字以 main 为准。
  2. 补齐主线 owner 要求的三类新测试，都做了变异验证：瞬断重试时回调只收最后一次尝试的参数、`request_owns_compact` 完整恢复路径、提交后刷新失败不回滚。
  3. 决策线接手前已有的 9 项失败清零：1 个产品缺陷、1 个合同缺口、5 项过时替身；架构守卫另修。
  4. review 发现 1 单独提交：来源身份无法证明时报 `COMPACT_TOOL_COVERAGE_UNKNOWN`。
  5. "原场景 → 新测试"对照表和受影响测试文件清单写入[交接](HANDOFF_18A_DECISION_MERGE_MAIN.md)。
- 结果：合并分支又吸收了 main 的 tui-scalability 整合和第 9 步，由主线 owner 审阅后合入 main（合并头 `67d10d626`，main `fae9d5855`）。18-A 完成；主线 owner 在新包上跑真实 TUI 矩阵。

**12.4 第二片 2a**（主线 owner 审阅通过，main 快进到 `931f83739`，已随 `d69f30cf3` 部署双机）：
- 种子的具体历史与只读来源二选一，已实现，并通过等价测试和 4.2M 字符前后对照，详见[容量审计](DECISION_MODEL_CONTEXT_AUDIT.md#宿主历史种子只读来源2a2026-09-23本地)。
- 2b（摘要期旧请求释放）已由主线 owner 审阅合入 main `911d0d14d`（已随 `d69f30cf3` 部署双机）：摘要期峰值从 10.2–11.7MB 降到 1.7–3.3MB，详见[容量审计](DECISION_MODEL_CONTEXT_AUDIT.md#摘要期释放旧请求历史2b2026-09-24本地)。建循环首次物化峰值：实测按需物化只会挪动峰值、不降峰，不做（方案 B 结论见[容量审计](DECISION_MODEL_CONTEXT_AUDIT.md#按需物化原生历史方案-b的测量结论不做2026-09-24)）；Gateway 21.3MB 峰值已定位为三处按行数（不按字节）整块读取消息文件，已改为字节有界的流式读取，准备期峰值 21.3→0.8MB，本地分支 `claude/decision-gateway-message-reads` 待审（见[容量审计](DECISION_MODEL_CONTEXT_AUDIT.md#gateway-213mb-峰值来自整块读取消息文件2026-09-24已实施待审)）。后台 messages 驻留与预算挤占的修复已合入 main `5dcdfd463`。

**.9 媒体真实验收（main `5dcdfd463`，2026-09-24）**：集成版的图片输入与手动压缩组合在真实 M3 TUI 中走通：图片原样进请求，压缩只摘要图片前的文字前缀，压缩后仍能依据原图作答。验收后本地复现一个缺陷：未压历史带图时，一越过压缩点，请求就整轮失败。修复在本地分支 `claude/decision-media-preflight`（主线 owner 已同意方案，.9 真实验收已通过，待审）。媒体屏障写进 DESIGN_LEDGER 待用户决策。详见[真实验收](DECISION_MODEL_REAL_VALIDATION.md#124-媒体与-compact-集成版真实验收2026-09-24main-5dcdfd463)和[容量审计](DECISION_MODEL_CONTEXT_AUDIT.md#媒体会话越过压缩点2026-09-24本地修复)。12.4 仍开放：媒体屏障决策、Gateway 消息文件整块读取（已改为流式读取，本地分支 `claude/decision-gateway-message-reads` 待审）。

**恢复候选提交后的同请求重试**（主线 owner 审阅通过，main 快进到 `c9794b9ca`）：18-A 合并后用探针发现，候选发送瞬断后重试会发回压缩前的旧请求；现改为复用已提交候选；经主线 owner 同意，空响应修复和插话取代两个重跑分支的同类问题一并修复（先换成候选参数）。补了 Gateway、后台和单元回归，每条都做了变异验证。详见[依赖拆分](../design/TOOL_LOOP_DEPENDENCY_SPLIT.md#恢复候选提交后的同请求重试决策分支2026-09-24本地)。

**.9 真实验收（main `c9794b9ca`，2026-09-24）**：活动工具精确来源压缩在真实 M2.7 TUI 中走通，五项证据齐全；同一线程的会话级长历史压缩也写出四元 refs，并在下一请求中移除旧原文。请求准备阶段对超长历史的压缩没有在真实运行中触发，仍由 fake 全链测试覆盖。详见[真实验收](DECISION_MODEL_REAL_VALIDATION.md#124-活动工具精确来源压缩真实验收2026-09-24main-c9794b9ca)。
